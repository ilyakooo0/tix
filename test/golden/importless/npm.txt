∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { etc = { npmrc = { text = ∀ α. α } }; systemPackages = ∀ β. [β]; variables = { NPM_CONFIG_GLOBALCONFIG = String } } }; options = { programs = { npm = { enable = String; npmrc = { default = String; description = String; example = String; type = ∀ α. α }; package = { default = ∀ α. α; description = String; example = String; type = ∀ α. α } } } } }