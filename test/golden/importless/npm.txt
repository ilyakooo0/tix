∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.npm = ε,
  γ.lines = ζ,
  γ.literalExample = String -> η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { environment =
        { etc = {npmrc = {text = κ;};};
          systemPackages = [λ];
          variables = {NPM_CONFIG_GLOBALCONFIG = String;};
        };
    } ->
    μ,
  γ.mkOption =
    {default = String; description = String; example = String; type = ζ;} -> ν,
  γ.types = ξ,
  δ.npm = ο,
  ε.enable = ι,
  ε.npmrc = κ,
  ε.package = λ,
  π.programs = β,
  ρ.types = γ,
  σ.nodePackages = δ,
  ξ.path = τ
) =>
α ->
{ config = μ;
  options = {programs = {npm = {enable = θ; npmrc = ν; package = υ;};};};
}