∀ β γ δ ε.
( β.buildPythonPackage =
    { disabled = γ;
      doCheck = Bool;
      meta = {description = String; homepage = String; license = String;};
      name = String;
      src = ∀ α. (δ.fetchurl = {sha256 = String; url = String;} -> α) => α;
    } ->
    ε
) =>
{isPy3k = γ; pkgs = δ; pythonPackages = β;} -> ε