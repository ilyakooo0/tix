∀ α β γ δ ε ζ η θ ι κ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      systemd =
        { services =
            { das_watchdog =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      RemainAfterExit = Bool;
                      Type = String;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    θ,
  δ.das_watchdog = η,
  ι.das_watchdog = β,
  κ.services = ι
) =>
α -> {config = θ; options = {services = {das_watchdog = {enable = ζ;};};};}