∀ θ.
θ ->
{ composeExtensions =
    ∀ α β γ δ ε ζ η.
    ((α) // (β) ~ (γ), (β) // (δ) ~ (ε)) => (ζ -> α -> β) ->
    (ζ -> η -> δ) -> ζ -> α -> ε;
  converge = ∀ α. (α -> α) -> α -> α;
  extends =
    ∀ α β γ δ. ((α) // (β) ~ (γ)) => (δ -> α -> β) -> (δ -> α) -> δ -> γ;
  fix = ∀ α. (α -> α) -> α;
  fix' = ∀ α β. ((α) // ({__unfix__ = β -> α;}) ~ (β)) => (β -> α) -> β;
  makeExtensible =
    ∀ α β γ.
    ((α) // ({__unfix__ = β -> α;}) ~ (β), (β) // ({}) ~ (γ)) => (β -> α) -> γ;
  makeExtensibleWithCustomName =
    ∀ α β γ δ.
    ((α) // ({__unfix__ = β -> α;}) ~ (β), (β) // ({}) ~ (γ)) => δ ->
    (β -> α) -> γ;
}