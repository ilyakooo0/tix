∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { tcpcrypt = { after = [String]; description = String; path = ∀ α. [α]; postStop = String; preStart = String; script = String; wantedBy = [String] } } }; users = { users = { tcpcryptd = { description = String; uid = ∀ α. α } } } }; options = { networking = { tcpcrypt = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }