∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.tcpcrypt = ε,
  β.tcpcryptd = ζ,
  γ.mkIf =
    η ->
    { systemd =
        { services =
            { tcpcrypt =
                { after = [String];
                  description = String;
                  path = [θ];
                  postStop = String;
                  preStart = String;
                  script = String;
                  wantedBy = [String];
                };
            };
        };
      users = {users = {tcpcryptd = {description = String; uid = ζ;};};};
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  δ.iptables = θ,
  δ.procps = θ,
  δ.tcpcrypt = θ,
  ε.enable = η,
  ν.networking = β,
  ξ.uids = β,
  ο.ids = ξ,
  μ.bool = κ
) =>
α -> {config = ι; options = {networking = {tcpcrypt = {enable = λ;};};};}