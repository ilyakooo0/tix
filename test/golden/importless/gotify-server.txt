∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { gotify-server = { after = [String]; description = String; environment = { GOTIFY_SERVER_PORT = ∀ α. α }; serviceConfig = { DynamicUser = String; ExecStart = String; Restart = String; StateDirectory = ∀ α. α; WorkingDirectory = String }; wantedBy = [String] } } } }; options = { services = { gotify = { enable = String; port = { description = String; type = ∀ β. β }; stateDirectoryName = { default = String; description = String; type = ∀ β. β } } } } }