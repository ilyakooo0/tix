∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.grahamc = ζ,
  γ.maintainers = η,
  γ.mkIf =
    ε ->
    { systemd =
        { packages = [θ];
          services =
            { mcelog =
                { serviceConfig =
                    { PrivateNetwork = Bool;
                      PrivateTmp = Bool;
                      ProtectHome = Bool;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  δ.mcelog = θ,
  ν.mcelog = β,
  ξ.hardware = ν,
  μ.bool = κ
) =>
α ->
{ config = ι;
  meta = {maintainers = [ζ];};
  options = {hardware = {mcelog = {enable = λ;};};};
}