∀ γ δ. { config = γ; lib = δ; pkgs = {  } } -> { config = [{ services = { xserver = { displayManager = { lightdm = { greeters = { gtk = { extraConfig = String } } } } } } }]; options = { environment = { cinnamon = { excludePackages = { default = ∀ β. β; description = String; example = String; type = ∀ β. β } } }; services = { cinnamon = { apps = { enable = String } }; xserver = { desktopManager = { cinnamon = { enable = String; extraGSettingsOverridePackages = { default = ∀ α. α; description = String; type = ∀ α. α }; extraGSettingsOverrides = { default = String; description = String; type = ∀ α. α }; sessionPath = { default = ∀ α. α; description = String; example = String } } } } } } }