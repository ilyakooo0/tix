∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.localtime = β,
  γ.mkIf =
    δ ->
    { environment = {systemPackages = [ε];};
      services =
        { geoclue2 =
            { appConfig = {localtime = {isAllowed = Bool; isSystem = Bool;};};
              enable = Bool;
            };
        };
      systemd =
        { packages = [ζ];
          services =
            { localtime =
                {serviceConfig = {Restart = String;}; wantedBy = [String];};
            };
        };
      users = {users = {localtimed = {description = String;};};};
    } ->
    η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.types = κ,
  λ.out = ε,
  β.enable = δ,
  μ.services = α,
  κ.bool = θ,
  ν.localtime = λ,
  ξ.localtime = λ
) =>
{config = α; lib = γ; pkgs = λ;} ->
{config = η; options = {services = {localtime = {enable = ι;};};};}