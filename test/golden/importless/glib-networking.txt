∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { variables = { GIO_EXTRA_MODULES = [String] } }; services = { dbus = { packages = ∀ α. [α] } }; systemd = { packages = ∀ β. [β] } }; meta = { maintainers = ∀ γ. γ }; options = { services = { gnome3 = { glib-networking = { enable = String } } } } }