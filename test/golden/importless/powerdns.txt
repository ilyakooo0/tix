∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { pdns = { after = [String]; description = String; serviceConfig = { CapabilityBoundingSet = String; ExecStart = String; ExecStartPre = String; NoNewPrivileges = Bool; PrivateDevices = Bool; ProtectHome = Bool; ProtectSystem = String; Restart = String; RestartSec = String; RestrictAddressFamilies = String; StartLimitInterval = String }; unitConfig = { Documentation = String }; wantedBy = [String] } } } }; options = { services = { powerdns = { enable = String; extraConfig = { default = String; description = String; type = ∀ α. α } } } } }