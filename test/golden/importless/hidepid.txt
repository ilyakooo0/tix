∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  β.hideProcessInformation = δ,
  β.proc = ε,
  γ.maintainers = ζ,
  γ.mkIf =
    δ ->
    { boot = {specialFileSystems = {/proc = {options = [String];};};};
      systemd =
        { services =
            { systemd-logind =
                {serviceConfig = {SupplementaryGroups = [String];};};
            };
        };
      users = {groups = {proc = {gid = ε; members = [String];};};};
    } ->
    η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.types = κ,
  λ.security = β,
  μ.gids = β,
  ν.ids = μ,
  ζ.joachifm = ξ,
  κ.bool = θ
) =>
α ->
{ config = η;
  meta = {doc = Path; maintainers = [ξ];};
  options = {security = {hideProcessInformation = ι;};};
}