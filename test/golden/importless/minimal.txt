∀ α β γ δ ε ζ η.
( α.config = β,
  α.lib = γ,
  β.defaultLocale = String,
  γ.mkDefault = Bool -> δ,
  ε.i18n = β
) =>
α ->
{ documentation = {enable = δ; nixos = {enable = ζ;};};
  environment = {noXlibs = η;};
  i18n = {supportedLocales = [String];};
}