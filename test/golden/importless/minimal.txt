∀ α β γ δ ε ζ.
(α.defaultLocale = String, β.mkDefault = Bool -> γ, δ.i18n = α) => { config = α;
  lib = β;
} ->
{ documentation = {enable = γ; nixos = {enable = ε;};};
  environment = {noXlibs = ζ;};
  i18n = {supportedLocales = [String];};
}