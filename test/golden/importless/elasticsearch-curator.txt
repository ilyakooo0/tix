∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { elasticsearch-curator = { serviceConfig = { ExecStart = String }; startAt = ∀ α. α } } } }; options = { services = { elasticsearch-curator = { actionYAML = { description = String; example = String }; enable = String; hosts = { default = [String]; description = String; type = ∀ α. α }; interval = { default = String; description = String; type = ∀ α. α }; port = { default = Integer; description = String; type = ∀ α. α } } } } }