∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { services = { mysql = { ensureUsers = [∀ β. { ensurePermissions = β; name = ∀ α. α }] } }; systemd = { services = { mysql-backup = { description = String; enable = Bool; script = String; serviceConfig = { User = ∀ α. α } } }; timers = { mysql-backup = { description = String; timerConfig = { AccuracySec = String; OnCalendar = ∀ α. α; Unit = String }; wantedBy = [String] } }; tmpfiles = { rules = [String] } }; users = { users = {  } } }; options = { services = { mysqlBackup = { calendar = { default = String; description = String; type = ∀ β. β }; databases = { default = ∀ β. β; description = String }; enable = String; location = { default = String; description = String }; singleTransaction = { default = Bool; description = String }; user = { default = String; description = String } } } } }