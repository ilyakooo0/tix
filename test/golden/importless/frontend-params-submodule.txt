∀ δ.
{lib = δ;} ->
{ options =
    { api =
        ∀ γ.
        ( δ.mkOption =
            { default = Bool;
              description = String;
              type = ∀ α β. (δ.types = α, α.bool = β) => β;
            } ->
            γ
        ) =>
        γ;
      healthmon =
        ∀ γ.
        ( δ.mkOption =
            { default = Bool;
              description = String;
              type = ∀ α β. (δ.types = α, α.bool = β) => β;
            } ->
            γ
        ) =>
        γ;
      proxyproto =
        ∀ γ.
        ( δ.mkOption =
            { default = Bool;
              description = String;
              type = ∀ α β. (δ.types = α, α.bool = β) => β;
            } ->
            γ
        ) =>
        γ;
      sni-fwd =
        ∀ γ.
        ( δ.mkOption =
            { default = Bool;
              description = String;
              type = ∀ α β. (δ.types = α, α.bool = β) => β;
            } ->
            γ
        ) =>
        γ;
      tls =
        ∀ γ.
        ( δ.mkOption =
            { default = String;
              description = String;
              type = ∀ α β. (δ.types = α, α.enum = [String] -> β) => β;
            } ->
            γ
        ) =>
        γ;
    };
}