∀ β γ. { config = β; lib = γ } -> { config = { systemd = { services = { nix-gc = { description = String; script = String; startAt = ∀ α. α } } } }; options = { nix = { gc = { automatic = { default = Bool; description = String; type = ∀ α. α }; dates = { default = String; description = String; type = ∀ α. α }; options = { default = String; description = String; example = String; type = ∀ α. α } } } } }