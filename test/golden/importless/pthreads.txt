∀ β γ δ.
( β.mkDerivation =
    { meta = ∀ α. (γ.meta = α) => α;
      name = String;
      preConfigure = String;
      src = ∀ α. (γ.src = α) => α;
    } ->
    δ
) =>
{mingw_w64 = γ; stdenv = β;} -> δ