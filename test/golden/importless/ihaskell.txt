∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { systemd = { services = { ihaskell = { after = [String]; description = String; serviceConfig = { ExecStart = String; Group = ∀ α. α; User = ∀ α. α }; wantedBy = [String] } } }; users = { groups = { ihaskell = { gid = ∀ β. β } }; users = { ihaskell = { createHome = Bool; description = String; group = ∀ β. β; home = String; uid = ∀ β. β } } } }; options = { services = { ihaskell = { enable = { default = Bool; description = String; type = ∀ β. β }; extraPackages = { default = ∀ β γ. β -> γ; description = String; example = String } } } } }