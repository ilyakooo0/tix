∀ α β γ δ ε ζ η.
( α.linux = β,
  α.mkDerivation =
    { installPhase = String;
      meta = {platforms = β;};
      name = String;
      phases = [String];
    } ->
    γ,
  δ.platforms = α,
  ε.lib = δ
) =>
{binutils = ζ; gccRaw = η; stdenv = α;} -> γ