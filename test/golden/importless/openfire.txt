∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.usePostgreSQL = Bool,
  γ.coreutils = ζ,
  γ.gawk = ζ,
  γ.gnugrep = ζ,
  γ.gnused = ζ,
  γ.jre = ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    ε ->
    { assertions = θ;
      systemd =
        { services =
            { openfire =
                { after = [String];
                  description = String;
                  path = [ζ];
                  script = String;
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.openfire = ζ,
  γ.optional = μ -> String -> [String],
  γ.singleton = {assertion = Bool; message = String;} -> θ,
  γ.types = ν,
  γ.which = ζ,
  ξ.openfire = β,
  ο.services = ξ,
  π.openfire = β,
  ρ.services = π,
  σ.postgresql = β,
  τ.services = σ,
  υ.openfire = β,
  φ.services = υ,
  ν.bool = κ
) =>
α ->
{ config = ι;
  options = {services = {openfire = {enable = η; usePostgreSQL = λ;};};};
}