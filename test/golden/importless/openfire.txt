∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { assertions = { assertion = Bool; message = String }; systemd = { services = { openfire = { after = [String]; description = String; path = ∀ α. [α]; script = String; wantedBy = [String] } } } }; options = { services = { openfire = { enable = String; usePostgreSQL = { default = Bool; description = String; type = ∀ α. α } } } } }