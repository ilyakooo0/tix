∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω.
( α.isDarwin = Bool,
  α.isFreeBSD = Bool,
  α.isMusl = β,
  α.licenses = γ,
  α.maintainers = δ,
  α.mkDerivation =
    { buildInputs = ε;
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = [ζ];
          maintainers = [η];
          platforms = θ;
        };
      nativeBuildInputs = [ι];
      pname = String;
      postUnpack = String;
      src = κ;
      version = λ;
    } ->
    μ,
  α.optional = Bool -> ν -> ε,
  α.optionalString = ξ -> String -> String,
  α.unix = θ,
  ο.lib = α,
  γ.mit = ζ,
  γ.ncsa = ζ,
  π.lib = α,
  δ.vlstill = η,
  ρ.lib = α,
  σ.platforms = α,
  τ.lib = σ,
  υ.lib = α,
  φ.lib = α,
  χ.hostPlatform = α
) =>
{ cmake = ι;
  fetch = String -> String -> κ;
  libcxx = ψ;
  libunwind = ν;
  llvm = ω;
  stdenv = α;
  version = λ;
} ->
μ