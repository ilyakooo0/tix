∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.digimend = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { boot = {extraModulePackages = [ε];};
      environment =
        {etc = {X11/xorg.conf.d/50-digimend.conf = {source = String;};};};
      services = {xserver = {wacom = {enable = Bool;};};};
    } ->
    θ,
  ι.kernelPackages = β,
  κ.boot = ι,
  λ.enable = η,
  μ.xserver = β,
  ν.services = μ
) =>
α ->
{config = θ; options = {services = {xserver = {digimend = {enable = ζ;};};};};}