∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { freeradius = ∀ β. β } }; users = { users = { radius = { description = String } } }; warnings = String }; options = { services = { freeradius = { configDir = { default = String; description = String; type = ∀ α. α }; debug = { default = Bool; description = String; type = ∀ α. α }; enable = String } } } }