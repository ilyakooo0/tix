∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.mailpile = ζ,
  γ.cfg = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ι];};
      systemd =
        { services =
            { mailpile =
                { after = [String];
                  description = String;
                  environment = {MAILPILE_HOME = String;};
                  serviceConfig =
                    { ExecStart = String;
                      KillMode = String;
                      KillSignal = String;
                      TimeoutSec = Integer;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {mailpile = {gid = κ;};};
          users =
            { mailpile =
                { createHome = Bool;
                  description = String;
                  home = String;
                  uid = λ;
                };
            };
        };
    } ->
    μ,
  γ.mkOption = {default = String; description = String;} -> ν,
  δ.mailpile = ι,
  η.port = ξ,
  ο.hostname = π,
  ρ.services = β,
  σ.mailpile = β,
  τ.services = σ,
  υ.gids = β,
  φ.ids = υ,
  χ.uids = β,
  ψ.ids = χ
) =>
α ->
{ config = μ;
  options = {services = {mailpile = {enable = θ; hostname = ν; port = ω;};};};
}