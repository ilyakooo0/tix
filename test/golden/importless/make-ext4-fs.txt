∀ α β γ δ ε ζ η θ ι κ λ.
( α.closureInfo = {rootPaths = β;} -> γ,
  α.mkDerivation =
    {buildCommand = String; name = String; nativeBuildInputs = [δ];} -> ε,
  ζ.optional = Bool -> η -> [δ],
  θ.bin = δ,
  ι.buildPackages = α,
  κ.stdenv = α
) =>
{ compressImage = Bool;
  e2fsprogs = θ;
  fakeroot = δ;
  lib = ζ;
  libfaketime = δ;
  perl = δ;
  pkgs = α;
  populateImageCommands = String;
  storePaths = β;
  uuid = String;
  volumeLabel = λ;
  zstd = η;
} ->
ε