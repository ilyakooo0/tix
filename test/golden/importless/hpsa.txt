∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.licenses = ζ,
  γ.maintainers = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ε ->
    { boot =
        { initrd =
            {availableKernelModules = [String]; kernelModules = [String];};
        };
      environment = {systemPackages = [ι];};
    } ->
    κ,
  γ.volth = λ,
  δ.dpkg = μ,
  δ.fetchurl = {sha256 = String; url = String;} -> ν,
  δ.mkDerivation =
    { dontStrip = Bool;
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = ξ;
          maintainers = [λ];
          platforms = [String];
        };
      nativeBuildInputs = [μ];
      pname = String;
      src = ν;
      unpackPhase = String;
      version = String;
    } ->
    ι,
  ο.stdenv = δ,
  ζ.unfreeRedistributable = ξ,
  π.HPSmartArray = β,
  ρ.raid = π,
  σ.hardware = ρ
) =>
α ->
{config = κ; options = {hardware = {raid = {HPSmartArray = {enable = θ;};};};};}