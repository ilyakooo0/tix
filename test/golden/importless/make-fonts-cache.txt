∀ β γ δ ε.
{ fontDirectories = β;
  fontconfig = γ;
  lib = δ;
  runCommand =
    String ->
    { allowSubstitutes = Bool;
      fontDirs = String;
      nativeBuildInputs = ∀ α. (γ.bin = α) => [α];
      passAsFile = [String];
      preferLocalBuild = Bool;
    } ->
    String -> ε;
} ->
ε