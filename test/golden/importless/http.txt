∀ γ δ ε ζ.
{ buildDunePackage =
    { checkInputs = ∀ α. (γ.optionals = Bool -> [δ] -> α) => α;
      doCheck = Bool;
      meta = ∀ α β. ((α) // ({description = String;}) ~ (β), ε.meta = α) => β;
      pname = String;
      propagatedBuildInputs = [ε];
      src = ∀ α. (ε.src = α) => α;
      useDune2 = Bool;
      version = ∀ α. (ε.version = α) => α;
    } ->
    ζ;
  checkseum = δ;
  cohttp-lwt = ε;
  git-unix = δ;
  irmin = ε;
  irmin-git = δ;
  irmin-mem = δ;
  irmin-test = δ;
  lib = γ;
  webmachine = ε;
} ->
ζ