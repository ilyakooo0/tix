∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { pathsToLink = [String]; sessionVariables = { MATE_PANEL_APPLETS_DIR = String; MATE_PANEL_EXTRA_MODULES = String; MATE_SESSION_DEBUG = String }; systemPackages = ∀ δ. [δ] }; programs = { bash = { vteIntegration = Bool }; dconf = { enable = Bool }; system-config-printer = { enable = Bool }; zsh = { vteIntegration = Bool } }; security = { pam = { services = { mate-screensaver = { unixAuth = Bool } } } }; services = { gnome3 = { at-spi2-core = { enable = Bool }; gnome-keyring = { enable = Bool } }; gvfs = { enable = Bool }; udev = { packages = ∀ γ. [γ] }; upower = { enable = ∀ γ. γ }; xserver = { displayManager = { sessionCommands = String; sessionPackages = ∀ β. [β] } } } }; options = { environment = { mate = { excludePackages = { default = ∀ β. β; description = String; example = String; type = ∀ β. β } } }; services = { xserver = { desktopManager = { mate = { debug = String; enable = { default = Bool; description = String; type = ∀ α. α } } } } } } }