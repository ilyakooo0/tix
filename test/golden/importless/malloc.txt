∀ γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( γ.memoryAllocator = δ,
  ε.attrNames =
    { graphene-hardened = {description = String; libPath = String;};
      jemalloc = {description = String; libPath = String;};
      scudo = {description = String; libPath = String;};
    } ->
    [String],
  ε.baseNameOf = ζ -> η,
  ε.maintainers = θ,
  ε.mkIf =
    Bool ->
    {environment = {etc = {ld-nix.so.preload = {text = String;};};};} -> ι,
  ε.mkOption = {default = String; description = String; type = κ;} -> λ,
  ε.types = μ,
  ν.runCommand =
    String ->
    { allowSubstitutes = Bool;
      libName = η;
      origLibPath = ∀ α β. (α.libPath = β) => β;
      preferLocalBuild = Bool;
    } ->
    String -> ξ,
  ο.libPath = ζ,
  δ.provider = String,
  π.environment = γ,
  θ.joachifm = ρ,
  μ.enum = [String] -> κ
) =>
{config = γ; lib = ε; pkgs = ν;} ->
{ config = ι;
  meta = {maintainers = [ρ];};
  options = {environment = {memoryAllocator = {provider = λ;};};};
}