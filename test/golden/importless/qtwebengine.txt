∀ γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ. { alsaLib = γ; bison = δ; coreutils = δ; cups = String; darwin = ε; dbus = ζ; enableProprietaryCodecs = Bool; fetchpatch = { extraPrefix = String; name = String; sha256 = String; stripLen = Integer; url = String } -> η; ffmpeg_3 = Null; flex = δ; fontconfig = γ; freetype = γ; git = δ; gn = δ; gperf = δ; harfbuzz = θ; icu = θ; jsoncpp = γ; lib = ι; libXScrnSaver = ζ; libXcursor = ζ; libXrandr = ζ; libXtst = κ; libcap = γ; libdrm = λ; libevent = θ; libjpeg = Null; libopus = θ; libpng = θ; libtiff = θ; libvpx = θ; libwebp = θ; minizip = γ; ninja = δ; nss = γ; openbsm = μ; pciutils = γ; pkgconfig = δ; protobuf = γ; python2 = δ; qtCompatVersion = String -> ν; qtModule = { NIX_CFLAGS_COMPILE = [String]; __impureHostDeps = String; buildInputs = [String]; doCheck = Bool; dontUseGnConfigure = Bool; dontUseNinjaBuild = Bool; dontUseNinjaInstall = Bool; dontUseXcbuild = Bool; enableParallelBuilding = Bool; hardeningDisable = [String]; meta = { description = String; maintainers = ∀ α. [α]; platforms = ∀ α. α }; name = String; nativeBuildInputs = [δ]; outputs = [String]; patches = [{ extraPrefix = String; name = String; sha256 = String; stripLen = Integer; url = String }]; postInstall = String; postPatch = String; preConfigure = String; propagatedBuildInputs = [θ]; qmakeFlags = [String]; qtInputs = [ξ] } -> ο; qtdeclarative = ξ; qtlocation = ξ; qtquickcontrols = ξ; qtwebchannel = ξ; runCommand = String -> π; snappy = γ; srtp = θ; stdenv = ρ; systemd = σ; which = δ; writeScriptBin = String -> τ; xcbuild = [δ]; xorg = υ; zlib = γ } -> { NIX_CFLAGS_COMPILE = [String]; __impureHostDeps = String; buildInputs = [String]; doCheck = Bool; dontUseGnConfigure = Bool; dontUseNinjaBuild = Bool; dontUseNinjaInstall = Bool; dontUseXcbuild = Bool; enableParallelBuilding = Bool; hardeningDisable = [String]; meta = { description = String; maintainers = ∀ β. [β]; platforms = ∀ β. β }; name = String; nativeBuildInputs = [δ]; outputs = [String]; patches = [{ extraPrefix = String; name = String; sha256 = String; stripLen = Integer; url = String }]; postInstall = String; postPatch = String; preConfigure = String; propagatedBuildInputs = [Null]; qmakeFlags = [String]; qtInputs = [ξ] }