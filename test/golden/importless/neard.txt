∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      services = {dbus = {packages = [θ];};};
      systemd = {packages = [ι];};
    } ->
    κ,
  δ.neard = η,
  λ.neard = β,
  μ.services = λ
) =>
α -> {config = κ; options = {services = {neard = {enable = ζ;};};};}