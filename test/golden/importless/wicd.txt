∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {enable = Bool; packages = [η];};};
      systemd =
        { services =
            { wicd =
                { after = [String];
                  before = [String];
                  script = String;
                  wantedBy = [String];
                  wants = [String];
                };
            };
        };
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.types = λ,
  δ.wicd = ζ,
  μ.wicd = β,
  ν.networking = μ,
  λ.bool = ι
) =>
α -> {config = θ; options = {networking = {wicd = {enable = κ;};};};}