∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; services = { dbus = { enable = Bool; packages = ∀ β. [β] } }; systemd = { services = { wicd = { after = [String]; before = [String]; script = String; wantedBy = [String]; wants = [String] } } } }; options = { networking = { wicd = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }