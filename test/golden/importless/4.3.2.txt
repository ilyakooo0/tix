∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( β.all = γ,
  β.enableFeature = Bool -> String -> δ,
  β.isDarwin = Bool,
  β.lgpl3Plus = ε,
  β.mkDerivation =
    { configureFlags = [δ];
      doCheck = Bool;
      meta =
        { badPlatforms = [String];
          branch = String;
          description = String;
          homepage = String;
          license = ε;
          longDescription = String;
          maintainers = ∀ α. α;
          platforms = γ;
        };
      name = String;
      nativeBuildInputs = [ζ];
      outputs = [String];
      passthru = {static = η;};
      preConfigure = String;
      src = θ;
    } ->
    ι,
  β.optionals = κ -> [String] -> [δ],
  ι.out = λ,
  μ.licenses = β,
  ν.lib = μ,
  ξ.platforms = β,
  ο.lib = ξ,
  π.lib = β,
  ρ.lib = β
) =>
{ cxx = Bool;
  fetchurl = {sha256 = String; url = String;} -> θ;
  m4 = ζ;
  stdenv = β;
} ->
ι