∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.tvheadend = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { systemd =
        { services =
            { tvheadend =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      ExecStop = String;
                      Group = String;
                      PIDFile = String;
                      Restart = String;
                      RestartSec = Integer;
                      Type = String;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
      users =
        { users =
            { tvheadend =
                { createHome = Bool;
                  description = String;
                  home = String;
                  uid = θ;
                };
            };
        };
    } ->
    ι,
  γ.mkOption = {default = Integer; description = String; type = κ;} -> λ,
  γ.types = μ,
  ε.enable = η,
  ν.services = β,
  ξ.uids = β,
  ο.ids = ξ,
  μ.int = κ,
  π.int = ρ
) =>
α ->
{ config = ι;
  options =
    {services = {tvheadend = {enable = ζ; htspPort = λ; httpPort = σ;};};};
}