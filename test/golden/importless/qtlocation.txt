∀ α β γ δ ε ζ.
(α.isDarwin = β, α.optional = β -> [String] -> γ, δ.lib = α) => { qtModule =
    {name = String; outputs = [String]; qmakeFlags = γ; qtInputs = [ε];} -> ζ;
  qtbase = ε;
  qtmultimedia = ε;
  stdenv = α;
} ->
ζ