∀ β γ δ ε. { SDL2 = β; autoconf = β; automake = β; fetchFromBitbucket = { owner = String; repo = String; rev = String; sha256 = String } -> γ; freetype = β; libGL = β; libGLU = β; libdevil = β; libmodplug = β; libogg = β; libtheora = β; libtool = β; libvorbis = β; luajit = β; mpg123 = β; openal = β; physfs = β; pkgconfig = δ; stdenv = ε; which = β } -> { NIX_CFLAGS_COMPILE = String; buildInputs = [β]; configureFlags = [String]; meta = { description = String; homepage = String; license = ∀ α. α; maintainers = ∀ α. [α]; platforms = ∀ α. α }; name = String; nativeBuildInputs = [δ]; preConfigure = String; src = { owner = String; repo = String; rev = String; sha256 = String } }