∀ α β γ. { buildDunePackage = { checkInputs = [α]; doCheck = Bool; meta = { description = String }; pname = String; propagatedBuildInputs = [β] } -> γ; cstruct = β; duration = β; mirage-clock = β; mirage-clock-unix = α; mirage-crypto-rng = β; mirage-runtime = β; mirage-time = β; mirage-time-unix = α; mirage-unix = α } -> { checkInputs = [α]; doCheck = Bool; meta = { description = String }; pname = String; propagatedBuildInputs = [β] }