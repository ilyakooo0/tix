∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { boot = { kernelModules = [String] }; environment = { systemPackages = ∀ α. [α] } }; options = { programs = { usbtop = { enable = String } } } }