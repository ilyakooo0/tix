∀ δ ε.
{ buildDunePackage =
    { meta =
        { description = String;
          homepage = ∀ α β. (δ.meta = α, α.homepage = β) => β;
          license = ∀ α β. (δ.meta = α, α.license = β) => β;
          maintainers = ∀ α β. (δ.meta = α, α.maintainers = β) => β;
        };
      pname = String;
      propagatedBuildInputs = [δ];
      src = ∀ γ. (δ.src = γ) => γ;
      version = ∀ γ. (δ.version = γ) => γ;
    } ->
    ε;
  ocplib-endian = δ;
  ocplib-json-typed = δ;
} ->
ε