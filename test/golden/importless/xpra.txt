∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; hardware = { pulseaudio = { enable = ∀ β. β; systemWide = ∀ β. β } }; services = { xserver = { deviceSection = String; displayManager = { job = { execCmd = String } }; monitorSection = String; resolutions = [{ x = String; y = String }]; serverFlagsSection = String; terminateOnReset = Bool; videoDrivers = [String] } }; virtualisation = { virtualbox = { guest = { x11 = Bool } } } }; options = { services = { xserver = { displayManager = { xpra = { auth = { default = String; description = String; example = String; type = ∀ α. α }; bindTcp = { default = String; description = String; example = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; extraOptions = { default = ∀ α. α; description = String; type = ∀ α. α }; pulseaudio = String } } } } } }