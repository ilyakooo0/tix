∀ ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ.
( ζ.services = η,
  ζ.services = θ,
  ι.mkIf = κ,
  ι.mkOption = λ,
  ι.mkOption = μ,
  ι.mkOption = ν,
  ι.mkOption = ξ,
  ι.mkOption = ο,
  ι.mkOption = π,
  ι.types = ρ,
  ι.types = σ,
  ι.types = τ,
  ι.types = υ,
  ι.types = φ,
  ι.types = χ
) =>
{config = ζ; lib = ι; pkgs = ψ;} ->
{ config = ∀ β γ δ ε. (β.exim = γ, γ.enable = δ) => ε;
  options =
    { services =
        { exim =
            { config = ∀ α. α;
              enable = ∀ α. α;
              group = ∀ α. α;
              package = ∀ α. α;
              spoolDir = ∀ α. α;
              user = ∀ α. α;
            };
        };
    };
}