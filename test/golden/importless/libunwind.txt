∀ α β γ δ ε ζ η θ.
( α.mkDerivation =
    { cmakeFlags = β;
      enableParallelBuilding = Bool;
      nativeBuildInputs = [γ];
      patches = [δ];
      pname = String;
      src = ε;
      version = ζ;
    } ->
    η,
  α.optional = Bool -> String -> β,
  θ.lib = α
) =>
{ cmake = γ;
  enableShared = Bool;
  fetch = String -> String -> ε;
  fetchpatch = {sha256 = String; url = String;} -> δ;
  stdenv = α;
  version = ζ;
} ->
η