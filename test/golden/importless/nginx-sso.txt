∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { nginx-sso = { after = [String]; description = String; serviceConfig = { DynamicUser = Bool; ExecStart = String; Restart = String }; wantedBy = [String] } } } }; options = { services = { nginx = { sso = { configuration = { default = {  }; description = String; example = String; type = ∀ α. α }; enable = String; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } } }