∀ α β γ δ ε ζ η θ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enabled = String,
  δ.nabi = ε,
  γ.mkIf =
    Bool ->
    { environment =
        { variables =
            { GTK_IM_MODULE = String;
              QT_IM_MODULE = String;
              XMODIFIERS = String;
            };
        };
      i18n = {inputMethod = {package = ε;};};
      services = {xserver = {displayManager = {sessionCommands = String;};};};
    } ->
    ζ,
  η.inputMethod = β,
  θ.i18n = η
) =>
α -> {config = ζ;}