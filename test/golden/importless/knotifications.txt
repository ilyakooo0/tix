∀ α β γ δ ε ζ.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  kcodecs = ε;
  kconfig = ε;
  kcoreaddons = ε;
  kwindowsystem = ε;
  lib = α;
  libdbusmenu = ε;
  mkDerivation =
    { buildInputs = [ε];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
    } ->
    ζ;
  phonon = ε;
  qttools = δ;
  qtx11extras = ε;
} ->
ζ