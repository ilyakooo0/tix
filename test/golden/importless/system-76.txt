∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = [{ boot = { extraModulePackages = ∀ γ. γ; kernelModules = ∀ γ. γ }; services = { udev = { packages = ∀ β. β } } }]; options = { hardware = { system76 = { enableAll = String; firmware-daemon = { enable = { default = ∀ α. α; description = String; example = Bool; type = ∀ α. α } }; kernel-modules = { enable = { default = ∀ α. α; description = String; example = Bool; type = ∀ α. α } } } } } }