∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.rdnssd = ζ,
  γ.mkIf =
    ε ->
    { assertions = [{assertion = η; message = String;}];
      systemd =
        { services =
            { rdnssd =
                { after = [String];
                  description = String;
                  postStop = String;
                  preStart = String;
                  serviceConfig =
                    {ExecStart = String; PIDFile = String; Type = String;};
                  wantedBy = [String];
                };
            };
        };
      users = {users = {rdnssd = {description = String; uid = ζ;};};};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.types = λ,
  δ.writeScript = String -> String -> μ,
  ν.rdnssd = β,
  ξ.services = ν,
  ο.resolvconf = β,
  π.networking = ο,
  ρ.uids = β,
  σ.ids = ρ,
  λ.bool = ι
) =>
α -> {config = θ; options = {services = {rdnssd = {enable = κ;};};};}