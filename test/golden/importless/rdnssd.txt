∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { assertions = [{ assertion = ∀ β. β; message = String }]; systemd = { services = { rdnssd = { after = [String]; description = String; postStop = String; preStart = String; serviceConfig = { ExecStart = String; PIDFile = String; Type = String }; wantedBy = [String] } } }; users = { users = { rdnssd = { description = String; uid = ∀ α. α } } } }; options = { services = { rdnssd = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }