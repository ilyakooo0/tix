∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β2.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.generationsDir = ζ,
  γ.mkIf =
    ε ->
    { system =
        { boot = {loader = {id = String; kernelFile = η;};};
          build = {installBootLoader = θ;};
        };
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  δ.bash = ν,
  δ.coreutils = ξ,
  δ.gnugrep = ξ,
  δ.gnused = ξ,
  δ.hostPlatform = ο,
  δ.substituteAll =
    {bash = ν; copyKernels = π; isExecutable = Bool; path = [ξ]; src = Path;} ->
    θ,
  ο.platform = ρ,
  σ.stdenv = δ,
  ρ.kernelTarget = η,
  ζ.copyKernels = π,
  τ.loader = β,
  υ.boot = τ,
  φ.generationsDir = β,
  χ.loader = φ,
  ψ.boot = χ,
  μ.bool = κ,
  ω.bool = α1
) =>
α ->
{ config = ι;
  options =
    {boot = {loader = {generationsDir = {copyKernels = λ; enable = β2;};};};};
}