∀ η θ. { makeInstalledTest = { preTestScript = String; testConfig = { environment = { systemPackages = [{ buildInputs = ∀ α. [α]; dontUnpack = Bool; installPhase = String; name = String; nativeBuildInputs = ∀ α. [α] }] }; programs = { dconf = { enable = Bool } }; services = { dbus = { packages = ∀ β. [β] }; gnome3 = { at-spi2-core = { enable = Bool } } } }; tested = ∀ ε. ε; withX11 = Bool } -> η; pkgs = θ } -> { preTestScript = String; testConfig = { environment = { systemPackages = [{ buildInputs = ∀ γ. [γ]; dontUnpack = Bool; installPhase = String; name = String; nativeBuildInputs = ∀ γ. [γ] }] }; programs = { dconf = { enable = Bool } }; services = { dbus = { packages = ∀ δ. [δ] }; gnome3 = { at-spi2-core = { enable = Bool } } } }; tested = ∀ ζ. ζ; withX11 = Bool }