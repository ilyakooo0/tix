∀ δ.
{lib = δ;} ->
{ options =
    { enable =
        ∀ γ.
        ( δ.mkOption =
            { default = Bool;
              description = String;
              example = Bool;
              type = ∀ α β. (δ.types = α, α.bool = β) => β;
            } ->
            γ
        ) =>
        γ;
    };
}