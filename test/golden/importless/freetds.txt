∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.freetds = ε,
  γ.attrNames = ε -> ζ,
  γ.concatStrings = η -> θ,
  γ.length = ζ -> Integer,
  γ.literalExample = String -> ι,
  γ.mapAttrsToList = (κ -> λ -> String) -> ε -> η,
  γ.mkIf =
    Bool ->
    { environment =
        { etc = {freetds.conf = {text = θ;};};
          variables =
            {FREETDS = String; FREETDSCONF = String; SYBASE = String;};
        };
    } ->
    μ,
  γ.mkOption =
    {default = {}; description = String; example = ι; type = ν;} -> ξ,
  γ.types = ο,
  π.environment = β,
  ο.attrsOf = ρ -> ν,
  σ.str = ρ
) =>
α -> {config = μ; options = {environment = {freetds = ξ;};};}