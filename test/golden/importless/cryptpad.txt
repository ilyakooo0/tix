∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { cryptpad = { after = [String]; description = String; serviceConfig = { DynamicUser = Bool; Environment = [String]; ExecStart = String; PrivateTmp = Bool; Restart = String; StateDirectory = String; WorkingDirectory = String }; wantedBy = [String] } } } }; options = { services = { cryptpad = { configFile = { default = String; defaultText = String; description = String; type = ∀ α. α }; enable = String; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }