∀ ε ζ η θ ι κ λ μ.
( ε.programs = ζ,
  ε.programs = η,
  θ.mkIf = ι,
  θ.mkOption = κ,
  θ.types = λ
) =>
{config = ε; lib = θ; pkgs = μ;} ->
{ config = ∀ β γ δ. (β.screen = γ, γ.screenrc = String) => δ;
  options = {programs = {screen = {screenrc = ∀ α. α;};};};
}