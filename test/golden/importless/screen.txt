∀ α β γ δ ε ζ η θ ι κ λ.
( α.screen = β,
  γ.mkIf =
    Bool ->
    { environment = {etc = {screenrc = {text = δ;};}; systemPackages = [ε];};
      security = {pam = {services = {screen = {};};};};
    } ->
    ζ,
  γ.mkOption = {default = String; description = String; type = η;} -> θ,
  γ.types = ι,
  κ.screen = ε,
  β.screenrc = String,
  λ.programs = α,
  ι.lines = η
) =>
{config = α; lib = γ; pkgs = κ;} ->
{config = ζ; options = {programs = {screen = {screenrc = θ;};};};}