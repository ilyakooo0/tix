∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.enableFontDir = ζ,
  γ.mkIf =
    ε ->
    { assertions = η;
      systemd =
        { services =
            { xfs =
                { after = [String];
                  description = String;
                  path = [θ];
                  script = String;
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.singleton = {assertion = ζ; message = String;} -> η,
  γ.types = μ,
  δ.xfs = θ,
  ν.xfs = β,
  ξ.services = ν,
  ο.fonts = β,
  π.xorg = δ,
  μ.bool = κ
) =>
α -> {config = ι; options = {services = {xfs = {enable = λ;};};};}