∀ α β γ δ ε ζ η θ.
( α.config = β,
  α.lib = γ,
  β.videoDrivers = δ,
  γ.elem = String -> δ -> ε,
  γ.mkIf = ε -> {boot = {blacklistedKernelModules = [String];};} -> ζ,
  η.xserver = β,
  θ.services = η
) =>
α -> {config = ζ;}