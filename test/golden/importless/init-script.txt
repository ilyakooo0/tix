∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf = ε -> {system = {build = {installBootLoader = ζ;};};} -> η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.types = κ,
  δ.bash = λ,
  δ.coreutils = μ,
  δ.gnugrep = μ,
  δ.gnused = μ,
  δ.substituteAll =
    {bash = λ; isExecutable = Bool; path = [μ]; src = Path;} -> ζ,
  ν.initScript = β,
  ξ.loader = ν,
  ο.boot = ξ,
  κ.bool = θ
) =>
α ->
{config = η; options = {boot = {loader = {initScript = {enable = ι;};};};};}