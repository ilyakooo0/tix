∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { environment = { systemPackages = [String]; variables = { EDITOR = String } }; systemd = { user = { services = { emacs = { description = String; serviceConfig = { ExecStart = String; ExecStop = String; Restart = String; Type = String }; wantedBy = [String] } } } } }; meta = { doc = Path }; options = { services = { emacs = { defaultEditor = { default = Bool; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; install = { default = Bool; description = String; type = ∀ α. α }; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }