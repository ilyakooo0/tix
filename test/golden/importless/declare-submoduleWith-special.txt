∀ ε ζ η θ ι.
( (ε) // ({foo = String;}) ~ (ζ),
  ε.mkOption = {default = {}; type = η;} -> θ,
  ε.submoduleWith =
    { modules =
        ∀ β γ δ.
        (β.foo = γ, β.mkOption = {default = γ;} -> δ) => [ ( {lib = β;} ->
            {options = {foo = δ;};}
          )
        ];
      specialArgs = {lib = ∀ α. α;};
    } ->
    η,
  ι.types = ε
) =>
{lib = ε;} -> {options = {submodule = θ;};}