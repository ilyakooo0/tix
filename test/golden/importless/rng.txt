∀ γ δ ε ζ.
{ buildDunePackage =
    { checkInputs = [γ];
      doCheck = Bool;
      meta = ∀ α β. ((α) // ({description = String;}) ~ (β), δ.meta = α) => β;
      minimumOCamlVersion = ∀ α. (δ.minimumOCamlVersion = α) => α;
      nativeBuildInputs = [ε];
      pname = String;
      propagatedBuildInputs = [δ];
      src = ∀ α. (δ.src = α) => α;
      useDune2 = ∀ α. (δ.useDune2 = α) => α;
      version = ∀ α. (δ.version = α) => α;
    } ->
    ζ;
  cstruct = δ;
  dune-configurator = ε;
  duration = δ;
  logs = δ;
  mirage-crypto = δ;
  mtime = δ;
  ocaml_lwt = δ;
  ounit = γ;
  randomconv = γ;
} ->
ζ