∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  δ.system-config-printer = ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    ε ->
    { services =
        { dbus = {packages = [ζ];};
          packagekit = {enable = Bool;};
          udev = {packages = [θ];};
        };
      systemd = {packages = [ι];};
    } ->
    κ,
  λ.system-config-printer = β,
  μ.services = λ
) =>
α ->
{config = κ; options = {services = {system-config-printer = {enable = η;};};};}