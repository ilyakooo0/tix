∀ γ δ. { lib = γ; pkgs = δ } -> { config = { services = { xserver = { displayManager = { gdm = { enable = Bool } }; enable = Bool; videoDrivers = ∀ β. β } }; systemd = { services = { terminal-server@ = { description = String; environment = { FD_GEOM = String; FD_XDMCP_IF = String }; path = ∀ α. [α]; serviceConfig = { ExecStart = String; KillMode = String; StandardError = String; StandardInput = String; StandardOutput = String } } }; sockets = { terminal-server = { before = [String]; description = String; socketConfig = { Accept = Bool; ListenStream = Integer }; wantedBy = [String] } } } } }