∀ α β γ δ ε ζ η θ ι κ.
( α.eelco = β,
  α.mkDerivation =
    { buildInputs = [γ];
      buildPhase = String;
      dontUnpack = Bool;
      installPhase = String;
      meta = {description = String; maintainers = [β]; platforms = δ;};
      name = String;
    } ->
    ε,
  α.unix = δ,
  ζ.XMLSimple = γ,
  η.maintainers = α,
  θ.lib = η,
  ι.platforms = α,
  κ.lib = ι
) =>
{makeWrapper = γ; perl = γ; perlPackages = ζ; stdenv = α;} -> ε