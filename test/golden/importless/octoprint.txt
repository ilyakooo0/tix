∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { systemd = { services = { octoprint = { after = [String]; description = String; path = [∀ α. [α] -> [α]]; preStart = String; serviceConfig = { ExecStart = String; Group = ∀ α. α; User = ∀ α. α }; wantedBy = [String] } }; tmpfiles = { rules = [String] } }; users = { groups = { octoprint = { gid = ∀ β. β } }; users = { octoprint = { group = ∀ β. β; uid = ∀ β. β } } } }; options = { services = { octoprint = { enable = String; extraConfig = { default = {  }; description = String; type = ∀ β. β }; group = { default = String; description = String; type = ∀ β. β }; host = { default = String; description = String; type = ∀ β. β }; plugins = { default = ∀ β γ. β -> γ; defaultText = String; description = String; example = String }; port = { default = Integer; description = String; type = ∀ β. β }; stateDir = { default = String; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }