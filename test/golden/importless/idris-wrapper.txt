∀ β γ δ ε ζ η.
{ gmp = β;
  idris-no-deps = γ;
  lib = δ;
  makeWrapper = ε;
  stdenv = ζ;
  symlinkJoin =
    { buildInputs = [ε];
      meta = ∀ α. (γ.meta = α) => α;
      name = ∀ α. (γ.name = α) => α;
      paths = [γ];
      postBuild = String;
      src = ∀ α. (γ.src = α) => α;
    } ->
    η;
} ->
η