∀ α β γ δ ε ζ η θ ι κ λ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.brltty = ε,
  γ.mkIf =
    ζ ->
    { systemd =
        { services =
            { brltty =
                { after = [String];
                  before = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      Nice = Integer;
                      OOMScoreAdjust = Integer;
                      ProtectHome = String;
                      ProtectSystem = String;
                      Restart = String;
                      RestartSec = Integer;
                      SystemCallArchitectures = String;
                      TimeoutStartSec = Integer;
                      TimeoutStopSec = Integer;
                      Type = String;
                    };
                  unitConfig =
                    { DefaultDependencies = String;
                      Documentation = String;
                      RequiresMountsFor = String;
                    };
                  wantedBy = [String];
                  wants = [String];
                };
            };
        };
    } ->
    η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.types = κ,
  ε.enable = ζ,
  λ.services = β,
  κ.bool = θ
) =>
α -> {config = η; options = {services = {brltty = {enable = ι;};};};}