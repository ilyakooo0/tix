∀ β γ δ ε ζ η θ. { erlang = β; gitMinimal = β; lib = γ; perl = β; stdenv = δ; wget = β; which = β; writeText = String -> ε } -> { beamDeps = ζ; buildInputs = [β]; buildPhase = Null; compilePorts = Bool; configurePhase = Null; enableDebugInfo = Bool; installPhase = Null; meta = {  }; name = String; postPatch = String; setupHook = Null; src = η; version = θ } -> ∀ α. α -> { app_name = String; beamDeps = ζ; buildInputs = [β]; buildPhase = String; compilePorts = Bool; configurePhase = String; dontStrip = Bool; enableDebugInfo = Bool; installPhase = String; meta = {  }; name = String; passthru = { beamDeps = ζ; env = α; packageName = String }; postPatch = String; propagatedBuildInputs = ζ; setupHook = String; src = η; version = θ }