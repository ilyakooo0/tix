∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { environment = { systemPackages = ∀ α. [α] }; systemd = { packages = ∀ α. [α]; services = { irqbalance = { wantedBy = [String] } } } }; options = { services = { irqbalance = { enable = String } } } }