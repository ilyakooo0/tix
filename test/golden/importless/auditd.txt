∀ α β γ δ ε ζ η θ ι κ λ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { boot = {kernelParams = [String];};
      environment = {systemPackages = [η];};
      systemd =
        { services =
            { auditd =
                { description = String;
                  path = [θ];
                  serviceConfig = {ExecStart = String; ExecStartPre = String;};
                  unitConfig =
                    { ConditionSecurity = [String];
                      ConditionVirtualization = String;
                      DefaultDependencies = Bool;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  δ.audit = η,
  κ.auditd = β,
  λ.security = κ
) =>
α -> {config = ι; options = {security = {auditd = {enable = ζ;};};};}