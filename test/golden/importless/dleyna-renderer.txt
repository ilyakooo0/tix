∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];};};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.types = λ,
  δ.dleyna-renderer = ζ,
  μ.dleyna-renderer = β,
  ν.services = μ,
  λ.bool = ι
) =>
α -> {config = θ; options = {services = {dleyna-renderer = {enable = κ;};};};}