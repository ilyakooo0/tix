∀ α β γ δ ε ζ η θ ι κ λ.
( α.stateVersion = β,
  γ.mkOption = {default = δ; description = String; internal = Bool;} -> ε,
  γ.versionAtLeast = β -> String -> ζ,
  η.isAarch64 = δ,
  θ.system = α,
  ι.hostPlatform = η,
  κ.stdenv = ι
) =>
{config = α; lib = γ; pkgs = η;} -> {options = {ec2 = {efi = ε; hvm = λ;};};}