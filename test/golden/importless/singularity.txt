∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  ε.installPhase = ζ,
  β.singularity = η,
  δ.overrideAttrs = (ε -> {installPhase = String;}) -> θ,
  γ.mkEnableOption = String -> ι,
  γ.mkIf =
    κ ->
    { environment = {systemPackages = [θ];};
      security = {wrappers = {singularity-suid = {source = String;};};};
      systemd = {tmpfiles = {rules = [String];};};
    } ->
    λ,
  μ.singularity = δ,
  η.enable = κ,
  ν.programs = β
) =>
α -> {config = λ; options = {programs = {singularity = {enable = ι;};};};}