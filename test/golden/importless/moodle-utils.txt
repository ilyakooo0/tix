∀ ε ζ η θ ι κ λ μ ν ξ ο.
( ( ε
  ) //
  ( { buildInputs = [ζ];
      buildPhase = η;
      configurePhase = θ;
      installPhase = String;
      name = ι;
      pluginType = κ;
    }
  ) ~
  ( λ
  ),
  μ.stdenv = ν,
  μ.unzip = ζ,
  ξ.buildInputs = [ζ],
  ν.mkDerivation = λ -> ο
) =>
μ ->
{ buildMoodlePlugin =
    ∀ α β γ δ.
    ( α.buildPhase = String,
      α.configurePhase = String,
      α.name = β,
      α.pluginType = γ,
      α.src = δ
    ) =>
    α -> ο;
}