∀ ε ζ η θ.
(ε.mkDerivation = ζ -> η) => {stdenv = ε; unzip = θ;} ->
{ buildMoodlePlugin =
    ∀ α β γ δ.
    { buildInputs = [θ];
      buildPhase = String;
      configurePhase = String;
      name = α;
      pluginType = β;
      src = γ;
    } ->
    δ;
}