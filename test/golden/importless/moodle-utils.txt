∀ δ ε ζ η θ ι.
( δ.mkDerivation =
    { buildInputs = [ε];
      buildPhase = String;
      configurePhase = String;
      installPhase = String;
      name = ζ;
      pluginType = η;
      src = θ;
    } ->
    ι
) =>
{stdenv = δ; unzip = ε;} ->
{ buildMoodlePlugin =
    ∀ α β γ.
    { buildInputs = [ε];
      buildPhase = String;
      configurePhase = String;
      name = α;
      pluginType = β;
      src = γ;
    } ->
    ι;
}