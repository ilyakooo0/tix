∀ π ρ σ τ υ φ χ.
(π.programs = ρ, π.programs = σ, π.programs = τ, π.programs = υ) => { config =
    π;
  lib = φ;
  pkgs = χ;
} ->
{ config =
    ∀ λ μ ν ξ ο.
    ( φ.mkIf =
        Bool ->
        { environment =
            { etc =
                { nanorc =
                    { text =
                        ∀ α β γ δ ε ζ η.
                        ( φ.concatStrings = [α] -> β,
                          γ.nano = δ,
                          δ.nanorc = α,
                          φ.optionalString = ε -> String -> α,
                          ζ.nano = η,
                          η.syntaxHighlight = ε
                        ) =>
                        β;
                    };
                };
            };
        } ->
        λ,
      μ.nano = ν,
      ν.nanorc = String,
      ξ.nano = ο,
      ο.syntaxHighlight = Bool
    ) =>
    λ;
  options =
    { programs =
        { nano =
            { nanorc =
                ∀ κ.
                ( φ.mkOption =
                    { default = String;
                      description = String;
                      example = String;
                      type = ∀ θ ι. (φ.types = θ, θ.lines = ι) => ι;
                    } ->
                    κ
                ) =>
                κ;
              syntaxHighlight =
                ∀ κ.
                ( φ.mkOption =
                    { default = Bool;
                      description = String;
                      type = ∀ θ ι. (φ.types = θ, θ.bool = ι) => ι;
                    } ->
                    κ
                ) =>
                κ;
            };
        };
    };
}