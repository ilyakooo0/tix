∀ α β γ δ ε ζ η θ.
( α.fix' = β -> γ,
  α.extends =
    (δ -> ε -> {}) ->
    ( ζ ->
      { Prelude = η;
        buildDhallDirectoryPackage = θ;
        buildDhallGitHubPackage = θ;
        buildDhallPackage = θ;
        dhall-kubernetes = η;
        dhall-packages = η;
      }
    ) ->
    β
) =>
{lib = α; newScope = ζ -> Path -> {} -> θ; overrides = δ -> ε -> {};} -> γ