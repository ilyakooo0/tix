∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ.
( α.filter = (Null -> Bool) -> [Null] -> β,
  α.gpl2 = γ,
  α.makeBinPath = β -> δ,
  α.mkDerivation =
    { buildInputs = ε;
      doCheck = Bool;
      enableParallelBuilding = Bool;
      meta =
        { description = String;
          homepage = String;
          license = γ;
          longDescription = String;
          maintainers = [ζ];
          platforms = η;
        };
      name = String;
      patches = [Path];
      postInstall = String;
      src = θ;
      version = String;
    } ->
    ι,
  α.optionalString = Bool -> String -> String,
  α.optionals = Bool -> [Path] -> [Path],
  α.peti = ζ,
  α.unix = η,
  κ.lib = α,
  λ.lib = α,
  μ.lib = α,
  ν.licenses = α,
  ξ.lib = ν,
  ο.maintainers = α,
  π.lib = ο,
  ρ.platforms = α,
  σ.lib = ρ,
  τ.lib = α,
  υ.lib = α
) =>
{ ecl = Null;
  ecl-fasl = Bool;
  fetchpatch = {sha256 = String; url = String;} -> Path;
  fetchurl = {sha256 = String; url = String;} -> θ;
  gnuplot = Null;
  makeWrapper = Null;
  perl = Null;
  python = Null;
  rlwrap = Null;
  sbcl = Null;
  stdenv = α;
  texinfo = Null;
  tk = Null;
} ->
ι