∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { pathsToLink = [String]; systemPackages = ∀ δ. [δ] }; services = { gvfs = { enable = Bool; package = ∀ γ. γ }; upower = { enable = ∀ γ. γ }; xserver = { desktopManager = { session = { bgSupport = Bool; name = String; start = String } } } } }; options = { environment = { lxqt = { excludePackages = { default = ∀ β. β; description = String; example = String; type = ∀ β. β } } }; services = { xserver = { desktopManager = { lxqt = { enable = { default = Bool; description = String; type = ∀ α. α } } } } } } }