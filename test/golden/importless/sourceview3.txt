∀ β γ δ.
{ buildDunePackage =
    { buildInputs = (β.buildInputs = [γ]) => [γ];
      meta = ∀ α. (β.meta = α) => α;
      nativeBuildInputs = ∀ α. (β.nativeBuildInputs = α) => α;
      pname = String;
      propagatedBuildInputs = [β];
      src = ∀ α. (β.src = α) => α;
      version = ∀ α. (β.version = α) => α;
    } ->
    δ;
  gtksourceview = γ;
  lablgtk3 = β;
} ->
δ