∀ β γ δ ε. { config = β; lib = γ; options = δ; pkgs = ε } -> { extraOpts = { configFile = { default = Null; description = String; example = String; type = ∀ α. α }; configuration = { default = Null; description = String; example = String; type = ∀ α. α } }; port = Integer; serviceOpts = { serviceConfig = { ExecStart = String } } }