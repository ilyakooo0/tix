∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { environment = { etc = { f-prot.conf = { source = String } }; systemPackages = ∀ α. [α] }; services = { cron = { systemCronJobs = [String] }; fprot = { updater = { licenseKeyfile = String; productData = String } } }; systemd = { services = { fprot-updater = { preStart = String; script = String; serviceConfig = { RemainAfterExit = Bool; Type = String }; wantedBy = [String] } } } }; options = { services = { fprot = { updater = { enable = String; frequency = { default = Integer; description = String }; licenseKeyfile = { description = String }; productData = { description = String } } } } } }