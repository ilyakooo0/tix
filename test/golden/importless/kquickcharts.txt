∀ α β γ δ ε ζ.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  lib = α;
  mkDerivation =
    { meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
      outputs = [String];
      propagatedBuildInputs = [ε];
    } ->
    ζ;
  qtquickcontrols2 = ε;
} ->
ζ