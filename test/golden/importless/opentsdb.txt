∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { opentsdb = { description = String; environment = { JAVA_HOME = String }; path = ∀ β. [β]; preStart = String; requires = [String]; serviceConfig = { ExecStart = String; Group = ∀ α. α; PermissionsStartOnly = Bool; User = ∀ α. α }; wantedBy = [String] } } }; users = { groups = { opentsdb = { gid = ∀ β. β } }; users = { opentsdb = { description = String; group = String; uid = ∀ β. β } } } }; options = { services = { opentsdb = { config = { default = String; description = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; group = { default = String; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; example = String; type = ∀ β. β }; port = { default = Integer; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }