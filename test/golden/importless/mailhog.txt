∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { mailhog = { after = [String]; description = String; serviceConfig = { ExecStart = String; Type = String; User = ∀ α. α }; wantedBy = [String] } } }; users = { users = { mailhog = { description = String; isSystemUser = Bool; name = ∀ β. β } } } }; options = { services = { mailhog = { enable = String; user = { default = String; description = String; type = ∀ β. β } } } } }