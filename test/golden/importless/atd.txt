∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.atd = ε,
  γ.builtins = ζ,
  γ.map =
    ( η ->
      { name = String;
        value =
          { group = String;
            owner = String;
            setgid = Bool;
            setuid = Bool;
            source = String;
          };
      }
    ) ->
    [String] -> θ,
  γ.mkIf =
    ι ->
    { environment = {systemPackages = [κ];};
      security = {pam = {services = {atd = {};};}; wrappers = λ;};
      systemd =
        { services =
            { atd =
                { description = String;
                  path = [κ];
                  preStart = String;
                  script = String;
                  serviceConfig = {Type = String;};
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {atd = {gid = μ;};};
          users = {atd = {description = String; home = String; uid = ν;};};
        };
    } ->
    ξ,
  γ.mkOption = {default = Bool; description = String; type = ο;} -> π,
  γ.types = ρ,
  δ.at = κ,
  ε.enable = ι,
  σ.services = β,
  ζ.listToAttrs = θ -> λ,
  τ.gids = β,
  υ.ids = τ,
  φ.uids = β,
  χ.ids = φ,
  ρ.bool = ο,
  ψ.bool = ω
) =>
α ->
{ config = ξ;
  options = {services = {atd = {allowEveryone = π; enable = α1;};};};
}