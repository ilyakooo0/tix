∀ γ δ ε.
{ buildDunePackage =
    { doCheck = Bool;
      meta = ∀ α β. ((α) // ({description = String;}) ~ (β), γ.meta = α) => β;
      pname = String;
      propagatedBuildInputs = [γ];
      src = ∀ α. (γ.src = α) => α;
      useDune2 = ∀ α. (γ.useDune2 = α) => α;
      version = ∀ α. (γ.version = α) => α;
    } ->
    δ;
  lib = ε;
  posix-base = γ;
} ->
δ