∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; services = { gitolite = { extraGitoliteRc = String } }; systemd = { services = { gitolite-init = { description = String; environment = { GITOLITE_RC = String; GITOLITE_RC_DEFAULT = String }; path = ∀ β. [β]; script = String; serviceConfig = [{ StateDirectory = String; StateDirectoryMode = String }]; unitConfig = { RequiresMountsFor = ∀ α. α }; wantedBy = [String] } } } }; options = { services = { gitolite = { adminPubkey = { description = String; type = ∀ β. β }; commonHooks = { default = ∀ β. β; description = String; type = ∀ β. β }; dataDir = { default = String; description = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; enableGitAnnex = { default = Bool; description = String; type = ∀ β. β }; extraGitoliteRc = { default = String; description = String; example = String; type = ∀ β. β }; group = { default = String; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }