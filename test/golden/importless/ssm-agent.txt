∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { ssm-agent = { after = [String]; path = [String]; serviceConfig = { ExecStart = String; KillMode = String; Restart = String; RestartSec = String }; users = { extraUsers = { ssm-user = {  } } }; wantedBy = [String] } } } }; options = { services = { ssm-agent = { enable = String; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }