∀ α β.
(α.e2fsprogs = β) => {pkgs = α;} ->
{ config =
    { boot =
        { initrd =
            {availableKernelModules = [String]; extraUtilsCommands = String;};
        };
      system = {fsPackages = [β];};
    };
}