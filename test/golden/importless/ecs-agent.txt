∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { ecs-agent = { after = [String]; environment = ∀ α. α; script = String; wantedBy = [String] } } }; virtualisation = { docker = { enable = Bool } } }; options = { services = { ecs-agent = { enable = String; extra-environment = { default = {  }; description = String; type = ∀ α. α }; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }