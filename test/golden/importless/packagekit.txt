∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { etc = { PackageKit/PackageKit.conf = { text = String }; PackageKit/Vendor.conf = { text = String } } }; services = { dbus = { packages = ∀ α. [α] } }; systemd = { packages = ∀ β. [β] } }; options = { services = { packagekit = { backend = { default = String; description = String; type = [String] }; enable = String } } } }