∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { hound = { after = [String]; description = String; path = ∀ β. [β]; serviceConfig = { ExecStart = String; ExecStartPre = String; Group = ∀ α. α; User = ∀ α. α; WorkingDirectory = ∀ α. α }; wantedBy = [String] } } }; users = { groups = { hound = { gid = ∀ β. β } }; users = { hound = { createHome = Bool; description = String; extraGroups = ∀ β. β; group = ∀ β. β; home = ∀ β. β; uid = ∀ β. β } } } }; options = { services = { hound = { config = { description = String; example = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; extraGroups = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; group = { default = String; description = String; type = ∀ β. β }; home = { default = String; description = String; type = ∀ β. β }; listen = { default = String; description = String; example = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }