∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.environment = ε,
  β.services = ζ,
  δ.writeText = String -> η -> θ,
  γ.builtins = ι,
  γ.cfg = κ,
  γ.mkEnableOption = String -> λ,
  γ.mkIf =
    μ ->
    { environment =
        {etc = {salt/minion = {source = θ;};}; systemPackages = [ν];};
      systemd =
        { services =
            { salt-minion =
                { after = [String];
                  description = String;
                  path = [ξ];
                  restartTriggers = [ο];
                  serviceConfig =
                    { ExecStart = String;
                      LimitNOFILE = Number;
                      NotifyAccess = String;
                      Type = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    π,
  γ.mkOption = {default = {}; description = String; type = ρ;} -> σ,
  γ.recursiveUpdate = {default_include = String; pki_dir = String;} -> τ -> υ,
  γ.salt = ν,
  γ.types = φ,
  γ.utillinux = ξ,
  κ.configuration = τ,
  ζ.salt = χ,
  χ.minion = ψ,
  ψ.enable = μ,
  ι.toJSON = υ -> η,
  ε.etc = ω,
  ω.salt/minion = α1,
  α1.source = ο,
  φ.attrs = ρ
) =>
α ->
{ config = π;
  options =
    {services = {salt = {minion = {configuration = σ; enable = λ;};};};};
}