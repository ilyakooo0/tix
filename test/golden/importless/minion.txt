∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.minion = ε,
  β.source = ζ,
  δ.writeText = String -> η -> θ,
  γ.builtins = ι,
  γ.cfg = κ,
  γ.mkEnableOption = String -> λ,
  γ.mkIf =
    μ ->
    { environment =
        {etc = {salt/minion = {source = θ;};}; systemPackages = [ν];};
      systemd =
        { services =
            { salt-minion =
                { after = [String];
                  description = String;
                  path = [ξ];
                  restartTriggers = [ζ];
                  serviceConfig =
                    { ExecStart = String;
                      LimitNOFILE = Integer;
                      NotifyAccess = String;
                      Type = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    ο,
  γ.mkOption = {default = {}; description = String; type = π;} -> ρ,
  γ.recursiveUpdate = {default_include = String; pki_dir = String;} -> σ -> τ,
  γ.salt = ν,
  γ.types = υ,
  γ.utillinux = ξ,
  κ.configuration = σ,
  ε.enable = μ,
  φ.salt = β,
  χ.services = φ,
  ι.toJSON = τ -> η,
  ψ.salt/minion = β,
  ω.etc = ψ,
  α1.environment = ω,
  υ.attrs = π
) =>
α ->
{ config = ο;
  options =
    {services = {salt = {minion = {configuration = ρ; enable = λ;};};};};
}