∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  β.enable = δ,
  γ.mkIf = δ -> {environment = {pathsToLink = [String];};} -> ε,
  γ.mkOption = {default = Bool; description = String; type = ζ;} -> η,
  γ.teams = θ,
  γ.types = ι,
  κ.autostart = β,
  λ.xdg = κ,
  θ.members = μ,
  ν.freedesktop = θ,
  ι.bool = ζ
) =>
α ->
{ config = ε;
  meta = {maintainers = μ;};
  options = {xdg = {autostart = {enable = η;};};};
}