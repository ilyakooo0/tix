∀ γ δ ε ζ. { config = γ; lib = δ; options = ε; pkgs = ζ } -> { extraOpts = { extraLabels = { default = { host = ∀ α. α }; defaultText = String; description = String; example = String; type = ∀ β. β }; listenAddress = {  }; url = { description = String; type = ∀ β. β } }; port = Integer; serviceOpts = { serviceConfig = { ExecStart = String } } }