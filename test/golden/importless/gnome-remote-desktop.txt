∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    {services = {pipewire = {enable = Bool;};}; systemd = {packages = [η];};} ->
    θ,
  γ.teams = ι,
  δ.gnome-remote-desktop = η,
  κ.gnome-remote-desktop = β,
  λ.gnome3 = κ,
  μ.services = λ,
  ν.gnome3 = δ,
  ι.members = ξ,
  ο.gnome = ι
) =>
α ->
{ config = θ;
  meta = {maintainers = ξ;};
  options = {services = {gnome3 = {gnome-remote-desktop = {enable = ζ;};};};};
}