∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = [∀ γ. γ] }; networking = { firewall = { allowedTCPPorts = ∀ γ. [γ] } }; systemd = { services = { beanstalkd = { after = [String]; description = String; serviceConfig = { DynamicUser = Bool; ExecStart = String; Restart = String; StateDirectory = String }; wantedBy = [String] } } } }; options = { services = { beanstalkd = { enable = String; listen = { address = { default = String; description = String; example = String; type = ∀ α. α }; port = { default = Integer; description = String; type = ∀ α. α } }; openFirewall = { default = Bool; description = String; type = ∀ β. β } } } } }