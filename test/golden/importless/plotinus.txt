∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.plotinus = ε,
  γ.mkIf =
    ζ ->
    { environment =
        {variables = {GTK3_MODULES = [String]; XDG_DATA_DIRS = [String];};};
    } ->
    η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.types = κ,
  δ.maintainers = λ,
  ε.enable = ζ,
  μ.programs = β,
  ν.meta = δ,
  ξ.plotinus = ν,
  κ.bool = θ
) =>
α ->
{ config = η;
  meta = {doc = Path; maintainers = λ;};
  options = {programs = {plotinus = {enable = ι;};};};
}