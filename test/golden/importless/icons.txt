∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.enable = β,
  γ.mkIf =
    β ->
    { environment =
        { pathsToLink = [String];
          profileRelativeSessionVariables = {XCURSOR_PATH = [String];};
          sessionVariables = {XCURSOR_PATH = [String];};
        };
    } ->
    δ,
  γ.mkOption = {default = Bool; description = String; type = ε;} -> ζ,
  γ.teams = η,
  γ.types = θ,
  ι.icons = α,
  κ.xdg = ι,
  η.members = λ,
  μ.freedesktop = η,
  θ.bool = ε
) =>
{config = α; lib = γ;} ->
{ config = δ;
  meta = {maintainers = λ;};
  options = {xdg = {icons = {enable = ζ;};};};
}