∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { environment = { systemPackages = [{ dir = String; isExecutable = Bool; name = String; perlFlags = ∀ α. [α]; src = Path }] }; programs = { bash = { interactiveShellInit = String }; zsh = { interactiveShellInit = String } } }; options = { programs = { command-not-found = { dbPath = { default = String; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α } } } } }