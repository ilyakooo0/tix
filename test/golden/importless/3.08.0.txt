∀ α β γ δ ε ζ.
( α.linux = β,
  α.mkDerivation =
    { buildFlags = [String];
      builder = Path;
      checkTarget = [String];
      configureFlags = [String];
      configureScript = Path;
      dontAddPrefix = String;
      meta = {platforms = β;};
      pname = String;
      src = γ;
      version = String;
    } ->
    δ,
  ε.platforms = α,
  ζ.lib = ε
) =>
{ fetchurl = {sha256 = String; url = String;} -> γ;
  stdenv = α;
  xlibsWrapper = String;
} ->
δ