∀ ε.
{lib = ε;} ->
{ options =
    { value =
        ∀ δ.
        ( ε.mkOption =
            { type =
                ∀ α β γ.
                ( ε.types = α,
                  α.ints = β,
                  β.between = Integer -> Integer -> γ
                ) =>
                γ;
            } ->
            δ
        ) =>
        δ;
    };
}