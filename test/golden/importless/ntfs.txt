∀ α β γ δ ε ζ η θ ι.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.supportedFilesystems = ε,
  γ.any = (String -> Bool) -> ε -> ζ,
  γ.mkIf = ζ -> {system = {fsPackages = [η];};} -> θ,
  δ.ntfs3g = η,
  ι.boot = β
) =>
α -> {config = θ;}