∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = [{ environment = { systemPackages = ∀ γ. [γ] }; services = { dbus = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ] } }]; meta = { maintainers = ∀ δ. δ }; options = { services = { gnome3 = { at-spi2-core = { enable = { default = Bool; description = String; type = ∀ α. α } } } } } }