∀ α β γ δ ε ζ η θ ι κ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enableGhostscriptFonts = ε,
  γ.mkIf = ε -> {fonts = {fonts = [String];};} -> ζ,
  γ.mkOption = {default = Bool; description = String; type = η;} -> θ,
  γ.types = ι,
  κ.fonts = β,
  ι.bool = η
) =>
α -> {config = ζ; options = {fonts = {enableGhostscriptFonts = θ;};};}