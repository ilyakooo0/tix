∀ β γ δ ε. { config = β; lib = γ; options = δ; pkgs = ε } -> { extraOpts = { minioAccessKey = { description = String; example = String; type = ∀ α. α }; minioAccessSecret = { description = String; type = ∀ α. α }; minioAddress = { description = String; example = String; type = ∀ α. α }; minioBucketStats = { default = Bool; description = String; type = ∀ α. α } }; port = Integer; serviceOpts = { serviceConfig = { ExecStart = String } } }