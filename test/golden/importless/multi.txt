∀ α β γ δ ε ζ η θ.
( α.override =
    { bintools = (α.bintools = β, β.override = {libc = γ;} -> δ) => δ;
      extraBuildCommands = String;
      libc = γ;
    } ->
    ε
) =>
{ clang = α;
  gcc32 = ζ;
  gcc64 = η;
  glibc_multi = θ;
  runCommand = String -> {} -> String -> γ;
} ->
ε