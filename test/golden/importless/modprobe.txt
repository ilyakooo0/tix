∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { modprobe.d/debian.conf = { source = ∀ α. α }; modprobe.d/nixos.conf = { text = String }; modprobe.d/ubuntu.conf = { source = String } }; systemPackages = ∀ γ. [γ] }; system = { activationScripts = { modprobe = String } } }; options = { boot = { blacklistedKernelModules = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; extraModprobeConfig = { default = String; description = String; example = String; type = ∀ β. β } } } }