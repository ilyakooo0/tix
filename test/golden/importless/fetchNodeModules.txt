∀ α β γ δ ε ζ η.
( α.mkDerivation =
    { buildCommand = String;
      name = String;
      nativeBuildInputs = [β];
      outputHash = γ;
      outputHashAlgo = String;
      outputHashMode = String;
    } ->
    δ,
  α.versionAtLeast = ε -> String -> Bool,
  β.version = ε,
  ζ.lib = α
) =>
{jq = β; stdenv = α;} -> {nodejs = β; sha256 = γ; src = η;} -> δ