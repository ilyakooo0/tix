∀ α β γ δ ε ζ η θ ι κ λ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enableIntel2200BGFirmware = ε,
  δ.intel2200BGFirmware = ζ,
  γ.bool = η,
  γ.mkIf = ε -> {hardware = {firmware = [ζ];};} -> θ,
  γ.mkOption = {default = Bool; description = String; type = η;} -> ι,
  κ.networking = β,
  λ.types = γ
) =>
α -> {config = θ; options = {networking = {enableIntel2200BGFirmware = ι;};};}