∀ α β γ δ ε ζ η θ ι κ.
( α.enableIntel2200BGFirmware = β,
  γ.intel2200BGFirmware = δ,
  ε.bool = ζ,
  ε.mkIf = β -> {hardware = {firmware = [δ];};} -> η,
  ε.mkOption = {default = Bool; description = String; type = ζ;} -> θ,
  ι.networking = α,
  κ.types = ε
) =>
{config = α; lib = ε; pkgs = γ;} ->
{config = η; options = {networking = {enableIntel2200BGFirmware = θ;};};}