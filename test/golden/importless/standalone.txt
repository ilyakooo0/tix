∀ β γ δ ε ζ. { alsaLib = β; atk = β; bzip2 = β; cairo = β; curl = β; debug = Bool; expat = β; fetchurl = { sha256 = String; url = String } -> γ; fontconfig = β; freetype = β; gdk-pixbuf = β; glib = β; glibc = β; graphite2 = β; gtk2 = β; harfbuzz = β; lib = δ; libICE = β; libSM = β; libX11 = β; libXau = β; libXcomposite = β; libXcursor = β; libXdamage = β; libXdmcp = β; libXext = β; libXfixes = β; libXi = β; libXinerama = β; libXrandr = β; libXrender = β; libXt = β; libXxf86vm = β; libdrm = β; libffi = β; libglvnd = β; libpng = β; libvdpau = β; libxcb = β; libxshmfence = β; nspr = β; nss = β; pango = β; pcre = β; pixman = β; stdenv = ε; unzip = ζ; zlib = β } -> { dontPatchELF = Bool; dontStrip = Bool; installPhase = String; meta = { broken = Bool; description = String; homepage = String; license = ∀ α. α; maintainers = ∀ α. [α]; platforms = [String] }; nativeBuildInputs = [ζ]; pname = String; preferLocalBuild = Bool; rpath = [β]; sourceRoot = String; src = { sha256 = String; url = String }; version = String }