∀ α β γ δ ε ζ η θ ι.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.supportedFilesystems = ε,
  γ.any = (String -> Bool) -> ε -> ζ,
  γ.mkIf =
    ζ ->
    { security =
        { wrappers =
            { mount.ecryptfs_private = {source = String;};
              umount.ecryptfs_private = {source = String;};
            };
        };
      system = {fsPackages = [η];};
    } ->
    θ,
  δ.ecryptfs = η,
  ι.boot = β
) =>
α -> {config = θ;}