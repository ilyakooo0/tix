∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = [{ assertions = [{ assertion = Bool; message = String }]; boot = { loader = { grub = { extraEntries = String; extraFiles = { memtest86.efi = String } } } } }]; options = { boot = { loader = { grub = { memtest86 = { enable = { default = Bool; description = String; type = ∀ α. α }; params = { default = ∀ α. α; description = String; example = [String]; type = ∀ α. α } } } } } } }