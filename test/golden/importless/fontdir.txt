∀ α β γ δ ε ζ η θ ι κ.
( α.enableFontDir = β,
  γ.mkIf =
    β -> {environment = {pathsToLink = [String]; systemPackages = [δ];};} -> ε,
  γ.mkOption = {default = Bool; description = String; type = ζ;} -> η,
  γ.types = θ,
  ι.runCommand = String -> {preferLocalBuild = Bool;} -> String -> δ,
  κ.fonts = α,
  θ.bool = ζ
) =>
{config = α; lib = γ; pkgs = ι;} ->
{config = ε; options = {fonts = {enableFontDir = η;};};}