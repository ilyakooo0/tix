∀ α β γ δ ε ζ η θ ι κ λ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enableFontDir = ε,
  γ.mkIf =
    ε -> {environment = {pathsToLink = [String]; systemPackages = [ζ];};} -> η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.types = κ,
  δ.runCommand = String -> {preferLocalBuild = Bool;} -> String -> ζ,
  λ.fonts = β,
  κ.bool = θ
) =>
α -> {config = η; options = {fonts = {enableFontDir = ι;};};}