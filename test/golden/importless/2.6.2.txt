∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.free = β,
  α.isDarwin = γ,
  α.mkDerivation =
    { NIX_CFLAGS_COMPILE = δ;
      buildPhase = String;
      checkPhase = String;
      doCheck = Bool;
      hardeningDisable = [String];
      installPhase = String;
      meta =
        {description = String; homepage = String; license = β; platforms = ε;};
      nativeBuildInputs = [ζ];
      patches = [Path];
      pname = String;
      preConfigure = String;
      src = η;
      version = String;
    } ->
    θ,
  α.optionalString = γ -> String -> δ,
  α.sharedLibrary = ι,
  α.unix = ε,
  κ.extensions = α,
  λ.hostPlatform = κ,
  μ.lib = α,
  ν.lib = α,
  ξ.licenses = α,
  ο.lib = ξ,
  π.platforms = α,
  ρ.lib = π
) =>
{fetchurl = {sha256 = String; url = String;} -> η; stdenv = α; unzip = ζ;} -> θ