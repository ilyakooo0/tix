∀ γ δ. { config = γ; lib = δ } -> { config = { environment = { pathsToLink = [String] } }; meta = { maintainers = ∀ β. β }; options = { xdg = { menus = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }