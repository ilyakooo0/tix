∀ ε.
{lib = ε;} ->
{ options =
    { value =
        ∀ δ.
        ( ε.mkOption =
            {type = ∀ α β γ. (ε.types = α, α.ints = β, β.unsigned = γ) => γ;} ->
            δ
        ) =>
        δ;
    };
}