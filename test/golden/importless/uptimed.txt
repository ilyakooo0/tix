∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.uptimed = ε,
  γ.mkIf =
    ζ ->
    { systemd =
        { services =
            { uptimed =
                { description = String;
                  preStart = String;
                  serviceConfig =
                    { ExecStart = String;
                      IOSchedulingClass = String;
                      InaccessibleDirectories = String;
                      Nice = Integer;
                      NoNewPrivileges = String;
                      PrivateNetwork = String;
                      PrivateTmp = String;
                      ReadWriteDirectories = String;
                      Restart = String;
                      User = String;
                    };
                  unitConfig = {Documentation = String;};
                  wantedBy = [String];
                };
            };
        };
      users =
        { users =
            { uptimed =
                { createHome = Bool;
                  description = String;
                  home = String;
                  uid = η;
                };
            };
        };
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.types = λ,
  ε.enable = ζ,
  μ.services = β,
  ν.uids = β,
  ξ.ids = ν,
  λ.bool = ι
) =>
α -> {config = θ; options = {services = {uptimed = {enable = κ;};};};}