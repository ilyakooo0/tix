∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.criu = β,
  α.kernelConfig = γ,
  δ.isYes = String -> ε,
  δ.mkIf =
    ζ ->
    { boot = {kernel = {features = {criu = Bool;};};};
      environment = {systemPackages = [η];};
      system = {requiredKernelConfig = [ε];};
    } ->
    θ,
  δ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  δ.types = λ,
  μ.criu = η,
  β.enable = ζ,
  ν.programs = α,
  ξ.lib = α,
  λ.bool = ι
) =>
{config = α; lib = δ; pkgs = μ;} ->
{config = θ; options = {programs = {criu = {enable = κ;};};};}