∀ β γ δ ε.
(β.version = γ, β.version = γ) => { buildEnv =
    { buildInputs = [δ];
      meta = ∀ α. (β.meta = α) => α;
      name = String;
      paths = [β];
      pathsToLink = [String];
      postBuild = String;
    } ->
    ε;
  makeWrapper = δ;
  zeroad-data = β;
  zeroad-unwrapped = β;
} ->
ε