∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; networking = { firewall = { allowedUDPPortRanges = [{ from = Integer; to = Integer }] } }; security = { wrappers = { utempter = { group = String; owner = String; setgid = Bool; setuid = Bool; source = String } } } }; options = { programs = { mosh = { enable = { default = Bool; description = String; type = ∀ α. α }; withUtempter = { default = Bool; description = String; type = ∀ α. α } } } } }