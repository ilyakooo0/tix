∀ β γ δ ε ζ.
( β.mkDerivation =
    { allowSubstitutes = Bool;
      buildCommand = String;
      meta = {platforms = [String];};
      msp430GccSupport = γ;
      name = String;
      newlib = δ;
      passthru =
        {incdir = ∀ α. (δ.incdir = α) => α; libdir = ∀ α. (δ.libdir = α) => α;};
      preferLocalBuild = Bool;
    } ->
    ε
) =>
{lndir = ζ; msp430GccSupport = γ; newlib = δ; stdenvNoCC = β;} -> ε