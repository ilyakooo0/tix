∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { assertions = [{ assertion = Bool; message = String }]; environment = { systemPackages = ∀ β. [β] }; services = { dbus = { packages = ∀ α. [α] } }; systemd = { packages = ∀ β. [β]; services = { iwd = { wantedBy = [String] } } } }; meta = { maintainers = ∀ γ. [γ] }; options = { networking = { wireless = { iwd = { enable = String } } } } }