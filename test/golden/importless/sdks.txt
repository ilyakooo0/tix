∀ α β γ δ ε ζ η θ ι.
(α.generators = β, β.toPlist = γ) => { lib = α;
  runCommand = String -> {} -> String -> δ;
  sdkName = ε;
  toolchainName = ζ;
  version = η;
  writeText = θ;
  xcodePlatform = ι;
} ->
δ