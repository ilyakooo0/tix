∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      services = {udev = {packages = [θ];};};
      users = {groups = {clickshare = {};};};
    } ->
    ι,
  γ.yarny = κ,
  δ.clickshare-csc1 = η,
  λ.modules = γ,
  μ.clickshare-csc1 = β,
  ν.programs = μ,
  ξ.maintainers = γ,
  ο.options = γ
) =>
α ->
{ config = ι;
  meta = {maintainers = [κ];};
  options = {programs = {clickshare-csc1 = {enable = ζ;};};};
}