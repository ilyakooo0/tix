∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.enable = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    β ->
    { environment = {systemPackages = [ε];};
      services = {udev = {packages = [ζ];};};
      users = {groups = {clickshare = {};};};
    } ->
    η,
  γ.yarny = θ,
  ι.clickshare-csc1 = ε,
  κ.clickshare-csc1 = α,
  λ.programs = κ,
  μ.modules = γ,
  ν.maintainers = γ,
  ξ.options = γ
) =>
{config = α; lib = γ; pkgs = ι;} ->
{ config = η;
  meta = {maintainers = [θ];};
  options = {programs = {clickshare-csc1 = {enable = δ;};};};
}