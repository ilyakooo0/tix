∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.sysdig = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { boot = {extraModulePackages = [θ];};
      environment = {systemPackages = [ι];};
    } ->
    κ,
  δ.sysdig = ι,
  ε.enable = η,
  λ.programs = β,
  μ.kernelPackages = β,
  ν.boot = μ
) =>
α -> {config = κ; options = {programs = {sysdig = {enable = ζ;};};};}