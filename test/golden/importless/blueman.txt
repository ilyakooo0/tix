∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.blueman = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { environment = {systemPackages = [θ];};
      services = {dbus = {packages = [ι];};};
      systemd = {packages = [κ];};
    } ->
    λ,
  δ.blueman = θ,
  ε.enable = η,
  μ.services = β
) =>
α -> {config = λ; options = {services = {blueman = {enable = ζ;};};};}