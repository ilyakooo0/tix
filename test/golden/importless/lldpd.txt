∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; systemd = { packages = ∀ γ. [γ]; services = { lldpd = { environment = { LLDPD_OPTIONS = ∀ α. α }; wantedBy = [String] } } }; users = { groups = { _lldpd = {  } }; users = { _lldpd = { description = String; group = String; home = String; isSystemUser = Bool } } } }; options = { services = { lldpd = { enable = String; extraArgs = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β } } } } }