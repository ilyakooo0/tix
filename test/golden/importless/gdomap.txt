∀ α β γ δ ε ζ η θ ι κ λ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { systemd =
        { services =
            { gdomap =
                { after = [String];
                  description = String;
                  path = [η];
                  serviceConfig = {ExecStart = String;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    θ,
  δ.base = η,
  ι.gdomap = β,
  κ.services = ι,
  λ.gnustep = δ
) =>
α -> {config = θ; options = {services = {gdomap = {enable = ζ;};};};}