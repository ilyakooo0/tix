∀ α β γ δ ε ζ η θ ι κ.
( α.enable = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    β ->
    { systemd =
        { services =
            { gdomap =
                { after = [String];
                  description = String;
                  path = [ε];
                  serviceConfig = {ExecStart = String;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    ζ,
  η.base = ε,
  θ.gdomap = α,
  ι.services = θ,
  κ.gnustep = η
) =>
{config = α; lib = γ; pkgs = η;} ->
{config = ζ; options = {services = {gdomap = {enable = δ;};};};}