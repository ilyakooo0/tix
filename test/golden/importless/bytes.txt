∀ α β γ δ ε.
(α.brainrape = β, γ.maintainers = α) => { build-idris-package =
    { meta = {description = String; homepage = String; maintainers = [β];};
      name = String;
      src = δ;
      version = String;
    } ->
    ε;
  fetchFromGitHub =
    {owner = String; repo = String; rev = String; sha256 = String;} -> δ;
  lib = α;
} ->
ε