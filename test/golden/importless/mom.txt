∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.mom = β,
  γ.torque = δ,
  γ.writeText = String -> String -> ε,
  ζ.inetutils = δ,
  ζ.mkEnableOption = String -> η,
  ζ.mkIf =
    θ ->
    { environment = {systemPackages = [ι];};
      systemd =
        { services =
            { torque-mom =
                { after = [String];
                  path = [δ];
                  requires = [String];
                  serviceConfig =
                    {ExecStart = String; PIDFile = String; Type = String;};
                  wantedBy = [String];
                };
              torque-mom-init =
                { path = [δ];
                  script = String;
                  serviceConfig = {Type = String;};
                  unitConfig = {ConditionPathExists = String;};
                };
            };
        };
    } ->
    κ,
  ζ.mkOption = {description = String; type = λ;} -> μ,
  ζ.procps = δ,
  ζ.types = ν,
  ζ.utillinux = δ,
  β.enable = θ,
  ξ.torque = α,
  ο.services = ξ,
  ν.str = λ
) =>
{config = α; lib = ζ; pkgs = γ;} ->
{ config = κ;
  options = {services = {torque = {mom = {enable = η; serverNode = μ;};};};};
}