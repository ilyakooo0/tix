∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; systemd = { services = { torque-mom = { after = [String]; path = [∀ α. α]; requires = [String]; serviceConfig = { ExecStart = String; PIDFile = String; Type = String }; wantedBy = [String] }; torque-mom-init = { path = [∀ α. α]; script = String; serviceConfig = { Type = String }; unitConfig = { ConditionPathExists = String } } } } }; options = { services = { torque = { mom = { enable = String; serverNode = { description = String; type = ∀ α. α } } } } } }