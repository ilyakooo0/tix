∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.throttled = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { environment = {etc = {lenovo_fix.conf = {source = String;};};};
      systemd =
        {packages = [ζ]; services = {lenovo_fix = {wantedBy = [String];};};};
    } ->
    η,
  γ.mkOption = {default = String; description = String; type = θ;} -> ι,
  γ.types = κ,
  λ.throttled = ζ,
  λ.writeText = String -> μ -> String,
  β.enable = ε,
  β.extraConfig = String,
  ν.services = α,
  κ.str = θ
) =>
{config = α; lib = γ; pkgs = λ;} ->
{ config = η;
  options = {services = {throttled = {enable = δ; extraConfig = ι;};};};
}