∀ α β γ δ ε.
( α.config = β,
  α.lib = γ,
  γ.mkIf = Bool -> String -> δ,
  γ.mkOption = {default = Bool;} -> ε,
  β.value = Bool
) =>
α -> {config = {value = {foo = δ;};}; options = {conditionalWorks = ε;};}