∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ.
( α.config = β,
  α.lib = γ,
  δ.device = ε,
  δ.fsType = ζ,
  β.defaultUnit = String,
  β.fileSystems = η,
  β.lockKernelModules = θ,
  γ.concatMap = (δ -> [String]) -> η -> ι,
  γ.maintainers = κ,
  γ.mkIf =
    θ ->
    { boot = {kernelModules = ι;};
      systemd =
        { services =
            { disable-kernel-module-loading =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      RemainAfterExit = Bool;
                      Type = String;
                    };
                  unitConfig = {ConditionPathIsReadWrite = String;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    λ,
  γ.mkOption = {default = Bool; description = String; type = μ;} -> ν,
  γ.types = ξ,
  ο.security = β,
  π.build = β,
  ρ.system = π,
  σ.systemd = β,
  κ.joachifm = τ,
  ξ.bool = μ
) =>
α ->
{ config = λ;
  meta = {maintainers = [τ];};
  options = {security = {lockKernelModules = ν;};};
}