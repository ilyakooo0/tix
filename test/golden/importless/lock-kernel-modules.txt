∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.defaultUnit = String,
  α.fileSystems = β,
  α.lockKernelModules = γ,
  δ.concatMap = (ε -> [String]) -> β -> ζ,
  δ.maintainers = η,
  δ.mkIf =
    γ ->
    { boot = {kernelModules = ζ;};
      systemd =
        { services =
            { disable-kernel-module-loading =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      RemainAfterExit = Bool;
                      Type = String;
                    };
                  unitConfig = {ConditionPathIsReadWrite = String;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    θ,
  δ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  δ.types = λ,
  ε.device = μ,
  ε.fsType = ν,
  ξ.security = α,
  ο.build = α,
  π.system = ο,
  ρ.systemd = α,
  η.joachifm = σ,
  λ.bool = ι
) =>
{config = α; lib = δ;} ->
{ config = θ;
  meta = {maintainers = [σ];};
  options = {security = {lockKernelModules = κ;};};
}