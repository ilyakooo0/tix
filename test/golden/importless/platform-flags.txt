∀ α β γ δ ε ζ η θ.
( ({}) // (α) ~ (β),
  γ.concatLists = [δ] -> ε,
  γ.optional = Bool -> String -> δ,
  ζ.abi = α,
  ζ.gcc = {},
  ζ.is64bit = Bool,
  ζ.isLittleEndian = Bool,
  ζ.isPower = Bool,
  ζ.isx86_64 = Bool,
  ζ.libc = String,
  η.platform = ζ,
  θ.parsed = ζ
) =>
{lib = γ; targetPlatform = ζ;} -> ε