∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  β.bcc = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { boot = {extraModulePackages = [δ];};
      environment = {systemPackages = [η];};
    } ->
    θ,
  ι.bcc = β,
  κ.programs = ι,
  λ.kernelPackages = β,
  μ.boot = λ,
  ν.kernelPackages = β,
  ξ.boot = ν
) =>
α -> {config = θ; options = {programs = {bcc = {enable = ζ;};};};}