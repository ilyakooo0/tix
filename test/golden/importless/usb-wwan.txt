∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {etc = {usb_modeswitch.d = {source = String;};};};
      services = {udev = {packages = [ζ];};};
      systemd = {packages = [η];};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.types = λ,
  γ.usb-modeswitch = η,
  γ.usb-modeswitch-data = ζ,
  μ.usbWwan = β,
  ν.hardware = μ,
  λ.bool = ι
) =>
α -> {config = θ; options = {hardware = {usbWwan = {enable = κ;};};};}