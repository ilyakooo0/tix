∀ α β γ δ ε ζ η θ ι κ λ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      services = {udev = {packages = [θ];};};
    } ->
    ι,
  δ.wootility = η,
  δ.wooting-udev-rules = θ,
  κ.wooting = β,
  λ.hardware = κ
) =>
α -> {config = ι; options = {hardware = {wooting = {enable = ζ;};};};}