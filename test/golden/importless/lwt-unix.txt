∀ α β γ δ ε ζ η θ.
( α.versionAtLeast = β -> String -> Bool,
  γ.meta = δ,
  γ.src = ε,
  γ.version = β,
  ζ.lib = α
) =>
{ buildDunePackage =
    { buildInputs = [η];
      meta = δ;
      pname = String;
      propagatedBuildInputs = [γ];
      src = ε;
      useDune2 = Bool;
      version = θ;
    } ->
    γ;
  cmdliner = η;
  cohttp-lwt = γ;
  conduit-lwt-unix = γ;
  fmt = γ;
  magic-mime = γ;
  ppx_sexp_conv = η;
  stdenv = α;
} ->
γ