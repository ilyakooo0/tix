∀ β γ δ ε ζ.
( β.lib = γ,
  γ.versionAtLeast = δ -> String -> Bool,
  ε.version = δ
) =>
{ buildDunePackage =
    { buildInputs = [ζ];
      meta = ∀ α. (ε.meta = α) => α;
      pname = String;
      propagatedBuildInputs = [ε];
      src = ∀ α. (ε.src = α) => α;
      useDune2 = Bool;
      version = ∀ α. (ε.version = α) => α;
    } ->
    ε;
  cmdliner = ζ;
  cohttp-lwt = ε;
  conduit-lwt-unix = ε;
  fmt = ε;
  magic-mime = ε;
  ppx_sexp_conv = ζ;
  stdenv = β;
} ->
ε