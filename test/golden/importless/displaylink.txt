∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.evdi = ε,
  β.videoDrivers = ζ,
  γ.elem = String -> ζ -> η,
  γ.mkIf =
    η ->
    { boot = {extraModulePackages = [ε]; kernelModules = [String];};
      environment =
        {etc = {X11/xorg.conf.d/40-displaylink.conf = {text = String;};};};
      powerManagement = {powerDownCommands = String; resumeCommands = String;};
      services =
        { udev = {packages = [θ];};
          xserver = {displayManager = {sessionCommands = String;};};
        };
      systemd =
        { services =
            { dlm =
                { after = [String];
                  conflicts = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      LogsDirectory = String;
                      Restart = String;
                      RestartSec = Integer;
                    };
                };
            };
        };
    } ->
    ι,
  δ.override = {evdi = ε;} -> θ,
  κ.kernelPackages = β,
  λ.boot = κ,
  μ.xserver = β,
  ν.services = μ,
  ξ.displaylink = δ
) =>
α -> {config = ι;}