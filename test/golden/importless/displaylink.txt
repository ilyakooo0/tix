∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.evdi = β,
  α.videoDrivers = γ,
  δ.elem = String -> γ -> ε,
  δ.mkIf =
    ε ->
    { boot = {extraModulePackages = [β]; kernelModules = [String];};
      environment =
        {etc = {X11/xorg.conf.d/40-displaylink.conf = {text = String;};};};
      powerManagement = {powerDownCommands = String; resumeCommands = String;};
      services =
        { udev = {packages = [ζ];};
          xserver = {displayManager = {sessionCommands = String;};};
        };
      systemd =
        { services =
            { dlm =
                { after = [String];
                  conflicts = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      LogsDirectory = String;
                      Restart = String;
                      RestartSec = Integer;
                    };
                };
            };
        };
    } ->
    η,
  θ.override = {evdi = β;} -> ζ,
  ι.kernelPackages = α,
  κ.boot = ι,
  λ.xserver = α,
  μ.services = λ,
  ν.displaylink = θ
) =>
{config = α; lib = δ; pkgs = θ;} -> {config = η;}