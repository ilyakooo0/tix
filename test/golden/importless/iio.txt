∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { boot = { initrd = { availableKernelModules = [String] } }; environment = { systemPackages = ∀ γ. [γ] }; services = { dbus = { packages = ∀ β. [β] }; udev = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ] } }; options = { hardware = { sensor = { iio = { enable = { default = Bool; description = String; type = ∀ α. α } } } } } }