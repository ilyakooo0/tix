∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.iio-sensor-proxy = ζ,
  γ.mkIf =
    ε ->
    { boot = {initrd = {availableKernelModules = [String];};};
      environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];}; udev = {packages = [θ];};};
      systemd = {packages = [ι];};
    } ->
    κ,
  γ.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  γ.types = ν,
  ξ.iio = β,
  ο.sensor = ξ,
  π.hardware = ο,
  ν.bool = λ
) =>
α -> {config = κ; options = {hardware = {sensor = {iio = {enable = μ;};};};};}