∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.lowPrio = ζ -> η,
  γ.maintainers = θ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      services = {udev = {packages = [ι];};};
      users = {groups = {adbusers = {};};};
    } ->
    κ,
  γ.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  γ.types = ν,
  δ.android-udev-rules = ι,
  δ.platform-tools = ζ,
  ξ.adb = β,
  ο.programs = ξ,
  π.androidPkgs_9_0 = δ,
  ρ.androidenv = π,
  θ.mic92 = σ,
  ν.bool = λ
) =>
α ->
{ config = κ;
  meta = {maintainers = [σ];};
  options = {programs = {adb = {enable = μ;};};};
}