∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { sniproxy = { after = [String]; description = String; preStart = String; serviceConfig = { ExecStart = String; Restart = String; Type = String }; wantedBy = [String] } } }; users = { groups = { sniproxy = { gid = ∀ α. α } }; users = { sniproxy = { group = ∀ α. α; uid = ∀ α. α } } } }; options = { services = { sniproxy = { config = { default = String; description = String; example = String; type = ∀ α. α }; enable = String; group = { default = String; description = String; type = ∀ α. α }; logDir = { default = String; description = String; type = ∀ α. α }; user = { default = String; description = String; type = ∀ α. α } } } } }