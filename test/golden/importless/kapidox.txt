∀ α β γ δ ε.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  lib = α;
  mkDerivation =
    { meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
      postFixup = String;
    } ->
    ε;
  python = δ;
} ->
ε