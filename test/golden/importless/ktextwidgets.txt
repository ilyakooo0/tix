∀ γ δ ε ζ η.
{ extra-cmake-modules = γ;
  kcompletion = δ;
  kconfig = δ;
  kconfigwidgets = δ;
  ki18n = ε;
  kiconthemes = δ;
  kservice = δ;
  kwindowsystem = δ;
  lib = ζ;
  mkDerivation =
    { buildInputs = [δ];
      meta = {maintainers = ∀ α β. (ζ.maintainers = α, α.ttuegel = β) => [β];};
      name = String;
      nativeBuildInputs = [γ];
      propagatedBuildInputs = [ε];
    } ->
    η;
  qtbase = ε;
  qttools = ε;
  sonnet = ε;
} ->
η