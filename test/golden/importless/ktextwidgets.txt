∀ α β γ δ ε ζ η.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  kcompletion = ε;
  kconfig = ε;
  kconfigwidgets = ε;
  ki18n = ζ;
  kiconthemes = ε;
  kservice = ε;
  kwindowsystem = ε;
  lib = α;
  mkDerivation =
    { buildInputs = [ε];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
      propagatedBuildInputs = [ζ];
    } ->
    η;
  qtbase = ζ;
  qttools = ζ;
  sonnet = ζ;
} ->
η