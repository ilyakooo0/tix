∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; systemd = { user = { services = { bloop = { description = String; environment = { PATH = String }; serviceConfig = { ExecStart = String; Restart = String; Type = String } } } } } }; options = { services = { bloop = { extraOptions = { default = ∀ α. α; description = String; example = [String]; type = ∀ α. α }; install = { default = Bool; description = String; type = ∀ α. α } } } } }