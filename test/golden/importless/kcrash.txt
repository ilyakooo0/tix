∀ γ δ ε ζ η.
{ extra-cmake-modules = γ;
  kcoreaddons = δ;
  kwindowsystem = δ;
  lib = ε;
  mkDerivation =
    { buildInputs = [δ];
      meta = {maintainers = ∀ α β. (ε.maintainers = α, α.ttuegel = β) => [β];};
      name = String;
      nativeBuildInputs = [γ];
      outputs = [String];
      propagatedBuildInputs = [ζ];
    } ->
    η;
  qtbase = ζ;
  qtx11extras = δ;
} ->
η