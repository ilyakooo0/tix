∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β1 γ1.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.gateone = ε,
  γ.coreutils = ζ,
  γ.gateone = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { environment = {systemPackages = [η];};
      systemd =
        { services =
            { gateone =
                { description = String;
                  path = [ζ];
                  preStart = String;
                  requires = [String];
                  serviceConfig =
                    { ExecStart = String;
                      Group = String;
                      User = String;
                      WorkingDirectory = κ;
                    };
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {gateone = {gid = λ;};};
          users = {gateone = {description = String; home = μ; uid = ν;};};
        };
    } ->
    ξ,
  γ.mkOption = {default = String; description = String; type = ο;} -> π,
  γ.nix = ζ,
  γ.openssh = ζ,
  γ.procps = ζ,
  γ.pythonPackages = ρ,
  γ.types = σ,
  δ.cacert = η,
  δ.coreutils = η,
  δ.openssh = η,
  δ.procps = η,
  δ.pythonPackages = τ,
  ε.enable = ι,
  ε.settingsDir = κ,
  υ.services = β,
  ρ.gateone = ζ,
  φ.gids = β,
  χ.ids = φ,
  ψ.uids = β,
  ω.ids = ψ,
  σ.path = ο,
  α1.path = β1
) =>
α ->
{ config = ξ;
  options =
    {services = {gateone = {enable = θ; pidDir = π; settingsDir = γ1;};};};
}