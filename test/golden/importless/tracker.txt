∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  δ.tracker = ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];};};
      systemd = {packages = [θ];};
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.teams = μ,
  γ.types = ν,
  ξ.tracker = β,
  ο.gnome3 = ξ,
  π.services = ο,
  μ.members = ρ,
  σ.gnome = μ,
  ν.bool = κ
) =>
α ->
{ config = ι;
  meta = {maintainers = ρ;};
  options = {services = {gnome3 = {tracker = {enable = λ;};};};};
}