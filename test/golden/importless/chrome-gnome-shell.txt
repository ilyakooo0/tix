∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { chromium/native-messaging-hosts/org.gnome.chrome_gnome_shell.json = { source = String }; opt/chrome/native-messaging-hosts/org.gnome.chrome_gnome_shell.json = { source = String } }; systemPackages = ∀ β. [β] }; nixpkgs = { config = { firefox = { enableGnomeExtensions = Bool } } }; services = { dbus = { packages = ∀ α. [α] } } }; meta = { maintainers = ∀ γ. γ }; options = { services = { gnome3 = { chrome-gnome-shell = { enable = String } } } } }