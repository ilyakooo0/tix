∀ α β γ δ ε ζ η θ.
( α.urserver = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { networking =
        { firewall =
            {allowedTCPPorts = [Integer]; allowedUDPPorts = [Integer];};
        };
      systemd =
        { user =
            { services =
                { urserver =
                    { after = [String];
                      description = String;
                      partOf = [String];
                      serviceConfig =
                        { ExecStart = String;
                          ExecStop = String;
                          Restart = String;
                          RestartSec = Integer;
                          Type = String;
                        };
                      wantedBy = [String];
                    };
                };
            };
        };
    } ->
    ζ,
  β.enable = ε,
  η.services = α
) =>
{config = α; lib = γ; pkgs = θ;} ->
{config = ζ; options = {services = {urserver = {enable = δ;};};};}