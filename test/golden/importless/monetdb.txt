∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; systemd = { services = { monetdb = { after = [String]; description = String; path = ∀ β. [β]; preStart = String; serviceConfig = { ExecStart = String; ExecStop = String; Group = ∀ α. α; User = ∀ α. α }; unitConfig = { RequiresMountsFor = String }; wantedBy = [String] } } }; users = { groups = { monetdb = { gid = ∀ β. β; members = ∀ β. [β] } }; users = { monetdb = { createHome = Bool; description = String; group = ∀ β. β; home = ∀ β. β; uid = ∀ β. β } } } }; meta = { maintainers = ∀ δ. [δ] }; options = { services = { monetdb = { dataDir = { default = String; description = String; type = ∀ β. β }; enable = String; group = { default = String; description = String; type = ∀ β. β }; listenAddress = { default = String; description = String; example = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β }; port = { default = Integer; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }