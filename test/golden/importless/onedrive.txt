∀ λ μ ν ξ ο.
(λ.services = μ, λ.services = ν) => {config = λ; lib = ξ; pkgs = ο;} ->
{ config =
    ∀ η θ ι κ.
    ( ξ.mkIf =
        η ->
        { environment =
            {systemPackages = ∀ γ δ ε. (γ.onedrive = δ, δ.package = ε) => [ε];};
          systemd =
            { user =
                { services =
                    { onedrive-launcher =
                        { serviceConfig = {ExecStart = String; Type = String;};
                          wantedBy = [String];
                        };
                      onedrive@ =
                        { description = String;
                          serviceConfig =
                            { ExecStart = String;
                              Restart = String;
                              RestartPreventExitStatus = Integer;
                              RestartSec = Integer;
                              Type = String;
                            };
                        };
                    };
                };
            };
        } ->
        θ,
      ι.onedrive = κ,
      κ.enable = η
    ) =>
    θ;
  options =
    { services =
        { onedrive =
            { enable =
                ∀ ζ.
                ( ξ.mkOption =
                    { default = Bool;
                      description = String;
                      type = ∀ α β. (ξ.types = α, α.bool = β) => β;
                    } ->
                    ζ
                ) =>
                ζ;
              package =
                ∀ ζ.
                ( ξ.mkOption =
                    { default = ∀ α. (ο.onedrive = α) => α;
                      defaultText = String;
                      description = String;
                      example = ∀ α. (ξ.literalExample = String -> α) => α;
                      type = ∀ α β. (ξ.types = α, α.package = β) => β;
                    } ->
                    ζ
                ) =>
                ζ;
            };
        };
    };
}