∀ α β γ δ ε ζ η θ ι κ.
( α.appendToName = String -> β -> γ,
  δ.meta = ε,
  δ.name = ζ,
  δ.version = η,
  θ.all = [δ],
  ι.lib = α
) =>
{ lilypond = δ;
  lndir = κ;
  makeWrapper = κ;
  openlilylib-fonts = θ;
  stdenv = α;
  symlinkJoin =
    { buildInputs = [κ];
      meta = ε;
      name = ζ;
      paths = [δ];
      postBuild = String;
      version = η;
    } ->
    β;
} ->
γ