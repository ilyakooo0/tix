∀ β γ δ ε ζ η θ.
(β.lib = γ, γ.appendToName = String -> δ -> ε) => { lilypond = ζ;
  lndir = η;
  makeWrapper = η;
  openlilylib-fonts = θ;
  stdenv = β;
  symlinkJoin =
    { buildInputs = [η];
      meta = ∀ α. (ζ.meta = α) => α;
      name = ∀ α. (ζ.name = α) => α;
      paths = (θ.all = [ζ]) => [ζ];
      postBuild = String;
      version = ∀ α. (ζ.version = α) => α;
    } ->
    δ;
} ->
ε