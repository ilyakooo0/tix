∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.services = ε,
  β.usb = ζ,
  γ.any = η -> θ -> Bool,
  γ.attrByPath = [String] -> Bool -> η,
  γ.attrValues = ε -> θ,
  γ.mkIf =
    Bool ->
    { environment = {systemPackages = [ι];};
      security =
        { wrappers =
            {pmount = {source = String;}; pumount = {source = String;};};
        };
    } ->
    κ,
  γ.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  γ.types = ν,
  δ.pmount = ι,
  ζ.enable = Bool,
  ξ.pam = β,
  ο.security = ξ,
  π.pam = β,
  ρ.security = π,
  ν.bool = λ
) =>
α -> {config = κ; options = {security = {pam = {usb = {enable = μ;};};};};}