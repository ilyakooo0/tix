∀ β γ δ ε ζ.
( β.mkDerivation =
    { buildInputs = [γ];
      buildPhase = String;
      installPhase = ∀ α. (δ.installPhase = α) => α;
      meta = ∀ α. (δ.meta = α) => α;
      pname = String;
      propagatedBuildInputs = [ε];
      src = ∀ α. (δ.src = α) => α;
      version = ∀ α. (δ.version = α) => α;
    } ->
    ζ
) =>
{ dune_2 = γ;
  findlib = γ;
  js_of_ocaml = ε;
  js_of_ocaml-compiler = δ;
  ocaml = γ;
  ppxlib = ε;
  stdenv = β;
} ->
ζ