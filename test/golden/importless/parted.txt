∀ η θ ι κ λ μ.
( η.mkDerivation =
    { buildInputs =
        ∀ δ ε ζ.
        ( η.lib = δ,
          δ.optional = Bool -> Null -> [θ],
          η.lib = ε,
          ε.optional = Bool -> Null -> [θ],
          η.lib = ζ,
          ζ.optional = Bool -> Null -> [θ]
        ) =>
        [θ];
      checkInputs = [ι];
      configureFlags =
        ∀ δ ε.
        ( η.lib = δ,
          δ.optional = Bool -> String -> [String],
          η.lib = ε,
          ε.optional = Bool -> String -> [String]
        ) =>
        [String];
      doCheck = Bool;
      meta =
        { description = String;
          homepage = String;
          license = ∀ α β γ. (η.lib = α, α.licenses = β, β.gpl3Plus = γ) => γ;
          longDescription = String;
          maintainers = ∀ α. α;
          platforms = ∀ α β γ. (η.lib = α, α.platforms = β, β.linux = γ) => γ;
        };
      name = String;
      patches = [κ];
      src = λ;
    } ->
    μ
) =>
{ check = ι;
  enableStatic = Bool;
  fetchpatch = {sha256 = String; url = String;} -> κ;
  fetchurl = {sha256 = String; url = String;} -> λ;
  gettext = Null;
  libuuid = θ;
  lvm2 = Null;
  readline = Null;
  stdenv = η;
  utillinux = ι;
} ->
μ