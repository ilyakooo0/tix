∀ γ δ.
{ alcotest = γ;
  buildDunePackage =
    { meta = ∀ α β. ((α) // ({description = String;}) ~ (β), γ.meta = α) => β;
      pname = String;
      propagatedBuildInputs = [γ];
      src = ∀ α. (γ.src = α) => α;
      useDune2 = Bool;
      version = ∀ α. (γ.version = α) => α;
    } ->
    δ;
  cmdliner = γ;
  irmin = γ;
  metrics-unix = γ;
  mtime = γ;
} ->
δ