∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { extraInit = String; systemPackages = ∀ γ. [γ] }; systemd = { packages = ∀ γ. [γ]; user = { services = { yubikey-agent = { path = ∀ α. [α] } } } } }; meta = { maintainers = ∀ δ. [δ] }; options = { services = { yubikey-agent = { enable = { default = Bool; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β } } } } }