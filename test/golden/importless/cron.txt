∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = [{ services = { cron = { enable = Bool } } }]; options = { services = { cron = { cronFiles = { default = ∀ α. α; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; mailto = { default = Null; description = String; type = ∀ α. α }; systemCronJobs = { default = ∀ α. α; description = String; example = String; type = ∀ α. α } } } } }