∀ α β γ δ ε ζ η.
( α.mkDerivation =
    { dontUnpack = Bool;
      installPhase = String;
      meta = {platforms = β;};
      name = String;
      nativeBuildInputs = [γ];
    } ->
    δ,
  α.unix = β,
  ε.platforms = α,
  ζ.lib = ε
) =>
{fetchurl = {sha256 = String; url = String;} -> η; stdenv = α; unzip = γ;} -> δ