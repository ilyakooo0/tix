∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = [∀ γ. γ] }; services = { xserver = { windowManager = { session = { name = String; start = String } } } } }; options = { services = { xserver = { windowManager = { exwm = { enable = String; enableDefaultConfig = { default = Bool; description = String; type = ∀ α. α }; extraPackages = { default = ∀ α β. α -> β; description = String; example = String } } } } } } }