∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; systemd = { services = { cgminer = { after = [String]; environment = { DISPLAY = String; GPU_MAX_ALLOC_PERCENT = String; GPU_USE_SYNC_OBJECTS = String; LD_LIBRARY_PATH = String }; path = ∀ β. [β]; serviceConfig = { ExecStart = String; Restart = String; RestartSec = String; StartLimitInterval = String; User = ∀ α. α }; wantedBy = [String] } } }; users = { users = { cgminer = { description = String; uid = ∀ β. β } } } }; options = { services = { cgminer = { config = { default = {  }; description = String; example = { auto-fan = Bool; auto-gpu = Bool; expiry = Integer; failover-only = Bool; gpu-threads = Integer; log = Integer; queue = Integer; scan-time = Integer; temp-histeresys = Integer } }; enable = String; hardware = { default = ∀ β. β; description = String; example = [{ gpu-engine = String; gpu-fan = String; gpu-memclock = Integer; gpu-powertune = Integer; intensity = Integer; temp-cutoff = Integer; temp-overheat = Integer; temp-target = Integer }] }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β }; pools = { default = ∀ β. β; description = String; example = [{ password = String; url = String; username = String }] }; user = { default = String; description = String } } } } }