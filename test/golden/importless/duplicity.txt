∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { assertions = { assertion = Bool; message = String }; systemd = { services = { duplicity = { description = String; environment = { HOME = String }; serviceConfig = { EnvironmentFile = ∀ α. α; ExecStart = String; PrivateTmp = Bool; ProtectHome = String; ProtectSystem = String; ReadWritePaths = Null; StateDirectory = String }; startAt = ∀ β. β } }; tmpfiles = { rules = String } } }; options = { services = { duplicity = { enable = String; exclude = { default = ∀ β. β; description = String; type = ∀ β. β }; extraFlags = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; frequency = { default = String; description = String; type = ∀ β. β }; include = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; root = { default = String; description = String; type = ∀ β. β }; secretFile = { default = Null; description = String; type = ∀ β. β }; targetUrl = { description = String; example = String; type = ∀ β. β } } } } }