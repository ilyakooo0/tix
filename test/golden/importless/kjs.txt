∀ α β γ δ ε ζ.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  kdoctools = δ;
  lib = α;
  mkDerivation =
    { buildInputs = [ε];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
    } ->
    ζ;
  pcre = ε;
  qtbase = ε;
} ->
ζ