∀ β γ δ ε ζ η θ ι κ λ μ ν.
( β.config = γ,
  β.lib = δ,
  β.pkgs = ε,
  γ.interfaces = ζ,
  δ.fold = (η -> η -> η) -> String -> θ -> ι,
  δ.map =
    ({interface = κ; method = String; password = String;} -> String) -> ζ -> θ,
  δ.mkOption =
    { default = ∀ α. α;
      description = String;
      example = [{interface = String; method = String; password = String;}];
    } ->
    λ,
  δ.throw = String -> String,
  μ.wakeonlan = γ,
  ν.services = μ
) =>
β ->
{ config = {powerManagement = {powerDownCommands = ι;};};
  options = {services = {wakeonlan = {interfaces = λ;};};};
}