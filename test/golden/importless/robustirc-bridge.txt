∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { robustirc-bridge = { after = [String]; description = String; documentation = [String]; serviceConfig = { DynamicUser = Bool; ExecStart = String; PrivateDevices = Bool; PrivateTmp = Bool; ProtectHome = Bool; ProtectSystem = Bool; Restart = String }; wantedBy = [String] } } } }; options = { services = { robustirc-bridge = { enable = String; extraFlags = { default = ∀ α. α; description = String; example = [String]; type = ∀ α. α } } } } }