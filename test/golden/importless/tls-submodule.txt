∀ α β γ δ ε ζ η.
( α.lib = β,
  β.mkOption =
    {default = String; description = String; example = String; type = γ;} -> δ,
  β.str = γ,
  ε.types = β,
  ζ.types = β
) =>
α -> {options = {crt = δ; key = η;};}