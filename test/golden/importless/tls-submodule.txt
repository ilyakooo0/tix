∀ α β γ δ ε ζ.
( α.mkOption =
    {default = String; description = String; example = String; type = β;} -> γ,
  α.str = β,
  δ.types = α,
  ε.types = α
) =>
{lib = α;} -> {options = {crt = γ; key = ζ;};}