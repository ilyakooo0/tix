∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.calibre-server = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { environment = {systemPackages = [θ];};
      systemd =
        { services =
            { calibre-server =
                { after = [String];
                  description = String;
                  serviceConfig =
                    {ExecStart = String; Restart = String; User = String;};
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {calibre-server = {gid = ι;};};
          users = {calibre-server = {group = String; uid = κ;};};
        };
    } ->
    λ,
  γ.mkOption = {description = String; type = μ;} -> ν,
  γ.types = ξ,
  δ.calibre = θ,
  ε.enable = η,
  ο.services = β,
  π.gids = β,
  ρ.ids = π,
  σ.uids = β,
  τ.ids = σ,
  ξ.path = μ
) =>
α ->
{ config = λ;
  options = {services = {calibre-server = {enable = ζ; libraryDir = ν;};};};
}