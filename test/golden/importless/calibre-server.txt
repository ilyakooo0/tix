∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; systemd = { services = { calibre-server = { after = [String]; description = String; serviceConfig = { ExecStart = String; Restart = String; User = String }; wantedBy = [String] } } }; users = { groups = { calibre-server = { gid = ∀ α. α } }; users = { calibre-server = { group = String; uid = ∀ α. α } } } }; options = { services = { calibre-server = { enable = String; libraryDir = { description = String; type = ∀ α. α } } } } }