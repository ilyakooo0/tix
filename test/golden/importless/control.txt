∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ.
( β.majorMinor = String -> γ,
  β.mkDerivation =
    { buildInputs = [δ];
      doBuild = Bool;
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = ε;
          maintainers = [ζ];
          platforms = [String];
        };
      nativeBuildInputs = [η];
      pname = String;
      postFixup = String;
      src = θ;
      unpackPhase = String;
      version = ∀ α. α;
    } ->
    ι,
  β.unfree = ε,
  β.zimbatm = ζ,
  κ.withPackages = (λ -> [μ]) -> δ,
  λ.pycairo = μ,
  λ.pygobject2 = ν,
  λ.pygtk = ξ,
  ο.versions = β,
  π.lib = ο,
  ρ.licenses = β,
  σ.lib = ρ,
  τ.maintainers = β,
  υ.lib = τ
) =>
{ autoPatchelfHook = φ;
  dpkg = η;
  fahviewer = δ;
  fetchurl = {sha256 = String; url = String;} -> θ;
  makeWrapper = η;
  python2 = κ;
  stdenv = β;
} ->
ι