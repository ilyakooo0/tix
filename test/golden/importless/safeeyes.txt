∀ α β γ δ ε ζ η θ.
( α.safeeyes = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { systemd =
        { user =
            { services =
                { safeeyes =
                    { description = String;
                      partOf = [String];
                      serviceConfig =
                        { ExecStart = String;
                          Restart = String;
                          RestartSec = Integer;
                          StartLimitBurst = Integer;
                          StartLimitInterval = Integer;
                        };
                      wantedBy = [String];
                    };
                };
            };
        };
    } ->
    ζ,
  β.enable = ε,
  η.services = α
) =>
{config = α; lib = γ; pkgs = θ;} ->
{config = ζ; options = {services = {safeeyes = {enable = δ;};};};}