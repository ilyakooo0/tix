∀ α β γ δ ε ζ η θ ι.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.safeeyes = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { systemd =
        { user =
            { services =
                { safeeyes =
                    { description = String;
                      partOf = [String];
                      serviceConfig =
                        { ExecStart = String;
                          Restart = String;
                          RestartSec = Integer;
                          StartLimitBurst = Integer;
                          StartLimitInterval = Integer;
                        };
                      wantedBy = [String];
                    };
                };
            };
        };
    } ->
    θ,
  ε.enable = η,
  ι.services = β
) =>
α -> {config = θ; options = {services = {safeeyes = {enable = ζ;};};};}