∀ α β γ δ ε ζ η θ ι κ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.powertop = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { systemd =
        { services =
            { powertop =
                { after = [String];
                  description = String;
                  path = [θ];
                  serviceConfig =
                    { ExecStart = String;
                      RemainAfterExit = String;
                      Type = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  δ.kmod = θ,
  ε.enable = η,
  κ.powerManagement = β
) =>
α -> {config = ι; options = {powerManagement = {powertop = {enable = ζ;};};};}