∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β2 γ3 δ4 ε5 ζ6 η7 θ8 ι9 κ10
λ11 μ12 ν13 ξ14 ο15 π16 ρ17 σ18 τ19 υ20 φ21 χ22 ψ23 ω24.
( β.Accelerate = γ,
  β.AppKit = δ,
  β.ApplicationServices = ε,
  β.AudioToolbox = ζ,
  β.AudioUnit = ζ,
  β.CFNetwork = η,
  β.Carbon = θ,
  β.CoreAudio = ι,
  β.CoreData = η,
  β.CoreFoundation = η,
  β.CoreGraphics = ε,
  β.CoreImage = κ,
  β.CoreMIDI = ι,
  β.CoreMedia = λ,
  β.CoreMediaIO = μ,
  β.CoreServices = ν,
  β.CoreText = ν,
  β.CoreVideo = ξ,
  β.CoreWLAN = ο,
  β.DiskArbitration = η,
  β.Foundation = ζ,
  β.IOBluetooth = ο,
  β.IOKit = π,
  β.IOSurface = γ,
  β.ImageIO = ν,
  β.JavaScriptCore = ρ,
  β.MediaToolbox = μ,
  β.Metal = σ,
  β.ModelIO = σ,
  β.NetFS = η,
  β.OpenCL = κ,
  β.OpenDirectory = η,
  β.OpenGL = θ,
  β.QTKit = τ,
  β.QuartzCore = ζ,
  β.QuickLook = τ,
  β.QuickTime = μ,
  β.Security = π,
  β.SecurityFoundation = υ,
  β.ServiceManagement = η,
  β.SystemConfiguration = γ,
  β.VideoToolbox = μ,
  β.xpc = φ
) =>
{frameworks = β; libobjc = χ; libs = ψ;} ->
{ AGL = [θ];
  AVFoundation = [ε];
  AVKit = ∀ α. α;
  Accelerate = [ο];
  Accounts = ∀ α. α;
  AddressBook = [ω];
  AppKit = [ζ];
  AppKitScripting = ∀ α. α;
  AppleScriptKit = ∀ α. α;
  AppleScriptObjC = ∀ α. α;
  ApplicationServices = [ν];
  AudioToolbox = [ι];
  AudioUnit = [α1];
  AudioVideoBridging = [β2];
  Automator = ∀ α. α;
  CFNetwork = ∀ α. α;
  CalendarStore = ∀ α. α;
  Carbon = [π];
  Cocoa = [δ];
  Collaboration = ∀ α. α;
  CoreAudio = [γ3];
  CoreAudioKit = [δ4];
  CoreBluetooth = ∀ α. α;
  CoreData = ∀ α. α;
  CoreFoundation = ∀ α. α;
  CoreGraphics = [γ];
  CoreImage = ∀ α. α;
  CoreLocation = ∀ α. α;
  CoreMIDI = ∀ α. α;
  CoreMIDIServer = ∀ α. α;
  CoreMedia = [ξ];
  CoreMediaIO = [λ];
  CoreServices = [η];
  CoreText = [ε5];
  CoreVideo = [ζ6];
  CoreWLAN = [υ];
  DVDPlayback = ∀ α. α;
  DirectoryService = ∀ α. α;
  DiscRecording = [η7];
  DiscRecordingUI = ∀ α. α;
  DiskArbitration = [θ8];
  EventKit = ∀ α. α;
  ExceptionHandling = ∀ α. α;
  FWAUserLib = ∀ α. α;
  ForceFeedback = [ι9];
  Foundation = [χ];
  GLKit = ∀ α. α;
  GLUT = [κ10];
  GSS = ∀ α. α;
  GameController = ∀ α. α;
  GameKit = [λ11];
  Hypervisor = ∀ α. α;
  ICADevices = [μ12];
  IMServicePlugIn = ∀ α. α;
  IOBluetooth = [ν13];
  IOBluetoothUI = [ξ14];
  IOKit = ∀ α. α;
  IOSurface = [φ];
  ImageCaptureCore = ∀ α. α;
  ImageIO = [ο15];
  InputMethodKit = [π16];
  InstallerPlugins = ∀ α. α;
  InstantMessage = ∀ α. α;
  JavaFrameEmbedding = ∀ α. α;
  JavaNativeFoundation = ∀ α. α;
  JavaRuntimeSupport = ∀ α. α;
  JavaScriptCore = ∀ α. α;
  JavaVM = ∀ α. α;
  Kerberos = ∀ α. α;
  Kernel = [ρ17];
  LDAP = ∀ α. α;
  LatentSemanticMapping = [σ18];
  LocalAuthentication = ∀ α. α;
  MapKit = ∀ α. α;
  MediaAccessibility = [τ19];
  MediaPlayer = ∀ α. α;
  MediaToolbox = [υ20];
  Metal = ∀ α. α;
  MetalKit = [σ];
  ModelIO = ∀ α. α;
  NetFS = ∀ α. α;
  OSAKit = [φ21];
  OpenAL = ∀ α. α;
  OpenCL = [χ22];
  OpenDirectory = ∀ α. α;
  OpenGL = ∀ α. α;
  PCSC = [ψ23];
  PreferencePanes = ∀ α. α;
  PubSub = ∀ α. α;
  QTKit = [μ];
  Quartz = [τ];
  QuartzCore = [κ];
  QuickLook = [ω24];
  QuickTime = [〚5 47〛];
  SceneKit = ∀ α. α;
  ScreenSaver = ∀ α. α;
  Scripting = ∀ α. α;
  ScriptingBridge = ∀ α. α;
  Security = [〚5 48〛];
  SecurityFoundation = ∀ α. α;
  SecurityInterface = [〚5 49〛];
  ServiceManagement = [〚5 50〛];
  Social = ∀ α. α;
  SpriteKit = ∀ α. α;
  StoreKit = ∀ α. α;
  SyncServices = ∀ α. α;
  SystemConfiguration = [〚5 51〛];
  TWAIN = [〚5 52〛];
  Tcl = ∀ α. α;
  VideoDecodeAcceleration = [〚5 53〛];
  VideoToolbox = [〚5 54〛];
  WebKit = [ρ];
  vmnet = ∀ α. α;
}