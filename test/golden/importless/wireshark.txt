∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = [∀ β. β] }; security = { wrappers = { dumpcap = { capabilities = String; group = String; owner = String; permissions = String; source = String } } }; users = { groups = { wireshark = {  } } } }; options = { programs = { wireshark = { enable = { default = Bool; description = String; type = ∀ α. α }; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }