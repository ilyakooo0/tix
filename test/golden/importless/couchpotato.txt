∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.couchpotato = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { systemd =
        { services =
            { couchpotato =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      Group = String;
                      Restart = String;
                      StateDirectory = String;
                      Type = String;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {couchpotato = {gid = θ;};};
          users =
            { couchpotato =
                {description = String; group = String; home = String; uid = ι;};
            };
        };
    } ->
    κ,
  ε.enable = η,
  λ.services = β,
  μ.gids = β,
  ν.ids = μ,
  ξ.uids = β,
  ο.ids = ξ
) =>
α -> {config = κ; options = {services = {couchpotato = {enable = ζ;};};};}