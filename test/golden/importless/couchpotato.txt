∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { couchpotato = { after = [String]; description = String; serviceConfig = { ExecStart = String; Group = String; Restart = String; StateDirectory = String; Type = String; User = String }; wantedBy = [String] } } }; users = { groups = { couchpotato = { gid = ∀ α. α } }; users = { couchpotato = { description = String; group = String; home = String; uid = ∀ α. α } } } }; options = { services = { couchpotato = { enable = String } } } }