∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.enable = β,
  γ.members = δ,
  γ.mkEnableOption = String -> ε,
  γ.mkIf =
    β ->
    { services = {dbus = {packages = [ζ];};};
      systemd =
        { user =
            { services =
                { nm-applet =
                    { description = String;
                      partOf = [String];
                      serviceConfig = {ExecStart = String;};
                      wantedBy = [String];
                    };
                };
            };
        };
    } ->
    η,
  θ.gcr = ζ,
  ι.nm-applet = α,
  κ.programs = ι,
  λ.freedesktop = γ,
  μ.teams = λ
) =>
{config = α; lib = γ; pkgs = θ;} ->
{ config = η;
  meta = {maintainers = δ;};
  options = {programs = {nm-applet = {enable = ε;};};};
}