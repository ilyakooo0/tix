∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.members = ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    ε ->
    { services = {dbus = {packages = [θ];};};
      systemd =
        { user =
            { services =
                { nm-applet =
                    { description = String;
                      partOf = [String];
                      serviceConfig = {ExecStart = String;};
                      wantedBy = [String];
                    };
                };
            };
        };
    } ->
    ι,
  δ.gcr = θ,
  κ.nm-applet = β,
  λ.programs = κ,
  μ.freedesktop = γ,
  ν.teams = μ
) =>
α ->
{ config = ι;
  meta = {maintainers = ζ;};
  options = {programs = {nm-applet = {enable = η;};};};
}