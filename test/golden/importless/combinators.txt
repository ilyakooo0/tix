∀ γ δ ε.
{ buildDunePackage =
    { meta = ∀ α β. ((α) // ({description = String;}) ~ (β), γ.meta = α) => β;
      pname = String;
      propagatedBuildInputs = [γ];
      src = ∀ α. (γ.src = α) => α;
      version = ∀ α. (γ.version = α) => α;
    } ->
    δ;
  cstruct = γ;
  fmt = ε;
  logs = γ;
  mirage-clock = γ;
  mirage-flow = γ;
  ocaml_lwt = γ;
} ->
δ