∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; security = { polkit = { enable = Bool } }; services = { dbus = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ] }; users = { users = { rtkit = { description = String; uid = ∀ α. α } } } }; options = { security = { rtkit = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }