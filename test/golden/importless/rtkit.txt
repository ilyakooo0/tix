∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.rtkit = ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      security = {polkit = {enable = Bool;};};
      services = {dbus = {packages = [θ];};};
      systemd = {packages = [ι];};
      users = {users = {rtkit = {description = String; uid = ζ;};};};
    } ->
    κ,
  γ.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  γ.types = ν,
  δ.rtkit = η,
  ξ.rtkit = β,
  ο.security = ξ,
  π.uids = β,
  ρ.ids = π,
  ν.bool = λ
) =>
α -> {config = κ; options = {security = {rtkit = {enable = μ;};};};}