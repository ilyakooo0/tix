∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { pathsToLink = [String]; systemPackages = ∀ γ. [γ] }; services = { dbus = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ]; services = { accounts-daemon = { environment = { NIXOS_USERS_PURE = String } } } } }; meta = { maintainers = ∀ δ. δ }; options = { services = { accounts-daemon = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }