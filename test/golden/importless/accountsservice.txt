∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.mutableUsers = Bool,
  γ.mkIf =
    ε ->
    { environment = {pathsToLink = [String]; systemPackages = [ζ];};
      services = {dbus = {packages = [η];};};
      systemd = {packages = [θ]; services = {accounts-daemon = ι;};};
    } ->
    κ,
  γ.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  γ.optionalAttrs = Bool -> {environment = {NIXOS_USERS_PURE = String;};} -> ν,
  γ.recursiveUpdate =
    {environment = {XDG_DATA_DIRS = String;}; wantedBy = [String];} -> ν -> ι,
  γ.teams = ξ,
  γ.types = ο,
  δ.accountsservice = ζ,
  π.accounts-daemon = β,
  ρ.services = π,
  σ.users = β,
  ξ.members = τ,
  υ.freedesktop = ξ,
  ο.bool = λ
) =>
α ->
{ config = κ;
  meta = {maintainers = τ;};
  options = {services = {accounts-daemon = {enable = μ;};};};
}