∀ δ ε. { makeInstalledTest = { testConfig = { environment = { systemPackages = ∀ α. [α] }; services = { flatpak = { enable = Bool } }; virtualisation = { diskSize = Integer }; xdg = { portal = { enable = Bool } } }; testRunnerFlags = String; tested = ∀ β. β } -> δ; pkgs = ε } -> { testConfig = { environment = { systemPackages = ∀ β. [β] }; services = { flatpak = { enable = Bool } }; virtualisation = { diskSize = Integer }; xdg = { portal = { enable = Bool } } }; testRunnerFlags = String; tested = ∀ γ. γ }