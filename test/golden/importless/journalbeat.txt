∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { assertions = [{ assertion = Bool; message = String }]; systemd = { services = { journalbeat = { description = String; preStart = String; serviceConfig = { ExecStart = String; Restart = String; StateDirectory = ∀ α. α }; wantedBy = [String] } } } }; options = { services = { journalbeat = { enable = String; extraConfig = { default = String; description = String; type = ∀ β. β }; name = { default = String; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; example = String; type = ∀ β. β }; stateDir = { default = String; description = String; type = ∀ β. β }; tags = { default = ∀ β. β; description = String; type = ∀ β. β } } } } }