∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; services = { xserver = { displayManager = { job = { execCmd = String }; lightdm = { enable = Bool } }; exportConfiguration = Bool } }; systemd = { services = { display-manager = { enable = Bool } } } }; options = { services = { xserver = { displayManager = { startx = { enable = { default = Bool; description = String; type = ∀ α. α } } } } } } }