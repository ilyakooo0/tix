∀ α β γ δ ε ζ η θ ι.
( ( α
  ) //
  ( { meta = {homepage = String; license = β;};
      minimumOCamlVersion = String;
      src = γ;
      version = String;
    }
  ) ~
  ( δ
  ),
  ε.mit = β,
  α.hash = ζ,
  α.pname = η,
  α.version = String,
  θ.licenses = ε
) =>
{ buildDunePackage = δ -> ι;
  defaultVersion = String;
  fetchFromGitHub = {owner = String; repo = η; rev = String; sha256 = ζ;} -> γ;
  lib = ε;
} ->
α -> ι