∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.updateMicrocode = ε,
  γ.mkIf = ε -> {boot = {initrd = {prepend = ζ;};};} -> η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.mkOrder = Integer -> [String] -> ζ,
  γ.types = κ,
  λ.amd = β,
  μ.cpu = λ,
  ν.hardware = μ,
  κ.bool = θ
) =>
α ->
{config = η; options = {hardware = {cpu = {amd = {updateMicrocode = ι;};};};};}