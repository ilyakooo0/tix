∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { boot = { initrd = { prepend = [String] } } }; options = { hardware = { cpu = { amd = { updateMicrocode = { default = Bool; description = String; type = ∀ α. α } } } } } }