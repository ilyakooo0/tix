∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.ati_drivers_x11 = ε,
  β.videoDrivers = ζ,
  γ.elem = String -> ζ -> η,
  γ.mkIf =
    η ->
    { boot = {blacklistedKernelModules = [String]; extraModulePackages = [ε];};
      environment = {etc = {ati = {source = String;};}; systemPackages = [ε];};
      hardware =
        {opengl = {package = ε; package32 = θ; setLdLibraryPath = Bool;};};
      nixpkgs = {config = {xorg = {abiCompat = String;};};};
      services = {xserver = {drivers = ι;};};
    } ->
    κ,
  γ.singleton = {display = Bool; modules = [ε]; name = String;} -> ι,
  δ.override = {kernel = Null; libsOnly = Bool;} -> θ,
  λ.xserver = β,
  μ.services = λ,
  ν.kernelPackages = β,
  ξ.boot = ν,
  ο.ati_drivers_x11 = δ,
  π.linuxPackages = ο,
  ρ.pkgsi686Linux = π
) =>
α -> {config = κ;}