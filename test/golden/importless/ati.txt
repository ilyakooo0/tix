∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { boot = { blacklistedKernelModules = [String]; extraModulePackages = [∀ β. β] }; environment = { etc = { ati = { source = String } }; systemPackages = [∀ β. β] }; hardware = { opengl = { package = ∀ β. β; package32 = { kernel = Null; libsOnly = Bool }; setLdLibraryPath = Bool } }; nixpkgs = { config = { xorg = { abiCompat = String } } }; services = { xserver = { drivers = { display = Bool; modules = [∀ α. α]; name = String } } } } }