∀ γ δ ε ζ η θ ι.
( γ.concatMapStringsSep = String -> (δ -> String) -> ε -> ζ,
  η.lib = γ
) =>
{file = θ; stdenv = γ; writeScript = String -> String -> ι;} ->
{ checkForRemainingFiles = ι;
  commonFileActions = ∀ α β. {docFiles = α; noiseFiles = β;} -> ι;
}