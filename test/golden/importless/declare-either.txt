∀ θ.
{lib = θ;} ->
{ options =
    { value =
        ∀ η.
        ( θ.mkOption =
            { type =
                ∀ α β γ δ ε ζ.
                ( θ.types = α,
                  α.either = β -> γ -> δ,
                  θ.types = ε,
                  ε.int = β,
                  θ.types = ζ,
                  ζ.str = γ
                ) =>
                δ;
            } ->
            η
        ) =>
        η;
    };
}