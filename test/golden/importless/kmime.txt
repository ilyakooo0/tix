∀ α β γ δ ε ζ η θ ι.
( α.lgpl21 = β,
  α.versionOlder = γ -> String -> δ,
  ε.version = γ,
  ζ.licenses = α
) =>
{ extra-cmake-modules = η;
  kcodecs = ε;
  kdepimTeam = θ;
  ki18n = ε;
  lib = α;
  mkDerivation =
    { buildInputs = [ε];
      meta = {broken = δ; license = [β]; maintainers = θ;};
      name = String;
      nativeBuildInputs = [η];
      outputs = [String];
    } ->
    ι;
  qtbase = ε;
} ->
ι