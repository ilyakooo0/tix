∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; services = { dbus = { packages = ∀ β. [β] }; udev = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ]; tmpfiles = { rules = [String] } } }; options = { services = { udisks2 = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }