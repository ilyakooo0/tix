∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];}; udev = {packages = [θ];};};
      systemd = {packages = [ι]; tmpfiles = {rules = [String];};};
    } ->
    κ,
  γ.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  γ.types = ν,
  δ.udisks2 = ζ,
  ξ.udisks2 = β,
  ο.services = ξ,
  ν.bool = λ
) =>
α -> {config = κ; options = {services = {udisks2 = {enable = μ;};};};}