∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; services = { xserver = { displayManager = { sessionCommands = String } } } }; options = { services = { fractalart = { enable = { default = Bool; description = String; example = Bool; type = ∀ α. α }; height = { default = Null; description = String; example = Integer; type = ∀ α. α }; width = { default = Null; description = String; example = Integer; type = ∀ α. α } } } } }