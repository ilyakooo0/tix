∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { polkit-1/rules.d/10-nixos.rules = { text = String } }; pathsToLink = [String]; systemPackages = ∀ γ. [γ] }; security = { pam = { services = { polkit-1 = {  } } }; wrappers = { pkexec = { source = String }; polkit-agent-helper-1 = { source = String } } }; services = { dbus = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ]; services = { polkit = { restartTriggers = ∀ α. [α]; stopIfChanged = Bool } }; tmpfiles = { rules = [String] } }; users = { users = { polkituser = { description = String; uid = ∀ α. α } } } }; options = { security = { polkit = { adminIdentities = { default = [String]; description = String; example = [String]; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; extraConfig = { default = String; description = String; example = String; type = ∀ α. α } } } } }