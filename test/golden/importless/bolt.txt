∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {udev = {packages = [η];};};
      systemd = {packages = [θ];};
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  δ.bolt = ζ,
  ν.bolt = β,
  ξ.hardware = ν,
  ο.services = ξ,
  μ.bool = κ
) =>
α ->
{config = ι; options = {services = {hardware = {bolt = {enable = λ;};};};};}