∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; services = { udev = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ] } }; options = { services = { hardware = { bolt = { enable = { default = Bool; description = String; type = ∀ α. α } } } } } }