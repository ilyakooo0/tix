∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { assertions = [{ assertion = Bool; message = String }]; environment = { etc = { sway/config = { source = String } }; systemPackages = [{ extraOptions = ∀ β. β; extraSessionCommands = ∀ β. β; withBaseWrapper = ∀ β. β; withGtkWrapper = ∀ β. β }] }; fonts = { enableDefaultFonts = Bool }; hardware = { opengl = { enable = Bool } }; programs = { dconf = { enable = Bool } }; security = { pam = { services = { swaylock = {  } } } }; services = { xserver = { displayManager = { sessionPackages = [{ extraOptions = ∀ α. α; extraSessionCommands = ∀ α. α; withBaseWrapper = ∀ α. α; withGtkWrapper = ∀ α. α }] } } } }; meta = { maintainers = ∀ γ. [γ] }; options = { programs = { sway = { enable = String; extraOptions = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; extraPackages = { default = ∀ β. [β]; defaultText = String; description = String; example = String; type = ∀ β. β }; extraSessionCommands = { default = String; description = String; example = String; type = ∀ β. β }; wrapperFeatures = { default = {  }; description = String; example = { gtk = Bool }; type = { options = { base = String; gtk = String } } } } } } }