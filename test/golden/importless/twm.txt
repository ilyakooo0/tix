∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.twm = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {xserver = {windowManager = {session = η;};};};
    } ->
    θ,
  γ.singleton = {name = String; start = String;} -> η,
  ι.twm = ζ,
  β.enable = ε,
  κ.windowManager = α,
  λ.xserver = κ,
  μ.services = λ,
  ν.xorg = ι
) =>
{config = α; lib = γ; pkgs = ι;} ->
{ config = θ;
  options =
    {services = {xserver = {windowManager = {twm = {enable = δ;};};};};};
}