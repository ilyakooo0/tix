∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { networking = { firewall = { allowedTCPPorts = [Integer]; allowedUDPPorts = [Integer] } }; systemd = { services = { serviio = { after = [String]; description = String; path = ∀ α. [α]; serviceConfig = { ExecStart = String; ExecStop = String; Group = String; User = String }; wantedBy = [String] } } }; users = { groups = { serviio = {  } }; users = { serviio = { createHome = Bool; description = String; group = String; home = ∀ α. α; isSystemUser = Bool } } } }; options = { services = { serviio = { dataDir = { default = String; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α } } } } }