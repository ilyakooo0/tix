∀ δ ε ζ η.
( δ.mkDerivation =
    { dontUnpack = Bool;
      installPhase = String;
      meta =
        {platforms = ∀ α β γ. (δ.lib = α, α.platforms = β, β.unix = γ) => γ;};
      name = String;
      nativeBuildInputs = [ε];
    } ->
    ζ
) =>
{fetchurl = {sha256 = String; url = String;} -> η; stdenv = δ; unzip = ε;} -> ζ