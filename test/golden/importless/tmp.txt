∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  β.cleanTmpDir = δ,
  β.tmpOnTmpfs = ε,
  γ.mkOption = {default = Bool; description = String; type = ζ;} -> η,
  γ.optional = ε -> String -> θ,
  γ.types = ι,
  κ.boot = β,
  λ.boot = β,
  ι.bool = ζ,
  μ.bool = ν
) =>
α ->
{ config =
    {systemd = {additionalUpstreamSystemUnits = θ; tmpfiles = {rules = ξ;};};};
  options = {boot = {cleanTmpDir = η; tmpOnTmpfs = ο;};};
}