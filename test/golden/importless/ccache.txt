∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = [{ security = { wrappers = { nix-ccache = { group = String; setgid = Bool; source = String } } }; systemd = { tmpfiles = { rules = [String] } } }]; options = { programs = { ccache = { cacheDir = { default = String; description = String; type = ∀ α. α }; enable = String; packageNames = { default = ∀ α. α; description = String; example = [String]; type = ∀ α. α } } } } }