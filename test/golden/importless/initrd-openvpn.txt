∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { assertions = [{ assertion = Bool; message = String }]; boot = { initrd = { extraUtilsCommands = String; extraUtilsCommandsTest = String; kernelModules = [String]; network = { postCommands = String }; secrets = { /etc/initrd.ovpn = ∀ β. β } } } }; options = { boot = { initrd = { network = { openvpn = { configuration = { description = String; example = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α } } } } } } }