∀ β γ δ ε. { config = β; lib = γ; options = δ; pkgs = ε } -> { extraOpts = { configFile = { description = String; type = ∀ α. α }; listenAddress = {  }; url = { description = String; type = ∀ α. α } }; port = Integer; serviceOpts = { serviceConfig = { ExecStart = String } } }