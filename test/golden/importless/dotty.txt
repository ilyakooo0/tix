∀ β γ δ ε ζ η.
( β.mkDerivation =
    { dontUnpack = Bool;
      installPhase = String;
      meta = ∀ α. (γ.meta = α) => α;
      name = String;
    } ->
    δ
) =>
{ callPackage =
    Path -> {fetchurl = ε; jre = ζ; makeWrapper = η; stdenv = β;} -> γ;
  fetchurl = ε;
  jre = ζ;
  makeWrapper = η;
  stdenv = β;
} ->
δ