∀ α β γ δ ε ζ η θ ι.
( α.config = β,
  α.lib = γ,
  β.enable = δ,
  γ.mkIf = δ -> {environment = {pathsToLink = [String];};} -> ε,
  γ.mkOption = {default = Bool; description = String; type = ζ;} -> η,
  γ.types = θ,
  ι.appstream = β,
  θ.bool = ζ
) =>
α -> {config = ε; options = {appstream = {enable = η;};};}