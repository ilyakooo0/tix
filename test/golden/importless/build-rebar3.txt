∀ γ δ ε ζ η θ ι κ λ. { erlang = γ; lib = δ; libyaml = γ; openssl = γ; pc = ε; rebar3 = γ; stdenv = ζ; writeText = String -> η } -> { beamDeps = [θ]; buildInputs = [γ]; buildPhase = Null; buildPlugins = [ι]; compilePorts = Bool; configurePhase = Null; enableDebugInfo = Bool; installPhase = Null; meta = {  }; name = String; postPatch = String; setupHook = Null; src = κ; version = λ } -> ∀ β. β -> { beamDeps = [θ]; buildInputs = [γ]; buildPhase = String; buildPlugins = ∀ α. α; compilePorts = Bool; configurePhase = String; dontStrip = Bool; enableDebugInfo = Bool; installPhase = String; meta = {  }; name = String; passthru = { beamDeps = [θ]; env = β; packageName = String }; postPatch = String; propagatedBuildInputs = [θ]; setupHook = String; src = κ; version = λ }