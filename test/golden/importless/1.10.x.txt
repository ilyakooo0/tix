∀ β γ δ ε. { boost = β; cmake = β; fetchurl = { sha256 = String; url = String } -> γ; freeglut = β; freeimage = β; freetype = β; lib = δ; libGL = β; libGLU = β; libICE = β; libSM = β; libX11 = β; libXaw = β; libXmu = β; libXrandr = β; libXrender = β; libXt = β; libXxf86vm = β; libpng = β; nvidia_cg_toolkit = [β]; ois = β; pkgconfig = β; stdenv = ε; withNvidiaCg = Bool; withSamples = Bool; xorgproto = β; zziplib = β } -> { buildInputs = [β]; cmakeFlags = [String]; enableParallelBuilding = Bool; meta = { description = String; homepage = String; license = ∀ α. α; maintainers = ∀ α. [α]; platforms = ∀ α. α }; name = String; src = { sha256 = String; url = String } }