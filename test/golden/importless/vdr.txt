∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = [{ systemd = { services = { vdr = { description = String; serviceConfig = { CacheDirectory = String; ExecStart = String; Restart = String; StateDirectory = String; User = String }; wantedBy = [String] } }; tmpfiles = { rules = [String] } }; users = { groups = { vdr = {  } }; users = { vdr = { group = String; home = String; isSystemUser = Bool } } } }]; options = { services = { vdr = { enable = String; enableLirc = String; extraArguments = { default = ∀ α. α; description = String; type = ∀ α. α }; package = { default = ∀ α. α; defaultText = String; description = String; example = String; type = ∀ α. α }; videoDir = { default = String; description = String; type = ∀ α. α } } } } }