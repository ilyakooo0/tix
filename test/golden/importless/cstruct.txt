∀ γ δ ε.
{ buildDunePackage =
    { doCheck = Bool;
      meta = ∀ α β. ((α) // ({description = String;}) ~ (β), γ.meta = α) => β;
      minimumOCamlVersion = ∀ α. (γ.minimumOCamlVersion = α) => α;
      pname = String;
      propagatedBuildInputs = [γ];
      src = ∀ α. (γ.src = α) => α;
      version = ∀ α. (γ.version = α) => α;
    } ->
    δ;
  cstruct = γ;
  lib = ε;
  macaddr = γ;
} ->
δ