∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.maintainers = ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      services = {udev = {packages = [θ];};};
      users = {groups = {camera = {};};};
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  δ.gphoto2 = η,
  δ.libgphoto2 = θ,
  ν.gphoto2 = β,
  ξ.programs = ν,
  ζ.league = ο,
  μ.bool = κ
) =>
α ->
{ config = ι;
  meta = {maintainers = [ο];};
  options = {programs = {gphoto2 = {enable = λ;};};};
}