∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.services = ε,
  γ.builtins = ζ,
  γ.cfg = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { systemd =
        { services =
            { prometheus-xmpp-alerts =
                { after = [String];
                  serviceConfig =
                    { DynamicUser = Bool;
                      ExecStart = String;
                      NoNewPrivileges = Bool;
                      PrivateDevices = Bool;
                      PrivateTmp = Bool;
                      ProtectControlGroups = Bool;
                      ProtectHome = Bool;
                      ProtectKernelModules = Bool;
                      ProtectKernelTunables = Bool;
                      ProtectSystem = String;
                      Restart = String;
                      RestrictAddressFamilies = [String];
                      SystemCallArchitectures = String;
                      SystemCallFilter = [String];
                    };
                  wantedBy = [String];
                  wants = [String];
                };
            };
        };
    } ->
    κ,
  γ.mkOption = {description = String; type = λ;} -> μ,
  γ.types = ν,
  δ.writeText = String -> ξ -> ο,
  ζ.toJSON = π -> ξ,
  η.configuration = π,
  ε.prometheus = ρ,
  ρ.xmpp-alerts = σ,
  σ.enable = ι,
  ν.attrs = λ
) =>
α ->
{ config = κ;
  options =
    { services =
        {prometheus = {xmpp-alerts = {configuration = μ; enable = θ;};};};
    };
}