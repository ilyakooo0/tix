∀ α β γ δ ε ζ η θ ι.
( α.config = β,
  α.lib = γ,
  β.enableDebugInfo = δ,
  γ.mkIf =
    δ ->
    { environment =
        { extraOutputsToInstall = [String];
          variables = {NIX_DEBUG_INFO_DIRS = [String];};
        };
    } ->
    ε,
  γ.mkOption = {default = Bool; description = String; type = ζ;} -> η,
  γ.types = θ,
  ι.environment = β,
  θ.bool = ζ
) =>
α -> {config = ε; options = {environment = {enableDebugInfo = η;};};}