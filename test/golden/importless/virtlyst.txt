∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { virtlyst = { environment = { VIRTLYST_ADMIN_PASSWORD = ∀ α. α }; serviceConfig = { ExecStart = String; User = String; WorkingDirectory = String }; wantedBy = [String] } } }; users = { users = { virtlyst = { createHome = Bool; group = String; home = String; isSystemUser = Bool } } } }; options = { services = { virtlyst = { adminPassword = { description = String; type = ∀ β. β }; enable = String; httpSocket = { default = String; description = String; type = ∀ β. β } } } } }