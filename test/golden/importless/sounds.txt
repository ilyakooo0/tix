∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε -> {environment = {pathsToLink = [String]; systemPackages = [ζ];};} -> η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.teams = κ,
  γ.types = λ,
  δ.sound-theme-freedesktop = ζ,
  μ.sounds = β,
  ν.xdg = μ,
  κ.members = ξ,
  ο.freedesktop = κ,
  λ.bool = θ
) =>
α ->
{ config = η;
  meta = {maintainers = ξ;};
  options = {xdg = {sounds = {enable = ι;};};};
}