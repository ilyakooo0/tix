∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.epmd = β,
  γ.mkIf =
    δ ->
    { systemd =
        { services =
            { epmd =
                { after = [String];
                  description = String;
                  requires = [String];
                  serviceConfig =
                    {DynamicUser = Bool; ExecStart = String; Type = String;};
                };
            };
          sockets =
            { epmd =
                { before = [String];
                  description = String;
                  socketConfig = {Accept = String; ListenStream = String;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    ε,
  γ.mkOption = {default = Bool; description = String; type = ζ;} -> η,
  γ.types = θ,
  ι.erlang = κ,
  β.enable = δ,
  λ.services = α,
  θ.bool = ζ,
  μ.package = ν
) =>
{config = α; lib = γ; pkgs = ι;} ->
{config = ε; options = {services = {epmd = {enable = η; package = ξ;};};};}