∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.enable = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    β ->
    { environment = {systemPackages = [ε];};
      services = {dbus = {packages = [ζ];};};
      systemd = {packages = [η];};
    } ->
    θ,
  ι.maintainers = κ,
  ι.sysprof = ε,
  λ.sysprof = α,
  μ.services = λ,
  ν.meta = ι,
  ξ.sysprof = ν
) =>
{config = α; lib = γ; pkgs = ι;} ->
{ config = θ;
  meta = {maintainers = κ;};
  options = {services = {sysprof = {enable = δ;};};};
}