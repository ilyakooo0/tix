∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ β. [β] }; services = { dbus = { packages = ∀ α. [α] } }; systemd = { packages = ∀ β. [β] } }; meta = { maintainers = ∀ γ. γ }; options = { services = { sysprof = { enable = String } } } }