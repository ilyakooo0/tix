∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { systemPackages = ∀ γ. [γ]; variables = { RXVT_SOCKET = String } }; systemd = { user = { services = { urxvtd = { description = String; partOf = [String]; path = ∀ α. [α]; serviceConfig = { Environment = String; ExecStart = String; Restart = String; RestartSec = String }; wantedBy = [String] } } } } }; meta = { maintainers = ∀ δ. [δ] }; options = { services = { urxvtd = { enable = { default = Bool; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β } } } } }