∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { peerflix = { after = [String]; description = String; environment = { HOME = ∀ α. α }; preStart = String; serviceConfig = { ExecStart = String; User = String }; wantedBy = [String] } }; tmpfiles = { rules = [String] } }; users = { users = { peerflix = { uid = ∀ β. β } } } }; options = { services = { peerflix = { downloadDir = { default = String; description = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; stateDir = { default = String; description = String; type = ∀ β. β } } } } }