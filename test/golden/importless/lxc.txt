∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { lxc/default.conf = { text = ∀ α. α }; lxc/lxc-usernet = { text = ∀ α. α }; lxc/lxc.conf = { text = ∀ α. α } }; systemPackages = ∀ γ. [γ] }; security = { apparmor = { packages = ∀ β. [β]; profiles = [String] } }; systemd = { tmpfiles = { rules = [String] } } }; options = { virtualisation = { lxc = { defaultConfig = { default = String; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; systemConfig = { default = String; description = String; type = ∀ α. α }; usernetConfig = { default = String; description = String; type = ∀ α. α } } } } }