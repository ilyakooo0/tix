∀ δ ε ζ.
( δ.mkDerivation =
    { dontBuild = Bool;
      installPhase = String;
      meta =
        { description = String;
          homepage = ∀ α β. (ε.meta = α, α.homepage = β) => β;
          license = ∀ α β. (ε.meta = α, α.license = β) => β;
          maintainers = ∀ α β. (ε.meta = α, α.maintainers = β) => β;
        };
      name = String;
      patchPhase = String;
      src = ∀ γ. (ε.src = γ) => γ;
    } ->
    ζ
) =>
{group = String; nitrokey-app = ε; stdenv = δ;} -> ζ