∀ η.
{system = η;} ->
{ baseConfig =
    ∀ β γ δ ε ζ.
    ( β.jq = γ,
      β.makeWrapper = δ,
      β.mkDerivation =
        { buildInputs = [δ];
          dontUnpack = Bool;
          installPhase = String;
          name = String;
          postFixup = String;
        } ->
        γ,
      β.writeTextDir = String -> String -> ε,
      ζ.stdenv = β
    ) =>
    {pkgs = β;} ->
    { environment = {systemPackages = [γ];};
      nix =
        { binaryCaches = ∀ α. α;
          buildMachines = [{hostName = String; systems = [η];}];
          distributedBuilds = Bool;
        };
      services =
        { hydra =
            { enable = Bool;
              extraConfig = String;
              hydraURL = String;
              notificationSender = String;
            };
          postfix = {enable = Bool;};
        };
      time = {timeZone = String;};
      virtualisation = {memorySize = Integer;};
    };
}