∀ β γ δ ε ζ η θ.
( β.all = γ,
  β.mkDerivation =
    { buildInputs = [δ];
      builder = ε;
      convert = Path;
      meta =
        { description = String;
          homepage = String;
          longDescription = String;
          maintainers = ∀ α. α;
          platforms = γ;
        };
      pname = String;
      version = String;
    } ->
    ζ,
  η.platforms = β,
  θ.lib = η
) =>
{python = δ; stdenv = β; wordnet = δ; writeScript = String -> String -> ε;} -> ζ