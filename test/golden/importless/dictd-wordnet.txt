∀ δ ε ζ η.
( δ.mkDerivation =
    { buildInputs = [ε];
      builder = ζ;
      convert = Path;
      meta =
        { description = String;
          homepage = String;
          longDescription = String;
          maintainers = ∀ α. α;
          platforms = ∀ α β γ. (δ.lib = α, α.platforms = β, β.all = γ) => γ;
        };
      pname = String;
      version = String;
    } ->
    η
) =>
{python = ε; stdenv = δ; wordnet = ε; writeScript = String -> String -> ζ;} -> η