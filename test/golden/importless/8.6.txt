∀ α β γ δ ε ζ η θ.
( (α) // ({patches = [Path]; src = β;}) ~ (γ),
  α.callPackage = Path -> γ -> δ,
  α.fetchurl = {sha256 = String; url = String;} -> β,
  α.stdenv = ε,
  α.tcl = ζ,
  ε.isDarwin = η,
  ε.lib = θ,
  θ.optionals = η -> [Path] -> [Path]
) =>
α -> δ