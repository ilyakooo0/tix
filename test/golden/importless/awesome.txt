∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = [∀ β. β] }; services = { xserver = { windowManager = { session = { name = String; start = String } } } } }; options = { services = { xserver = { windowManager = { awesome = { enable = String; luaModules = { default = ∀ α. α; description = String; example = String; type = ∀ α. α }; noArgb = { default = Bool; description = String; type = ∀ α. α }; package = { apply = Null -> Null; default = Null; description = String; type = ∀ α. α } } } } } } }