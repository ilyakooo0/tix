∀ α β γ δ ε ζ η θ ι κ λ.
( α.xow = β,
  γ.xow = δ,
  ε.mkEnableOption = String -> ζ,
  ε.mkIf =
    η ->
    { hardware = {uinput = {enable = Bool;};};
      services = {udev = {packages = [δ];};};
      systemd = {packages = [θ];};
    } ->
    ι,
  β.enable = η,
  κ.hardware = α,
  λ.services = κ
) =>
{config = α; lib = ε; pkgs = γ;} ->
{config = ι; options = {services = {hardware = {xow = {enable = ζ;};};};};}