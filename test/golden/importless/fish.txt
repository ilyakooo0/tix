∀ ζ η θ. { config = ζ; lib = η; pkgs = θ } -> { config = { documentation = { man = { generateCaches = Bool } }; environment = { etc = { fish/config.fish = { text = String }; fish/foreign-env/interactiveShellInit = { text = ∀ γ. γ }; fish/foreign-env/loginShellInit = { text = ∀ γ. γ }; fish/foreign-env/shellInit = { text = ∀ γ. γ }; fish/generated_completions = { source = ∀ γ. { ignoreCollisions = Bool; name = String; paths = { meta = { priority = ∀ α. α } } -> γ } }; fish/nixos-env-preinit.fish = { text = String } }; pathsToLink = ∀ ε. [ε]; shells = [String]; systemPackages = ∀ ε. [ε] }; programs = { fish = { interactiveShellInit = String; shellAliases = ∀ δ. δ } } }; options = { programs = { fish = { enable = { default = Bool; description = String; type = ∀ γ. γ }; interactiveShellInit = { default = String; description = String; type = ∀ γ. γ }; loginShellInit = { default = String; description = String; type = ∀ γ. γ }; promptInit = { default = String; description = String; type = ∀ γ. γ }; shellAliases = { default = {  }; description = String; type = ∀ γ. γ }; shellInit = { default = String; description = String; type = ∀ γ. γ }; vendor = { completions = { enable = { default = Bool; description = String; type = ∀ β. β } }; config = { enable = { default = Bool; description = String; type = ∀ β. β } }; functions = { enable = { default = Bool; description = String; type = ∀ β. β } } } } } } }