∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( (β) // ({description = String;}) ~ (γ),
  δ.config = ε,
  δ.lib = ζ,
  δ.pkgs = η,
  ε.enable = Bool,
  ζ.mkEnableOption = String -> β,
  ζ.mkIf =
    Bool ->
    { boot = {kernelModules = [String];};
      hardware = {sane = {extraBackends = [θ];};};
      services = {udev = {packages = [ι];};};
    } ->
    κ,
  η.dsseries = θ,
  λ.sane = ε,
  μ.hardware = λ,
  ν.dsseries = ε,
  ξ.sane = ν,
  ο.hardware = ξ
) =>
δ ->
{ config = κ;
  options = {hardware = {sane = {dsseries = {enable = ∀ α. α;};};};};
}