∀ β γ δ ε ζ η θ ι κ λ μ ν ξ.
( (β) // ({description = String;}) ~ (γ),
  δ.enable = Bool,
  ε.mkEnableOption = String -> β,
  ε.mkIf =
    Bool ->
    { boot = {kernelModules = [String];};
      hardware = {sane = {extraBackends = [ζ];};};
      services = {udev = {packages = [η];};};
    } ->
    θ,
  ι.dsseries = ζ,
  κ.sane = δ,
  λ.hardware = κ,
  μ.dsseries = δ,
  ν.sane = μ,
  ξ.hardware = ν
) =>
{config = δ; lib = ε; pkgs = ι;} ->
{ config = θ;
  options = {hardware = {sane = {dsseries = {enable = ∀ α. α;};};};};
}