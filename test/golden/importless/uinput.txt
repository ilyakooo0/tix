∀ α β γ δ ε ζ η θ.
( α.uinput = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { boot = {kernelModules = [String];};
      services = {udev = {extraRules = String;};};
      users = {groups = {uinput = {};};};
    } ->
    ζ,
  β.enable = ε,
  η.hardware = α
) =>
{config = α; lib = γ; pkgs = θ;} ->
{config = ζ; options = {hardware = {uinput = {enable = δ;};};};}