∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { boot = { loader = { grub = { enable = Bool; version = Integer } } }; environment = { systemPackages = ∀ α. [α] }; programs = { ssh = { startAgent = Bool } }; services = { lshd = { enable = Bool }; openssh = { enable = Bool }; xserver = { startGnuPGAgent = Bool } } }; options = { gnu = { default = Bool; description = String; type = ∀ α. α } } }