∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = [{ environment = { systemPackages = ∀ β. [β] }; services = { udev = { packages = ∀ α. [α] } }; systemd = { packages = ∀ β. [β]; tmpfiles = { packages = ∀ α. [α] } } }]; options = { services = { lvm = { boot = { thin = { enable = String } }; dmeventd = { enable = String }; package = { default = ∀ α. α; defaultText = String; description = String; internal = Bool; type = ∀ α. α } } } } }