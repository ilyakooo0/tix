∀ α β γ δ ε ζ η θ ι κ λ μ.
( ( α
  ) //
  ( { GIT_SSL_CAINFO = String;
      LANG = String;
      LD_LIBRARY_PATH = β;
      STACK_IN_NIX_EXTRA_ARGS = γ;
      STACK_IN_NIX_SHELL = Integer;
      STACK_PLATFORM_VARIANT = String;
      __noChroot = Bool;
      buildInputs = [δ];
      buildPhase = String;
      checkPhase = String;
      doCheck = Bool;
      installPhase = String;
      nativeBuildInputs = [ε];
      preConfigure = String;
      preferLocalBuild = Bool;
    }
  ) ~
  ( ζ
  ),
  η.libc = String,
  η.mkDerivation = ζ -> θ,
  ι.makeLibraryPath = [δ] -> β,
  ι.optional = Bool -> κ -> [δ],
  α.LD_LIBRARY_PATH = [δ],
  α.buildInputs = [δ],
  α.buildPhase = String,
  α.checkPhase = String,
  α.doCheck = Bool,
  α.extraArgs = γ,
  α.ghc = ε,
  α.installPhase = String,
  α.nativeBuildInputs = [ε],
  α.stack = ε,
  λ.hostPlatform = η
) =>
{ cacert = μ;
  ghc = ε;
  glibcLocales = κ;
  lib = ι;
  makeSetupHook = {} -> Path -> ε;
  pkgconfig = ε;
  stack = ε;
  stdenv = η;
} ->
α -> θ