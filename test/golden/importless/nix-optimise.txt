∀ β γ. { config = β; lib = γ } -> { config = { systemd = { services = { nix-optimise = { description = String; serviceConfig = { ExecStart = String }; startAt = ∀ α. α; unitConfig = { ConditionPathIsReadWrite = String } } } } }; options = { nix = { optimise = { automatic = { default = Bool; description = String; type = ∀ α. α }; dates = { default = [String]; description = String; type = ∀ α. α } } } } }