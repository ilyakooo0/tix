∀ β γ.
( β.substituteAll =
    { bash = ∀ α. (β.bash = α) => α;
      isExecutable = Bool;
      path = ∀ α. (β.coreutils = α, β.gnused = α, β.gnugrep = α) => [α];
      src = Path;
    } ->
    γ
) =>
{pkgs = β;} -> γ