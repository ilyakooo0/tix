∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { boot = { kernelParams = [String] }; environment = { etc = { apparmor/parser.conf = { text = ∀ β. β } }; systemPackages = ∀ γ. [γ] }; systemd = { services = { apparmor = { after = [String]; before = [String]; serviceConfig = { ExecReload = ∀ α. α; ExecStart = ∀ α. α; ExecStop = ∀ α. α; RemainAfterExit = String; Type = String }; unitConfig = { DefaultDependencies = String }; wantedBy = [String] } } } }; options = { security = { apparmor = { enable = { default = Bool; description = String; type = ∀ β. β }; packages = { default = ∀ β. β; description = String; type = ∀ β. β }; parserConfig = { default = String; description = String; type = ∀ β. β }; profiles = { default = ∀ β. β; description = String; type = ∀ β. β } } } } }