∀ α β γ δ.
( α.overrideAttrs =
    (β -> {cmakeFlags = (β.cmakeFlags = [String]) => [String]; src = γ;}) -> δ
) =>
{ fetchFromGitHub =
    {owner = String; repo = String; rev = String; sha256 = String;} -> γ;
  libgit2 = α;
} ->
δ