∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; systemd = { user = { services = { greenclip = { after = [String]; description = String; enable = Bool; serviceConfig = { ExecStart = String }; wantedBy = [String] } } } } }; options = { services = { greenclip = { enable = String; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }