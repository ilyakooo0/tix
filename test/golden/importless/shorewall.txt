∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = ∀ γ. γ; systemPackages = ∀ γ. [γ] }; systemd = { services = { firewall = { enable = Bool }; shorewall = { after = [String]; before = [String]; description = String; preStart = String; reloadIfChanged = Bool; restartTriggers = ∀ α. α; serviceConfig = { ExecReload = String; ExecStart = String; ExecStop = String; RemainAfterExit = String; Type = String }; wantedBy = [String]; wants = [String] } } } }; options = { services = { shorewall = { configs = { apply = ∀ α β. α -> β -> β; default = {  }; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }