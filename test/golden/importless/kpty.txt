∀ α β γ δ ε ζ.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  kcoreaddons = ε;
  ki18n = ε;
  lib = α;
  mkDerivation =
    { buildInputs = [ε];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
      outputs = [String];
    } ->
    ζ;
  qtbase = ε;
} ->
ζ