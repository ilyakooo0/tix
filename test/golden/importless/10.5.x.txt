∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ.
( α.hostPlatform = β,
  α.isDarwin = Bool,
  α.isMusl = γ,
  α.lib = δ,
  α.mkDerivation =
    { buildInputs = [ε];
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = ζ;
          maintainers = [η];
        };
      passthru = {tests = {smoke-test = θ;};};
      pname = String;
      preferLocalBuild = Bool;
      propagatedBuildInputs = [ι];
      src = κ;
      version = String;
    } ->
    λ,
  μ.optionalString = γ -> String -> ν,
  μ.warn = String -> String -> String,
  ξ.aspnetcore_3_1 = ι,
  ο.jellyfin = θ,
  β.isAarch32 = Bool,
  β.isAarch64 = Bool,
  β.isx86_32 = Bool,
  β.isx86_64 = Bool,
  π.hostPlatform = α,
  δ.licenses = ρ,
  δ.maintainers = σ,
  δ.minijackson = η,
  δ.nyanloutre = η,
  ρ.gpl2 = ζ
) =>
{ dotnetCorePackages = ξ;
  fetchurl = {sha256 = String; url = String;} -> κ;
  ffmpeg = τ;
  fontconfig = υ;
  freetype = φ;
  lib = μ;
  makeWrapper = ε;
  nixosTests = ο;
  sqlite = ι;
  stdenv = α;
  unzip = ε;
} ->
λ