∀ α β γ δ ε ζ η θ ι.
( α.lgpl21 = β,
  α.ttuegel = γ,
  δ.licenses = α,
  ε.maintainers = α
) =>
{ extra-cmake-modules = ζ;
  fetchpatch = η;
  karchive = θ;
  kio = θ;
  lib = α;
  libkdcraw = θ;
  libkexiv2 = θ;
  mkDerivation =
    { buildInputs = [θ];
      meta = {license = [β]; maintainers = [γ];};
      name = String;
      nativeBuildInputs = [ζ];
    } ->
    ι;
} ->
ι