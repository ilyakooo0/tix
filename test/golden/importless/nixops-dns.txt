∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { services = { dnsmasq = { enable = Bool; extraConfig = String; resolveLocalQueries = Bool; servers = [String] } }; systemd = { services = { nixops-dns = { description = String; serviceConfig = { ExecStart = String; Type = String; User = ∀ α. α }; wantedBy = [String] } } } }; options = { services = { nixops-dns = { dnsmasq = { default = Bool; description = String; type = ∀ β. β }; domain = { default = String; description = String; example = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; user = { description = String; type = ∀ β. β } } } } }