∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { services = { udev = { packages = ∀ γ. [γ] } }; systemd = { services = { trezord = { after = [String]; description = String; path = ∀ β. β; serviceConfig = { ExecStart = String; Type = String; User = String }; wantedBy = [String] } } }; users = { groups = { trezord = {  } }; users = { trezord = { description = String; group = String; isSystemUser = Bool } } } }; meta = { doc = Path }; options = { services = { trezord = { emulator = { enable = { default = Bool; description = String; type = ∀ α. α }; port = { default = Integer; description = String; type = ∀ α. α } }; enable = { default = Bool; description = String; type = ∀ β. β } } } } }