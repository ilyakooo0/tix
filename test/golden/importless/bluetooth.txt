∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { bluetooth/main.conf = { source = ∀ β. β } }; systemPackages = [∀ γ. γ] }; hardware = { bluetooth = { config = { Policy = { AutoEnable = ∀ α. α } } } }; services = { dbus = { packages = [∀ β. β] }; udev = { packages = [∀ β. β] } }; systemd = { packages = [∀ γ. γ]; services = { bluetooth = { aliases = [String]; wantedBy = [String] } }; user = { services = { obex = { aliases = [String] } } } }; warnings = String }; options = { hardware = { bluetooth = { config = { description = String; example = { General = { ControllerMode = String } }; type = ∀ β. [β] }; enable = String; extraConfig = { default = Null; description = String; example = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; example = String; type = ∀ β. β }; powerOnBoot = { default = Bool; description = String; type = ∀ β. β } } } } }