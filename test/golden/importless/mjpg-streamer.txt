∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = { mjpg-streamer = { description = String; script = String; serviceConfig = { Group = ∀ α. α; Restart = String; RestartSec = Integer; User = ∀ α. α }; wantedBy = [String] } } }; users = { users = { mjpg-streamer = { group = ∀ β. β; uid = ∀ β. β } } } }; options = { services = { mjpg-streamer = { enable = String; group = { default = String; description = String; type = ∀ β. β }; inputPlugin = { default = String; description = String; type = ∀ β. β }; outputPlugin = { default = String; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }