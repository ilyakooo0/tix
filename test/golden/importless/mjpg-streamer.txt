∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β1 γ1 δ1.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.mjpg-streamer = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { systemd =
        { services =
            { mjpg-streamer =
                { description = String;
                  script = String;
                  serviceConfig =
                    { Group = θ;
                      Restart = String;
                      RestartSec = Integer;
                      User = ι;
                    };
                  wantedBy = [String];
                };
            };
        };
      users = {users = κ;};
    } ->
    λ,
  γ.mkOption = {default = String; description = String; type = μ;} -> ν,
  γ.optionalAttrs = Bool -> {mjpg-streamer = {group = ξ; uid = ο;};} -> κ,
  γ.types = π,
  ε.enable = η,
  ε.group = θ,
  ε.user = ι,
  ρ.services = β,
  σ.uids = β,
  τ.ids = σ,
  π.str = μ,
  υ.str = φ,
  χ.str = ψ,
  ω.str = α1
) =>
α ->
{ config = λ;
  options =
    { services =
        { mjpg-streamer =
            { enable = ζ;
              group = ν;
              inputPlugin = β1;
              outputPlugin = γ1;
              user = δ1;
            };
        };
    };
}