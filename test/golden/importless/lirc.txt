∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { lirc/lirc_options.conf = { text = ∀ β. β } }; systemPackages = ∀ γ. [γ] }; passthru = { lirc = { socket = String } }; systemd = { services = { lircd = { after = [String]; description = String; serviceConfig = { ExecStart = String; ExecStartPre = [String]; PermissionsStartOnly = Bool; RuntimeDirectory = String; RuntimeDirectoryPreserve = Bool; User = String }; unitConfig = { Documentation = [String] } } }; sockets = { lircd = { description = String; socketConfig = { ListenStream = ∀ α. α; SocketMode = String; SocketUser = String }; wantedBy = [String] } } }; users = { groups = { lirc = { gid = ∀ β. β } }; users = { lirc = { description = String; group = String; uid = ∀ β. β } } } }; options = { services = { lirc = { configs = { description = String; type = ∀ β. β }; enable = String; extraArguments = { default = ∀ β. β; description = String; type = ∀ β. β }; options = { description = String; example = String; type = ∀ β. β } } } } }