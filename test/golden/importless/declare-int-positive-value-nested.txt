∀ ε.
{lib = ε;} ->
{ options =
    { set =
        { value =
            ∀ δ.
            ( ε.mkOption =
                { type =
                    ∀ α β γ. (ε.types = α, α.ints = β, β.positive = γ) => γ;
                } ->
                δ
            ) =>
            δ;
        };
    };
}