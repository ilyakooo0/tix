∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.bin = ε,
  β.hypervGuest = ζ,
  β.lib = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { boot = {initrd = {kernelModules = [String];}; kernelParams = [String];};
      environment = {systemPackages = [ε];};
      security = {rngd = {enable = Bool;};};
      services = {udev = {packages = κ;};};
      systemd =
        {packages = [η]; targets = {hyperv-daemons = {wantedBy = [String];};};};
    } ->
    λ,
  γ.mkOption =
    {default = String; description = String; example = String; type = μ;} -> ν,
  γ.singleton = ξ -> κ,
  γ.types = ο,
  δ.writeTextFile = {destination = String; name = String; text = String;} -> ξ,
  ζ.enable = ι,
  π.virtualisation = β,
  ρ.hyperv-daemons = β,
  σ.kernelPackages = ρ,
  τ.boot = σ,
  υ.hyperv-daemons = β,
  φ.kernelPackages = υ,
  χ.boot = φ,
  ο.str = μ
) =>
α ->
{ config = λ;
  options = {virtualisation = {hypervGuest = {enable = θ; videoMode = ν;};};};
}