∀ γ.
{lib = γ; options = Bool;} ->
{ config =
    { set =
        ∀ α β.
        ( ({value = Integer;}) // (α) ~ (β),
          γ.optionalAttrs = Bool -> {enable = Bool;} -> α
        ) =>
        β;
    };
}