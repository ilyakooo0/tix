∀ β γ δ ε ζ η θ ι κ λ.
( β.overrideAttrs = (γ -> {postInstall = String;}) -> δ,
  β.overrideAttrs = (ε -> {postInstall = String;}) -> ζ,
  β.overrideAttrs = (η -> {postInstall = String;}) -> θ
) =>
{ buildOpenRAEngine =
    { description = String;
      homepage = String;
      mods = [String];
      src = ι;
      version = String;
    } ->
    κ -> β;
  extraPostFetch = λ;
  fetchFromGitHub =
    { extraPostFetch = λ;
      owner = String;
      repo = String;
      rev = String;
      sha256 = String;
    } ->
    ι;
} ->
{bleed = ∀ α. κ -> α; playtest = ∀ α. κ -> α; release = ∀ α. κ -> α;}