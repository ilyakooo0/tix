∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = { systemd = { services = { go-shadowsocks2-server = { after = [String]; description = String; serviceConfig = { DynamicUser = Bool; ExecStart = String }; wantedBy = [String] } } } }; options = { services = { go-shadowsocks2 = { server = { enable = String; listenAddress = { description = String; example = String; type = ∀ α. α } } } } } }