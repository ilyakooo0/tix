∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.malcontent = ζ,
  γ.malcontent-ui = ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services =
        {accounts-daemon = {enable = Bool;}; dbus = {packages = [θ];};};
    } ->
    ι,
  δ.out = θ,
  κ.malcontent = β,
  λ.services = κ,
  μ.malcontent = δ
) =>
α -> {config = ι; options = {services = {malcontent = {enable = η;};};};}