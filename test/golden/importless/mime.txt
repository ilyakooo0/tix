∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { extraSetup = String; pathsToLink = [String]; systemPackages = ∀ β. [β] } }; meta = { maintainers = ∀ γ. γ }; options = { xdg = { mime = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }