∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.all = β,
  α.mkDerivation =
    { buildInputs = [γ];
      enableParallelBuilding = Bool;
      meta =
        { badPlatforms = [String];
          description = String;
          homepage = String;
          license = δ;
          platforms = β;
        };
      nativeBuildInputs = [ε];
      outputs = [String];
      pname = String;
      postInstall = String;
      src = ζ;
      version = η;
    } ->
    θ,
  α.ncsa = δ,
  ι.licenses = α,
  κ.lib = ι,
  λ.platforms = α,
  μ.lib = λ
) =>
{ cmake = ε;
  fetch = String -> String -> ζ;
  llvm = γ;
  stdenv = α;
  version = η;
} ->
θ