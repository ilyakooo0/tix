∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω.
( β.config = γ,
  β.lib = δ,
  β.pkgs = ε,
  γ.enable = Bool,
  γ.flatpak = ζ,
  δ.literalExample = String -> η,
  δ.mkEnableOption = String -> θ,
  δ.mkIf =
    ι ->
    { assertions = [{assertion = Bool; message = String;}];
      environment = {profiles = [String]; systemPackages = [κ];};
      services = {dbus = {packages = [λ];};};
      systemd = {packages = [μ];};
      users =
        { groups = {flatpak = {};};
          users =
            { flatpak =
                {description = String; group = String; isSystemUser = Bool;};
            };
        };
    } ->
    ν,
  δ.mkOption =
    { default = ∀ α. α;
      description = String;
      example = η;
      internal = Bool;
      type = ξ;
    } ->
    ο,
  δ.types = π,
  ε.flatpak = κ,
  ε.maintainers = ρ,
  ζ.enable = ι,
  ζ.guiPackages = [κ],
  σ.services = γ,
  τ.portal = γ,
  υ.xdg = τ,
  φ.meta = ε,
  χ.flatpak = φ,
  π.listOf = ψ -> ξ,
  ω.package = ψ
) =>
β ->
{ config = ν;
  meta = {doc = Path; maintainers = ρ;};
  options = {services = {flatpak = {enable = θ; guiPackages = ο;};};};
}