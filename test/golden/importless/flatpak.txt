∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { assertions = [{ assertion = Bool; message = String }]; environment = { profiles = [String]; systemPackages = ∀ γ. [γ] }; services = { dbus = { packages = ∀ β. [β] } }; systemd = { packages = ∀ γ. [γ] }; users = { groups = { flatpak = {  } }; users = { flatpak = { description = String; group = String; isSystemUser = Bool } } } }; meta = { doc = Path; maintainers = ∀ δ. δ }; options = { services = { flatpak = { enable = String; guiPackages = { default = ∀ α. α; description = String; example = String; internal = Bool; type = ∀ α. α } } } } }