∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; systemd = { user = { services = { lorri = { after = [String]; description = String; path = ∀ α. [α]; requires = [String]; serviceConfig = { ExecStart = String; PrivateTmp = Bool; ProtectHome = String; ProtectSystem = String; Restart = String } } }; sockets = { lorri = { description = String; socketConfig = { ListenStream = String; RuntimeDirectory = String }; wantedBy = [String] } } } } }; options = { services = { lorri = { enable = { default = Bool; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; example = String; type = ∀ β. β } } } } }