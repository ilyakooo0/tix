∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β2.
( α.guest = β,
  α.kernelPackages = γ,
  α.vboxsf = δ,
  ε.mkIf = ζ -> η -> θ,
  ε.mkMerge =
    [ { assertions = [{assertion = Bool; message = String;}];
        boot =
          { extraModulePackages = [ι];
            initrd = {supportedFilesystems = [String];};
            supportedFilesystems = [String];
          };
        environment = {systemPackages = [κ];};
        services = {udev = {extraRules = String;};};
        systemd =
          { services =
              { virtualbox =
                  { after = [String];
                    description = String;
                    requires = [String];
                    serviceConfig = {ExecStart = String;};
                    unitConfig = {ConditionVirtualization = String;};
                    wantedBy = [String];
                  };
              };
          };
        users = {groups = {vboxsf = {gid = δ;};};};
      }
    ] ->
    η,
  ε.mkOption = {default = Bool; description = String; type = λ;} -> μ,
  ε.mkOverride = Integer -> [String] -> ν,
  ε.types = ξ,
  ο.isi686 = Bool,
  ο.isx86_64 = Bool,
  γ.virtualboxGuestAdditions = ι,
  π.boot = α,
  β.enable = ζ,
  β.x11 = ρ,
  σ.virtualbox = α,
  τ.virtualisation = σ,
  υ.gids = α,
  φ.ids = υ,
  ξ.bool = λ,
  χ.bool = ψ,
  ω.stdenv = ο,
  α1.stdenv = ο
) =>
{config = α; lib = ε; pkgs = ο;} ->
{ config = θ;
  options =
    {virtualisation = {virtualbox = {guest = {enable = μ; x11 = β2;};};};};
}