∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β2 γ3.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.guest = ε,
  β.kernelPackages = ζ,
  β.vboxsf = η,
  γ.mkIf = θ -> ι -> κ,
  γ.mkMerge =
    [ { assertions = [{assertion = Bool; message = String;}];
        boot =
          { extraModulePackages = [λ];
            initrd = {supportedFilesystems = [String];};
            supportedFilesystems = [String];
          };
        environment = {systemPackages = [μ];};
        services = {udev = {extraRules = String;};};
        systemd =
          { services =
              { virtualbox =
                  { after = [String];
                    description = String;
                    requires = [String];
                    serviceConfig = {ExecStart = String;};
                    unitConfig = {ConditionVirtualization = String;};
                    wantedBy = [String];
                  };
              };
          };
        users = {groups = {vboxsf = {gid = η;};};};
      }
    ] ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = ν;} -> ξ,
  γ.mkOverride = Integer -> [String] -> ο,
  γ.types = π,
  δ.isi686 = Bool,
  δ.isx86_64 = Bool,
  ζ.virtualboxGuestAdditions = λ,
  ρ.boot = β,
  ε.enable = θ,
  ε.x11 = σ,
  τ.virtualbox = β,
  υ.virtualisation = τ,
  φ.stdenv = δ,
  χ.stdenv = δ,
  ψ.gids = β,
  ω.ids = ψ,
  π.bool = ν,
  α1.bool = β2
) =>
α ->
{ config = κ;
  options =
    {virtualisation = {virtualbox = {guest = {enable = ξ; x11 = γ3;};};};};
}