∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { networking = { firewall = { allowedTCPPorts = [Integer] } }; systemd = { services = { sonarr = { after = [String]; description = String; serviceConfig = { ExecStart = String; Group = ∀ α. α; Restart = String; Type = String; User = ∀ α. α }; wantedBy = [String] } }; tmpfiles = { rules = [String] } }; users = { groups = { sonarr = { gid = ∀ β. β } }; users = { sonarr = { group = ∀ β. β; home = ∀ β. β; uid = ∀ β. β } } } }; options = { services = { sonarr = { dataDir = { default = String; description = String; type = ∀ β. β }; enable = String; group = { default = String; description = String; type = ∀ β. β }; openFirewall = { default = Bool; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }