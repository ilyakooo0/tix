∀ α β γ δ ε ζ.
(α.bkchr = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  kcodecs = ε;
  lib = α;
  mkDerivation =
    { buildInputs = [ε];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
    } ->
    ζ;
} ->
ζ