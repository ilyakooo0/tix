∀ θ ι κ.
( θ.mkDerivation =
    { configureFlags =
        ∀ δ ε ζ η.
        ( θ.lib = δ,
          δ.optional = ε -> String -> ζ,
          θ.hostPlatform = η,
          η.isMinGW = ε
        ) =>
        ζ;
      meta =
        { description = String;
          homepage = String;
          license = ∀ α β γ. (θ.lib = α, α.licenses = β, β.gpl3Plus = γ) => γ;
          longDescription = String;
          maintainers = ∀ α. α;
          platforms = ∀ α β γ. (θ.lib = α, α.platforms = β, β.all = γ) => γ;
        };
      name = String;
      outputs = [String];
      src = ι;
    } ->
    κ
) =>
{fetchurl = {sha256 = String; url = String;} -> ι; stdenv = θ;} -> κ