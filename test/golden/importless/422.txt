∀ β γ δ ε ζ η θ ι κ λ μ ν ξ.
( β.all = γ,
  β.gpl3Plus = δ,
  β.isMinGW = ε,
  β.mkDerivation =
    { configureFlags = ζ;
      meta =
        { description = String;
          homepage = String;
          license = δ;
          longDescription = String;
          maintainers = ∀ α. α;
          platforms = γ;
        };
      name = String;
      outputs = [String];
      src = η;
    } ->
    θ,
  β.optional = ε -> String -> ζ,
  ι.lib = β,
  κ.hostPlatform = β,
  λ.licenses = β,
  μ.lib = λ,
  ν.platforms = β,
  ξ.lib = ν
) =>
{fetchurl = {sha256 = String; url = String;} -> η; stdenv = β;} -> θ