∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.sabnzbd = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { systemd =
        { services =
            { sabnzbd =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      Group = String;
                      GuessMainPID = String;
                      Type = String;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {sabnzbd = {gid = θ;};};
          users =
            { sabnzbd =
                { createHome = Bool;
                  description = String;
                  group = String;
                  home = String;
                  uid = ι;
                };
            };
        };
    } ->
    κ,
  γ.mkOption = {default = String; description = String;} -> λ,
  δ.sabnzbd = μ,
  ε.enable = η,
  ν.services = β,
  ξ.gids = β,
  ο.ids = ξ,
  π.uids = β,
  ρ.ids = π
) =>
α ->
{ config = κ;
  options =
    { services =
        {sabnzbd = {configFile = λ; enable = ζ; group = σ; user = τ;};};
    };
}