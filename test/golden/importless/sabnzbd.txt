∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.sabnzbd = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { systemd =
        { services =
            { sabnzbd =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ExecStart = String;
                      Group = String;
                      GuessMainPID = String;
                      Type = String;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {sabnzbd = {gid = ζ;};};
          users =
            { sabnzbd =
                { createHome = Bool;
                  description = String;
                  group = String;
                  home = String;
                  uid = η;
                };
            };
        };
    } ->
    θ,
  γ.mkOption = {default = String; description = String;} -> ι,
  κ.sabnzbd = λ,
  β.enable = ε,
  μ.services = α,
  ν.gids = α,
  ξ.ids = ν,
  ο.uids = α,
  π.ids = ο
) =>
{config = α; lib = γ; pkgs = κ;} ->
{ config = θ;
  options =
    { services =
        {sabnzbd = {configFile = ι; enable = δ; group = ρ; user = σ;};};
    };
}