∀ α β γ δ ε ζ η θ ι.
( α.appendToName = String -> β -> γ,
  α.closePropagation = δ -> [ε],
  ε.name = ζ,
  η.lib = α,
  θ.lib = α
) =>
{ idris = ε;
  makeWrapper = ι;
  stdenv = α;
  symlinkJoin =
    {buildInputs = [ι]; name = ζ; paths = [ε]; postBuild = String;} -> β;
} ->
δ -> γ