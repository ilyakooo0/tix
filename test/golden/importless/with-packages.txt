∀ β γ δ ε ζ η θ ι κ.
( β.lib = γ,
  β.lib = δ,
  δ.appendToName = String -> ε -> ζ,
  γ.closePropagation = η -> θ
) =>
{ idris = ι;
  makeWrapper = κ;
  stdenv = β;
  symlinkJoin =
    { buildInputs = [κ];
      name = ∀ α. (ι.name = α) => α;
      paths = [ι];
      postBuild = String;
    } ->
    ε;
} ->
η -> ζ