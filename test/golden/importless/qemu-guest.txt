∀ α β.
(α.mkDefault = Bool -> β) => {lib = α;} ->
{ boot =
    { initrd =
        { availableKernelModules = [String];
          kernelModules = [String];
          postDeviceCommands = String;
        };
    };
  security = {rngd = {enable = β;};};
}