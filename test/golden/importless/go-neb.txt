∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { systemd = { services = { go-neb = { after = [String]; description = String; environment = { BASE_URL = ∀ α. α; BIND_ADDRESS = ∀ α. α; CONFIG_FILE = ∀ α. α }; serviceConfig = { DynamicUser = Bool; ExecStart = String }; wantedBy = [String] } } } }; meta = { maintainers = ∀ γ. [γ] }; options = { services = { go-neb = { baseUrl = { description = String; type = ∀ β. β }; bindAddress = { default = String; description = String; type = ∀ β. β }; config = { description = String; type = ∀ β. β }; enable = String } } } }