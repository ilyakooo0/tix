∀ β γ δ ε. { config = β; lib = γ; options = δ; pkgs = ε } -> { extraOpts = { refreshRate = { default = String; description = String; type = ∀ α. α } }; port = Integer; serviceOpts = { serviceConfig = { ExecStart = String; SupplementaryGroups = String } } }