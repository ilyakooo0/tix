∀ α β γ δ ε ζ η.
( α.makeSourceTarball =
    { buildInputs = [β];
      distPhase = String;
      name = String;
      officialRelease = Bool;
      src = γ;
      version = δ;
      versionSuffix = ε;
    } ->
    ζ,
  α.nix = β,
  η.releaseTools = α
) =>
{nixpkgs = γ; pkgs = α; version = δ; versionSuffix = ε;} -> ζ