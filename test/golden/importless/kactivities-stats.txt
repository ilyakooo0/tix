∀ α β γ δ ε ζ η.
(α.maintainers = β, β.ttuegel = γ) => { boost = δ;
  extra-cmake-modules = ε;
  kactivities = δ;
  kconfig = δ;
  lib = α;
  mkDerivation =
    { buildInputs = [δ];
      meta = {maintainers = [γ];};
      name = String;
      nativeBuildInputs = [ε];
      outputs = [String];
      propagatedBuildInputs = [ζ];
    } ->
    η;
  qtbase = ζ;
} ->
η