∀ α β γ δ ε ζ η.
(α.ttuegel = β, γ.maintainers = α) => { boost = δ;
  extra-cmake-modules = ε;
  kactivities = δ;
  kconfig = δ;
  lib = α;
  mkDerivation =
    { buildInputs = [δ];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [ε];
      outputs = [String];
      propagatedBuildInputs = [ζ];
    } ->
    η;
  qtbase = ζ;
} ->
η