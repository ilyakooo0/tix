∀ δ ε ζ η θ.
( δ.mkDerivation =
    { buildInputs = [ε];
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = ∀ α β γ. (δ.lib = α, α.licenses = β, β.gpl2 = γ) => γ;
          maintainers =
            ∀ α β γ. (δ.lib = α, α.maintainers = β, β.bstrik = γ) => [γ];
        };
      name = String;
      nativeBuildInputs = [ζ];
      postUnpack = String;
      src = η;
    } ->
    θ
) =>
{ alsaLib = ε;
  fetchgit = {rev = String; sha256 = String; url = String;} -> η;
  libX11 = ε;
  libXext = ε;
  libXpm = ε;
  pkgconfig = ζ;
  stdenv = δ;
} ->
θ