∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { systemPackages = ∀ β. [β] }; services = { xserver = { desktopManager = { session = { name = String; start = String } } } } }; options = { services = { xserver = { desktopManager = { xterm = { enable = { default = Bool; defaultText = String; description = String; type = ∀ α. α } } } } } } }