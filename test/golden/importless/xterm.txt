∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = Bool,
  β.stateVersion = ε,
  β.xterm = ζ,
  γ.mkIf =
    η ->
    { environment = {systemPackages = [θ];};
      services = {xserver = {desktopManager = {session = ι;};};};
    } ->
    κ,
  γ.mkOption =
    {default = Bool; defaultText = String; description = String; type = λ;} ->
    μ,
  γ.singleton = {name = String; start = String;} -> ι,
  γ.types = ν,
  γ.versionOlder = ε -> String -> Bool,
  δ.xterm = θ,
  ξ.xserver = β,
  ο.services = ξ,
  ζ.enable = η,
  π.desktopManager = β,
  ρ.xserver = π,
  σ.services = ρ,
  τ.system = β,
  υ.system = β,
  ν.bool = λ
) =>
α ->
{ config = κ;
  options =
    {services = {xserver = {desktopManager = {xterm = {enable = μ;};};};};};
}