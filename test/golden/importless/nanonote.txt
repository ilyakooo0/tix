∀ δ ε ζ η.
( δ.mkDerivation =
    { configurePhase = String;
      dontStrip = Bool;
      installPhase = String;
      makeFlags = [String];
      meta =
        {platforms = ∀ α β γ. (δ.lib = α, α.platforms = β, β.mips = γ) => γ;};
      name = String;
      patchPhase = String;
      preBuild = String;
      src = ε;
      srcPatches = ζ;
    } ->
    η
) =>
{ fetchgit = {rev = String; sha256 = String; url = String;} -> ζ;
  fetchurl = {sha256 = String; url = String;} -> ε;
  stdenv = δ;
} ->
η