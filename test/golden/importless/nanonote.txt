∀ α β γ δ ε ζ η.
( α.mips = β,
  α.mkDerivation =
    { configurePhase = String;
      dontStrip = Bool;
      installPhase = String;
      makeFlags = [String];
      meta = {platforms = β;};
      name = String;
      patchPhase = String;
      preBuild = String;
      src = γ;
      srcPatches = δ;
    } ->
    ε,
  ζ.platforms = α,
  η.lib = ζ
) =>
{ fetchgit = {rev = String; sha256 = String; url = String;} -> δ;
  fetchurl = {sha256 = String; url = String;} -> γ;
  stdenv = α;
} ->
ε