∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.maintainers = ζ,
  γ.mkIf =
    ε ->
    { environment =
        {systemPackages = [η]; variables = {QT_QPA_PLATFORMTHEME = String;};};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.qt5ct = η,
  γ.types = λ,
  μ.qt5ct = β,
  ν.programs = μ,
  ζ.romildo = ξ,
  λ.bool = ι
) =>
α ->
{ config = θ;
  meta = {maintainers = [ξ];};
  options = {programs = {qt5ct = {enable = κ;};};};
}