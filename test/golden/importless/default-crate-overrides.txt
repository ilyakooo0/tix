∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ.
( β.isDarwin = γ,
  β.optional = δ -> ε -> ζ,
  β.optionals = γ -> [ε] -> [η],
  θ.frameworks = ι,
  κ.lib = β,
  λ.lib = β,
  ι.Security = ε,
  μ.apple_sdk = θ,
  ν.CoreFoundation = ε,
  ξ.apple_sdk = θ
) =>
{ cairo = ο;
  curl = η;
  darwin = θ;
  dbus = η;
  dbus-glib = π;
  foundationdb = ρ;
  gdk-pixbuf = σ;
  gmp = τ;
  libgit2 = η;
  libiconv = ε;
  libsodium = η;
  libssh2 = η;
  openssl = η;
  pkgconfig = η;
  postgresql = η;
  python3 = υ;
  sqlite = η;
  stdenv = β;
  zlib = η;
} ->
{ cairo-rs = ∀ α. α -> {buildInputs = [ο];};
  cargo = ∀ α. α -> {buildInputs = [η];};
  curl-sys =
    ∀ α.
    α ->
    { buildInputs = [η];
      extraLinkFlags = [String];
      propagatedBuildInputs = [η];
    };
  dbus = ∀ α. α -> {buildInputs = [η];};
  foundationdb = ∀ α. α -> {buildInputs = [ρ];};
  foundationdb-sys = ∀ α. α -> {buildInputs = [ρ];};
  gdk-pixbuf = ∀ α. α -> {buildInputs = [σ];};
  gdk-pixbuf-sys = ∀ α. α -> {buildInputs = [π];};
  gio-sys = ∀ α. α -> {buildInputs = [π];};
  gobject-sys = ∀ α. α -> {buildInputs = [π];};
  libdbus-sys = ∀ α. α -> {buildInputs = [η];};
  libgit2-sys =
    ∀ α. α -> {LIBGIT2_SYS_USE_PKG_CONFIG = Bool; buildInputs = [η];};
  libsqlite3-sys = ∀ α. α -> {buildInputs = [η];};
  libssh2-sys = ∀ α. α -> {buildInputs = [η];};
  libz-sys = ∀ α. α -> {buildInputs = [η]; extraLinkFlags = [String];};
  openssl = ∀ α. α -> {buildInputs = [η];};
  openssl-sys = ∀ α. α -> {buildInputs = [η];};
  pq-sys = ∀ α. α -> {buildInputs = [η];};
  rink =
    ∀ α.
    α -> {buildInputs = [τ]; crateBin = [{name = String; path = String;}];};
  security-framework-sys = ∀ α. α -> {propagatedBuildInputs = [ε];};
  serde_derive = ∀ α. α -> {buildInputs = ζ;};
  thrussh-libsodium = ∀ α. α -> {buildInputs = [η];};
  xcb = ∀ α. α -> {buildInputs = [υ];};
}