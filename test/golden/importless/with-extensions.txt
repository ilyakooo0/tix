∀ β γ δ ε ζ.
{ buildEnv = {name = String; paths = β;} -> γ;
  lib = δ;
  makeWrapper = ε;
  runCommand =
    String ->
    { buildInputs = [ε];
      dontPatchELF = Bool;
      dontStrip = Bool;
      meta = ∀ α. (ε.meta = α) => α;
    } ->
    String -> ζ;
  vscode = ε;
  vscodeExtensions = β;
} ->
ζ