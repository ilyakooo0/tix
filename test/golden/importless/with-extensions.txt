∀ β γ δ ε ζ η θ.
(β.executableName = γ) => { buildEnv = {name = String; paths = δ;} -> ε;
  lib = ζ;
  makeWrapper = β;
  runCommand =
    String ->
    { buildInputs = [β];
      dontPatchELF = Bool;
      dontStrip = Bool;
      meta = ∀ α. (β.meta = α) => α;
    } ->
    String -> η;
  vscode = θ;
  vscodeExtensions = δ;
} ->
η