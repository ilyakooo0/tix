∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { X11/imwheel/imwheelrc = { source = ∀ β. β } }; systemPackages = ∀ γ. [γ] }; systemd = { user = { services = { imwheel = { description = String; partOf = [String]; serviceConfig = { ExecStart = String; ExecStop = String; Restart = String; RestartSec = Integer }; wantedBy = [String] } } } } }; options = { services = { xserver = { imwheel = { enable = String; extraOptions = { default = [String]; description = String; example = [String]; type = ∀ α. α }; rules = { default = {  }; description = String; example = String; type = ∀ α. α } } } } } }