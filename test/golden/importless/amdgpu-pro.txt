∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { boot = { blacklistedKernelModules = [String]; extraModulePackages = [∀ γ. γ]; kernelPackages = { extraConfig = String } }; environment = { etc = { amd/amdapfxx.blb = { source = ∀ β. β }; amd/amdrc = { source = ∀ β. β }; gbm/gbm.conf = { source = ∀ β. β } } }; hardware = { firmware = [∀ γ. γ]; opengl = { package = ∀ γ. γ; package32 = { kernel = Null; libsOnly = Bool }; setLdLibraryPath = Bool } }; nixpkgs = { config = { xorg = { abiCompat = String } } }; services = { xserver = { drivers = { display = Bool; modules = [∀ α. α]; name = String } } }; system = { activationScripts = { setup-amdgpu-pro = String }; requiredKernelConfig = [String] } } }