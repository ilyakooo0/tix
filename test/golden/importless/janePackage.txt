∀ α β γ δ ε ζ η θ.
(α.asl20 = β, γ.licenses = α, δ.lib = γ) => { buildDunePackage =
    { hash = ε;
      meta = {homepage = String; license = β;};
      minimumOCamlVersion = String;
      pname = ζ;
      src = η;
      version = String;
    } ->
    θ;
  defaultVersion = String;
  fetchFromGitHub = {owner = String; repo = ζ; rev = String; sha256 = ε;} -> η;
  stdenv = α;
} ->
{hash = ε; pname = ζ; version = String;} -> θ