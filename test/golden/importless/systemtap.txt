∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.config = β,
  α.lib = γ,
  β.kernelConfig = δ,
  β.systemtap = ε,
  γ.isYes = String -> ζ,
  γ.mkIf =
    η ->
    { boot = {kernel = {features = {debug = Bool;};};};
      environment = {systemPackages = [θ];};
      system = {requiredKernelConfig = [ζ];};
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  ε.enable = η,
  ν.programs = β,
  ξ.kernelPackages = β,
  ο.boot = ξ,
  π.lib = β,
  μ.bool = κ
) =>
α -> {config = ι; options = {programs = {systemtap = {enable = λ;};};};}