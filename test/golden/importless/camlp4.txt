∀ α β γ δ ε ζ η θ ι κ.
( α.lgpl21 = β,
  α.vbgl = γ,
  δ.meta = ε,
  ζ.licenses = α,
  η.maintainers = α,
  ε.homepage = θ
) =>
{ buildDunePackage =
    { meta =
        {description = String; homepage = θ; license = β; maintainers = [γ];};
      minimumOCamlVersion = String;
      pname = String;
      preBuild = String;
      propagatedBuildInputs = [ι];
      src = δ;
      version = String;
    } ->
    κ;
  camlp4 = ι;
  fetchFromGitHub =
    {owner = String; repo = String; rev = String; sha256 = String;} -> δ;
  lib = α;
} ->
κ