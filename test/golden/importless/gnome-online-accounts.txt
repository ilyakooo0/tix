∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  δ.gnome-online-accounts = ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];};};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.teams = λ,
  γ.types = μ,
  ν.gnome-online-accounts = β,
  ξ.gnome3 = ν,
  ο.services = ξ,
  λ.members = π,
  ρ.gnome = λ,
  μ.bool = ι
) =>
α ->
{ config = θ;
  meta = {maintainers = π;};
  options = {services = {gnome3 = {gnome-online-accounts = {enable = κ;};};};};
}