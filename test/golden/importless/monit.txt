∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.monit = ε,
  β.source = ζ,
  δ.monit = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { environment =
        {etc = {monitrc = {mode = String; text = κ;};}; systemPackages = [η];};
      systemd =
        { services =
            { monit =
                { after = [String];
                  description = String;
                  restartTriggers = [ζ];
                  serviceConfig =
                    { ExecReload = String;
                      ExecStart = String;
                      ExecStop = String;
                      KillMode = String;
                      Restart = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    λ,
  γ.mkOption = {default = String; description = String; type = μ;} -> ν,
  γ.types = ξ,
  ε.config = κ,
  ε.enable = ι,
  ο.services = β,
  π.monitrc = β,
  ρ.etc = π,
  σ.environment = ρ,
  ξ.lines = μ
) =>
α -> {config = λ; options = {services = {monit = {config = ν; enable = θ;};};};}