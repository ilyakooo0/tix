∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { etc = { monitrc = { mode = String; text = ∀ α. α } }; systemPackages = ∀ β. [β] }; systemd = { services = { monit = { after = [String]; description = String; restartTriggers = ∀ α. [α]; serviceConfig = { ExecReload = String; ExecStart = String; ExecStop = String; KillMode = String; Restart = String }; wantedBy = [String] } } } }; options = { services = { monit = { config = { default = String; description = String; type = ∀ α. α }; enable = String } } } }