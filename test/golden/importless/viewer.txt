∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( β.majorMinor = String -> γ,
  β.mkDerivation =
    { buildInputs = [δ];
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = ε;
          maintainers = [ζ];
          platforms = [String];
        };
      nativeBuildInputs = [η];
      pname = String;
      src = θ;
      unpackPhase = String;
      version = ∀ α. α;
    } ->
    ι,
  β.unfree = ε,
  β.zimbatm = ζ,
  κ.lib = δ,
  λ.versions = β,
  μ.lib = λ,
  ν.licenses = β,
  ξ.lib = ν,
  ο.maintainers = β,
  π.lib = ο
) =>
{ autoPatchelfHook = η;
  dpkg = η;
  fetchurl = {sha256 = String; url = String;} -> θ;
  freeglut = δ;
  gcc-unwrapped = κ;
  libGL = δ;
  libGLU = δ;
  makeWrapper = η;
  stdenv = β;
  zlib = δ;
} ->
ι