∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  δ.gnome-user-share = ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    ε ->
    {environment = {systemPackages = [ζ];}; systemd = {packages = [θ];};} -> ι,
  γ.teams = κ,
  λ.gnome-user-share = β,
  μ.gnome3 = λ,
  ν.services = μ,
  ξ.gnome3 = δ,
  ο.gnome3 = δ,
  κ.members = π,
  ρ.gnome = κ
) =>
α ->
{ config = ι;
  meta = {maintainers = π;};
  options = {services = {gnome3 = {gnome-user-share = {enable = η;};};};};
}