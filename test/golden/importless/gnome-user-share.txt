∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.enable = β,
  γ.gnome-user-share = δ,
  ε.mkEnableOption = String -> ζ,
  ε.mkIf =
    β ->
    {environment = {systemPackages = [δ];}; systemd = {packages = [η];};} -> θ,
  ε.teams = ι,
  κ.gnome-user-share = α,
  λ.gnome3 = κ,
  μ.services = λ,
  ν.gnome3 = γ,
  ξ.gnome3 = γ,
  ι.members = ο,
  π.gnome = ι
) =>
{config = α; lib = ε; pkgs = γ;} ->
{ config = θ;
  meta = {maintainers = ο;};
  options = {services = {gnome3 = {gnome-user-share = {enable = ζ;};};};};
}