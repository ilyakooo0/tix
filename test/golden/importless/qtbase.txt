∀ β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ. { bison = β; buildExamples = Bool; buildTests = Bool; coreutils = γ; cups = Null; darwin = δ; dbus = ε; dconf = Null; decryptSslTraffic = Bool; developerBuild = Bool; flex = β; fontconfig = ζ; freetype = ζ; gdb = [η]; glib = ζ; gperf = β; gtk3 = Null; harfbuzz = θ; icu = θ; lib = ι; libGL = String; libGLSupported = String -> κ; libX11 = ζ; libXcomposite = ζ; libXcursor = λ; libXext = ζ; libXi = ζ; libXrender = ζ; libiconv = μ; libinput = ν; libjpeg = θ; libmysqlclient = Null; libpng = θ; libxcb = ζ; libxkbcommon = ζ; libxml2 = ε; libxslt = θ; lndir = β; openssl = θ; patches = ξ; pcre16 = θ; pcre2 = ο; perl = β; pkgconfig = β; postgresql = Null; python3 = ν; qtCompatVersion = π; sqlite = θ; src = ρ; stdenv = σ; udev = ζ; version = String -> τ; which = β; withGtk3 = Bool; xcbutil = ζ; xcbutilimage = ζ; xcbutilkeysyms = ζ; xcbutilrenderutil = ζ; xcbutilwm = ζ; zlib = θ } -> { NIX_CFLAGS_COMPILE = [String]; PSQL_LIBS = String; buildInputs = [ν]; configureFlags = [String]; devTools = [String]; enableParallelBuilding = Bool; fix_qt_builtin_paths = Path; fix_qt_module_paths = Path; meta = { broken = Bool; description = String; homepage = String; license = ∀ α. [α]; maintainers = ∀ α. [α]; platforms = ∀ α. α }; name = String; nativeBuildInputs = [β]; outputs = [String]; patches = ξ; postConfigure = String; postFixup = String; postInstall = String; postPatch = String; preConfigure = String; preHook = String; prefixKey = String; propagatedBuildInputs = [ε]; propagatedNativeBuildInputs = [β]; qtCompatVersion = π; qtDocPrefix = String; qtPluginPrefix = String; qtQmlPrefix = String; setOutputFlags = Bool; setupHook = Path; src = ρ; version = String -> τ }