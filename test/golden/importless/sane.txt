∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = [{ environment = { sessionVariables = { LD_LIBRARY_PATH = [String]; SANE_CONFIG_DIR = ∀ β. β }; systemPackages = ∀ γ. [γ] }; hardware = { sane = { configDir = String } }; services = { udev = { packages = ∀ β. [β] } }; users = { groups = { scanner = { gid = ∀ α. α } } } }]; options = { hardware = { sane = { configDir = { description = String; internal = Bool; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; extraBackends = { default = ∀ β. β; description = String; example = String; type = ∀ β. β }; netConf = { default = String; description = String; example = String; type = ∀ β. β }; snapshot = { default = Bool; description = String; type = ∀ β. β } } }; services = { saned = { enable = { default = Bool; description = String; type = ∀ β. β }; extraConfig = { default = String; description = String; example = String; type = ∀ β. β } } } } }