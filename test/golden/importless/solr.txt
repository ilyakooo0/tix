∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; systemd = { services = { solr = { after = [String]; environment = { LOG4J_PROPS = String; SOLR_HOME = String; SOLR_LOGS_DIR = String; SOLR_PORT = String }; path = ∀ β. [β]; preStart = String; serviceConfig = { ExecStart = String; ExecStop = String; Group = ∀ α. α; User = ∀ α. α }; wantedBy = [String] } } }; users = { groups = { solr = { gid = ∀ β. β } }; users = { solr = { createHome = Bool; group = ∀ β. β; home = ∀ β. β; uid = ∀ β. β } } } }; options = { services = { solr = { enable = String; extraJavaOptions = { default = ∀ β. β; description = String; type = ∀ β. β }; group = { default = String; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β }; port = { default = Integer; description = String; type = ∀ β. β }; stateDir = { default = String; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }