∀ β γ δ ε ζ η θ ι.
( β.all = γ,
  β.mkDerivation =
    { buildInputs = [δ];
      builder = ε;
      convert = Path;
      data = ζ;
      meta =
        { description = String;
          homepage = String;
          maintainers = ∀ α. α;
          platforms = γ;
        };
      pname = String;
      version = String;
    } ->
    η,
  θ.platforms = β,
  ι.lib = θ
) =>
{ dict = δ;
  fetchurl = {sha256 = String; url = String;} -> ζ;
  glibcLocales = δ;
  python = δ;
  stdenv = β;
  writeScript = String -> String -> ε;
} ->
η