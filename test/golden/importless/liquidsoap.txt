∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β1 γ1 δ1 ε1 ζ1 η1 θ1 ι1 κ1
λ1 μ1 ν1 ξ1 ο1.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.ids = ε,
  β.services = ζ,
  γ.builtins = η,
  γ.literalExample = String -> θ,
  γ.map =
    ( ι ->
      { name = ι;
        value =
          { after = [String];
            description = String;
            path = [κ];
            serviceConfig =
              {ExecStart = String; LogsDirectory = String; User = String;};
            wantedBy = [String];
          };
      }
    ) ->
    λ -> μ,
  γ.mkIf =
    Bool ->
    { systemd = {services = ν;};
      users =
        { groups = {liquidsoap = {gid = ξ;};};
          users =
            { liquidsoap =
                { createHome = Bool;
                  description = String;
                  extraGroups = [String];
                  group = String;
                  home = String;
                  uid = ο;
                };
            };
        };
    } ->
    π,
  γ.mkOption =
    { default = {};
      description = String;
      example = {myStream1 = θ; myStream2 = ρ; myStream3 = σ;};
      type = τ;
    } ->
    υ,
  γ.types = φ,
  δ.wget = κ,
  η.attrNames = χ -> λ,
  ζ.liquidsoap = ψ,
  ψ.streams = χ,
  ω.getAttr = ι -> α1 -> β1,
  γ1.liquidsoap = δ1,
  δ1.streams = α1,
  ε1.length = λ -> Number,
  ζ1.listToAttrs = μ -> ν,
  ε.gids = η1,
  η1.liquidsoap = ξ,
  θ1.uids = ι1,
  ι1.liquidsoap = ο,
  φ.attrsOf = κ1 -> τ,
  λ1.either = μ1 -> ν1 -> κ1,
  ξ1.path = μ1,
  ο1.str = ν1
) =>
α -> {config = π; options = {services = {liquidsoap = {streams = υ;};};};}