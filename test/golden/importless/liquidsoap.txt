∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { systemd = { services = ∀ β. β }; users = { groups = { liquidsoap = { gid = ∀ α. α } }; users = { liquidsoap = { createHome = Bool; description = String; extraGroups = [String]; group = String; home = String; uid = ∀ α. α } } } }; options = { services = { liquidsoap = { streams = { default = {  }; description = String; example = { myStream1 = String; myStream2 = String; myStream3 = String }; type = ∀ α. α } } } } }