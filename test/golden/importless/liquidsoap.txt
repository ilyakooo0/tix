∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β2 γ3 δ4 ε5 ζ6 η7 θ8 ι9 κ10
λ11 μ12 ν13 ξ14 ο15.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.liquidsoap = ε,
  β.streams = ζ,
  γ.builtins = η,
  γ.literalExample = String -> θ,
  γ.map =
    ( ι ->
      { name = ι;
        value =
          { after = [String];
            description = String;
            path = [κ];
            serviceConfig =
              {ExecStart = String; LogsDirectory = String; User = String;};
            wantedBy = [String];
          };
      }
    ) ->
    λ -> μ,
  γ.mkIf =
    Bool ->
    { systemd = {services = ν;};
      users =
        { groups = {liquidsoap = {gid = ε;};};
          users =
            { liquidsoap =
                { createHome = Bool;
                  description = String;
                  extraGroups = [String];
                  group = String;
                  home = String;
                  uid = ξ;
                };
            };
        };
    } ->
    ο,
  γ.mkOption =
    { default = {};
      description = String;
      example = {myStream1 = θ; myStream2 = π; myStream3 = ρ;};
      type = σ;
    } ->
    τ,
  γ.types = υ,
  δ.wget = κ,
  η.attrNames = ζ -> λ,
  φ.liquidsoap = β,
  χ.services = φ,
  ψ.getAttr = ι -> ω -> α1,
  β2.liquidsoap = β,
  γ3.services = β2,
  δ4.length = λ -> Integer,
  ε5.listToAttrs = μ -> ν,
  ζ6.gids = β,
  η7.ids = ζ6,
  θ8.uids = β,
  ι9.ids = θ8,
  υ.attrsOf = κ10 -> σ,
  λ11.either = μ12 -> ν13 -> κ10,
  ξ14.path = μ12,
  ο15.str = ν13
) =>
α -> {config = ο; options = {services = {liquidsoap = {streams = τ;};};};}