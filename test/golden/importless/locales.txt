∀ α β γ δ ε.
(α.optionalString = Bool -> String -> String, β.lib = α) => { allLocales = Bool;
  buildPackages = γ;
  callPackage =
    Path ->
    {stdenv = α;} ->
    { buildPhase = String;
      builder = Path;
      installPhase = String;
      meta = {description = String;};
      name = String;
      outputs = [String];
      setupHook = δ;
    } ->
    ε;
  locales = [String];
  stdenv = α;
  writeText = String -> String -> δ;
} ->
ε