∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.windowmaker = ε,
  γ.mkEnableOption = String -> ζ,
  γ.mkIf =
    η ->
    { environment = {systemPackages = [θ];};
      services = {xserver = {windowManager = {session = ι;};};};
    } ->
    κ,
  γ.singleton = {name = String; start = String;} -> ι,
  δ.windowmaker = θ,
  ε.enable = η,
  λ.windowManager = β,
  μ.xserver = λ,
  ν.services = μ
) =>
α ->
{ config = κ;
  options =
    { services = {xserver = {windowManager = {windowmaker = {enable = ζ;};};};};
    };
}