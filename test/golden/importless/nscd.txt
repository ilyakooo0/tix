∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { etc = { nscd.conf = { text = ∀ β. β } } }; systemd = { services = { nscd = { description = String; environment = { LD_LIBRARY_PATH = ∀ α. α }; restartTriggers = ∀ β. [β]; serviceConfig = { DynamicUser = Bool; ExecReload = [String]; ExecStart = String; PIDFile = String; Restart = String; RuntimeDirectory = String; Type = String }; wantedBy = [String] } } } }; options = { services = { nscd = { config = { default = Path; description = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β } } } } }