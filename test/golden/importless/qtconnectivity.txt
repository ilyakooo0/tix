∀ α β γ δ ε ζ η.
(α.isLinux = β, α.optional = β -> γ -> δ, ε.lib = α) => { bluez = γ;
  qtModule =
    {buildInputs = δ; name = String; outputs = [String]; qtInputs = [ζ];} -> η;
  qtbase = ζ;
  qtdeclarative = ζ;
  stdenv = α;
} ->
η