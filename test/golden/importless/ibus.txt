∀ α β γ.
(α.ibus = β) => { makeInstalledTest =
    { testConfig =
        { i18n = {inputMethod = {enabled = String;};};
          systemd =
            { user =
                { services =
                    { ibus-daemon =
                        { partOf = [String];
                          serviceConfig = {ExecStart = String;};
                          wantedBy = [String];
                        };
                    };
                };
            };
        };
      tested = β;
      withX11 = Bool;
    } ->
    γ;
  pkgs = α;
} ->
γ