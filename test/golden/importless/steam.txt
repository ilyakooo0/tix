∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.steam = ζ,
  γ.maintainers = η,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { environment = {systemPackages = [κ];};
      hardware =
        { opengl = {driSupport32Bit = Bool; enable = Bool;};
          pulseaudio = {support32Bit = ε;};
          steam-hardware = {enable = Bool;};
        };
    } ->
    λ,
  γ.mkg20001 = μ,
  δ.steam = κ,
  ζ.enable = ι,
  ν.programs = β,
  ξ.pulseaudio = β,
  ο.hardware = ξ
) =>
α ->
{ config = λ;
  meta = {maintainers = [μ];};
  options = {programs = {steam = {enable = θ;};};};
}