∀ α β γ δ ε ζ η θ ι.
( α.teamviewer = β,
  γ.mkEnableOption = String -> δ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      systemd =
        { services =
            { teamviewerd =
                { after = [String];
                  description = String;
                  preStart = String;
                  serviceConfig =
                    { ExecReload = String;
                      ExecStart = String;
                      PIDFile = String;
                      Restart = String;
                      StartLimitBurst = String;
                      StartLimitInterval = String;
                      Type = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    η,
  θ.teamviewer = ζ,
  β.enable = ε,
  ι.services = α
) =>
{config = α; lib = γ; pkgs = θ;} ->
{config = η; options = {services = {teamviewer = {enable = δ;};};};}