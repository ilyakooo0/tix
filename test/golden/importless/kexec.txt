∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.hostPlatform = β,
  α.kexectools = γ,
  α.platforms = δ,
  ε.any = ζ -> δ -> η,
  ε.mkIf =
    η ->
    { environment = {systemPackages = [γ];};
      systemd =
        { services =
            { prepare-kexec =
                { before = [String];
                  description = String;
                  path = [θ];
                  script = String;
                  serviceConfig = {Type = String;};
                  unitConfig = {DefaultDependencies = Bool;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  ε.platformMatch = β -> ζ,
  κ.stdenv = α,
  λ.meta = α,
  μ.kexectools = λ,
  ν.meta = ε
) =>
{lib = ε; pkgs = α;} -> {config = ι;}