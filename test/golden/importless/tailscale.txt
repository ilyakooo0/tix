∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.tailscale = ε,
  γ.danderson = ζ,
  γ.maintainers = η,
  γ.mbaillie = ζ,
  γ.mkEnableOption = String -> θ,
  γ.mkIf =
    ι ->
    { systemd =
        { services =
            { tailscale =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { CacheDirectory = String;
                      CacheDirectoryMode = Integer;
                      ExecStart = String;
                      Restart = String;
                      RuntimeDirectory = String;
                      RuntimeDirectoryMode = Integer;
                      StateDirectory = String;
                      StateDirectoryMode = Integer;
                    };
                  unitConfig =
                    { StartLimitBurst = Integer;
                      StartLimitIntervalSec = Integer;
                    };
                  wantedBy = [String];
                  wants = [String];
                };
            };
        };
    } ->
    κ,
  γ.mkOption = {default = Integer; description = String; type = λ;} -> μ,
  γ.types = ν,
  ε.enable = ι,
  ξ.services = β,
  ν.port = λ
) =>
α ->
{ config = κ;
  meta = {maintainers = [ζ];};
  options = {services = {tailscale = {enable = θ; port = μ;};};};
}