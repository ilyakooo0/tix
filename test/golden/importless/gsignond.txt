∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { gsignond.conf = { source = String } } }; services = { dbus = { packages = [{ plugins = ∀ α. α }] } } }; meta = { maintainers = ∀ γ. γ }; options = { services = { gsignond = { enable = { default = Bool; description = String; type = ∀ β. β }; plugins = { default = ∀ β. β; description = String; type = ∀ β. β } } } } }