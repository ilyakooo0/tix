∀ α β γ δ ε ζ η θ ι κ λ.
( α.linux = β,
  α.mkDerivation =
    { installPhase = String;
      meta = {broken = Bool; platforms = β;};
      name = String;
      phases = [String];
      src = γ;
    } ->
    δ,
  ε.makeBinPath = [ζ] -> η,
  θ.src = γ,
  ι.platforms = α,
  κ.lib = ι
) =>
{file = ζ; lib = ε; mumble = θ; mumble_i686 = λ; stdenv = α; which = ζ;} -> δ