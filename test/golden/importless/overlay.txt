∀ ε ζ η θ ι κ.
( ε.mkDerivation =
    { installPhase = String;
      meta =
        { broken = Bool;
          platforms = ∀ α β γ. (ε.lib = α, α.platforms = β, β.linux = γ) => γ;
        };
      name = String;
      phases = [String];
      src = ∀ δ. (ζ.src = δ) => δ;
    } ->
    η
) =>
{file = θ; lib = ι; mumble = ζ; mumble_i686 = κ; stdenv = ε; which = θ;} -> η