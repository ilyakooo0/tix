∀ β γ δ. { config = β; lib = γ; pkgs = δ } -> { config = [{ services = { cron = { systemCronJobs = ∀ α. α } } }]; options = { services = { rsnapshot = { cronIntervals = { default = {  }; description = String; example = { daily = String; hourly = String }; type = ∀ α. α }; enable = String; enableManualRsnapshot = { default = Bool; description = String; type = ∀ α. α }; extraConfig = { default = String; description = String; example = String; type = ∀ α. α } } } } }