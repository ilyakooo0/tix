∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = [{ services = { mongodb = { enable = Bool } }; systemd = { services = { uptime = { description = String; environment = { NODE_CONFIG_DIR = String; NODE_ENV = ∀ α. α; NODE_PATH = String }; preStart = String; serviceConfig = { ExecStart = String }; wantedBy = [String] } } } }]; options = { services = { uptime = { configFile = { default = Null; description = String; type = ∀ β. β }; enableSeparateMonitoringService = { default = ∀ β. β }; enableWebService = String; nodeEnv = { default = String; description = String; type = ∀ β. β }; usesRemoteMongo = { default = Bool; description = String; type = ∀ β. β } } } } }