∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; services = { dbus = { packages = ∀ β. [β] } } }; meta = { maintainers = ∀ δ. δ }; options = { services = { telepathy = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }