∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];};};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.teams = λ,
  γ.types = μ,
  δ.telepathy-mission-control = ζ,
  ν.telepathy = β,
  ξ.services = ν,
  λ.members = ο,
  π.gnome = λ,
  μ.bool = ι
) =>
α ->
{ config = θ;
  meta = {maintainers = ο;};
  options = {services = {telepathy = {enable = κ;};};};
}