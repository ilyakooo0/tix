∀ α β γ δ ε ζ η θ.
(α.redis = β, γ.exporters = α, δ.prometheus = γ, ε.services = δ) => { config =
    α;
  lib = ζ;
  options = η;
  pkgs = θ;
} ->
{port = Integer; serviceOpts = {serviceConfig = {ExecStart = String;};};}