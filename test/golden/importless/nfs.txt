∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { boot = { initrd = { kernelModules = [String] } }; environment = { etc = { idmapd.conf = { source = String }; nfs.conf = { source = ∀ β. β }; request-key.conf = { source = String } }; systemPackages = ∀ γ. [γ] }; services = { rpcbind = { enable = Bool } }; system = { fsPackages = ∀ γ. [γ] }; systemd = { packages = ∀ γ. [γ]; services = { auth-rpcgss-module = { unitConfig = { ConditionPathExists = [String] } }; nfs-blkmap = { restartTriggers = [∀ α. α] }; nfs-idmapd = { restartTriggers = [String] }; nfs-mountd = { enable = Bool; restartTriggers = [∀ α. α] }; nfs-server = { enable = Bool; restartTriggers = [∀ α. α] }; rpc-gssd = { restartTriggers = [∀ α. α]; unitConfig = { ConditionPathExists = [String] } }; rpc-statd = { preStart = String; restartTriggers = [∀ α. α] } }; targets = { nfs-client = { wantedBy = [String] } } } }; options = { services = { nfs = { extraConfig = { default = String; description = String; type = ∀ β. β } } } } }