∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { etc = { ankisyncd/ankisyncd.conf = { source = { sync_app = { auth_db_path = ∀ α. α; base_media_url = String; base_url = String; data_root = ∀ α. α; host = ∀ α. α; port = ∀ α. α; session_db_path = ∀ α. α } } } } }; networking = { firewall = { allowedTCPPorts = ∀ γ. [γ] } }; systemd = { services = { ankisyncd = { after = [String]; description = String; path = ∀ β. [β]; serviceConfig = { DynamicUser = Bool; ExecStart = String; Restart = String; StateDirectory = String; Type = String }; wantedBy = [String] } } } }; options = { services = { ankisyncd = { enable = String; host = { default = String; description = String; type = ∀ β. β }; openFirewall = { default = Bool; description = String; type = ∀ β. β }; package = { default = ∀ β. β; defaultText = String; description = String; type = ∀ β. β }; port = { default = Integer; description = String; type = ∀ β. β } } } } }