∀ α β γ δ ε ζ η θ ι κ.
( α.mkDerivation =
    { buildInputs = [β];
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          maintainers = [γ];
          platforms = δ;
        };
      name = String;
      src = ε;
    } ->
    ζ,
  α.offline = γ,
  α.unix = δ,
  η.maintainers = α,
  θ.lib = η,
  ι.platforms = α,
  κ.lib = ι
) =>
{ coreutils = β;
  fetchgit = {rev = String; sha256 = String; url = String;} -> ε;
  gawk = β;
  makeWrapper = β;
  stdenv = α;
  utillinux = β;
} ->
ζ