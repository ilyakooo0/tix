∀ δ ε ζ η.
( δ.mkDerivation =
    { buildInputs = [ε];
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          maintainers =
            ∀ α β γ. (δ.lib = α, α.maintainers = β, β.offline = γ) => [γ];
          platforms = ∀ α β γ. (δ.lib = α, α.platforms = β, β.unix = γ) => γ;
        };
      name = String;
      src = ζ;
    } ->
    η
) =>
{ coreutils = ε;
  fetchgit = {rev = String; sha256 = String; url = String;} -> ζ;
  gawk = ε;
  makeWrapper = ε;
  stdenv = δ;
  utillinux = ε;
} ->
η