∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.services = ε,
  γ.builtins = ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    θ ->
    { systemd =
        { services =
            { squeezelite =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { DynamicUser = Bool;
                      ExecStart = String;
                      StateDirectory = ι;
                      SupplementaryGroups = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    κ,
  γ.mkOption = {default = String; description = String; type = λ;} -> μ,
  γ.types = ν,
  ε.squeezelite = ξ,
  ξ.enable = θ,
  ζ.baseNameOf = String -> ι,
  ν.str = λ
) =>
α ->
{ config = κ;
  options = {services = {squeezelite = {enable = η; extraArguments = μ;};};};
}