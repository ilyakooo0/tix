∀ α β γ δ ε ζ η θ ι.
(α.getLib = β, α.optional = γ -> String -> δ, ε.isLinux = γ) => { lib = α;
  qtModule = {NIX_CFLAGS_COMPILE = δ; name = String; qtInputs = [ζ];} -> η;
  qtbase = ζ;
  stdenv = θ;
  systemd = ι;
} ->
η