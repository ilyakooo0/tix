∀ γ δ ε ζ η θ.
(γ.optional = δ) => { lib = γ;
  qtModule =
    { NIX_CFLAGS_COMPILE = ∀ α β. (ε.isLinux = α) => β;
      name = String;
      qtInputs = [ζ];
    } ->
    η;
  qtbase = ζ;
  stdenv = ε;
  systemd = θ;
} ->
η