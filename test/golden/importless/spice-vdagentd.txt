∀ α β γ δ ε ζ η θ ι.
( α.spice-vdagentd = β,
  γ.spice-vdagent = δ,
  ε.mkEnableOption = String -> ζ,
  ε.mkIf =
    η ->
    { environment = {systemPackages = [δ];};
      systemd =
        { services =
            { spice-vdagentd =
                { description = String;
                  preStart = String;
                  serviceConfig = {ExecStart = String; Type = String;};
                  wantedBy = [String];
                };
            };
        };
    } ->
    θ,
  β.enable = η,
  ι.services = α
) =>
{config = α; lib = ε; pkgs = γ;} ->
{config = θ; options = {services = {spice-vdagentd = {enable = ζ;};};};}