∀ γ δ ε ζ. { config = γ; lib = δ; pkgs = ε; utils = ζ } -> { config = { environment = { systemPackages = ∀ β. [β] }; systemd = { services = String } }; options = { services = { freefall = { devices = { default = [String]; description = String; type = ∀ α. α }; enable = { default = Bool; description = String; type = ∀ α. α }; package = { default = ∀ α. α; defaultText = String; description = String; type = ∀ α. α } } } } }