∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.clickhouse = ε,
  γ.mkDefault = String -> ζ,
  γ.mkEnableOption = String -> η,
  γ.mkIf =
    θ ->
    { environment =
        { etc =
            { clickhouse-server/config.xml = {source = String;};
              clickhouse-server/users.xml = {source = String;};
            };
          systemPackages = [ι];
        };
      systemd =
        { services =
            { clickhouse =
                { after = [String];
                  description = String;
                  serviceConfig =
                    { ConfigurationDirectory = String;
                      ExecStart = String;
                      Group = String;
                      LogsDirectory = String;
                      StateDirectory = String;
                      User = String;
                    };
                  wantedBy = [String];
                };
            };
        };
      time = {timeZone = ζ;};
      users =
        { groups = {clickhouse = {gid = κ;};};
          users =
            { clickhouse =
                {description = String; group = String; name = String; uid = λ;};
            };
        };
    } ->
    μ,
  δ.clickhouse = ι,
  ε.enable = θ,
  ν.services = β,
  ξ.gids = β,
  ο.ids = ξ,
  π.uids = β,
  ρ.ids = π
) =>
α -> {config = μ; options = {services = {clickhouse = {enable = η;};};};}