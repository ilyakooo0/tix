∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.stateVersion = ζ,
  δ.gnome-initial-setup = η,
  δ.writeScript = String -> String -> θ,
  δ.writeTextFile = {destination = String; name = String; text = String;} -> ι,
  γ.mkEnableOption = String -> κ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      systemd =
        { packages = [λ];
          user =
            { targets =
                { gnome-session = {wants = [String];};
                  gnome-session@gnome-initial-setup = {wants = [String];};
                };
            };
        };
    } ->
    μ,
  γ.optional = ν -> ι -> [η],
  γ.teams = ξ,
  γ.versionOlder = ζ -> String -> ν,
  ο.gnome-initial-setup = β,
  π.gnome3 = ο,
  ρ.services = π,
  σ.gnome3 = δ,
  τ.system = β,
  υ.gnome3 = δ,
  ξ.members = φ,
  χ.gnome = ξ
) =>
α ->
{ config = μ;
  meta = {maintainers = φ;};
  options = {services = {gnome3 = {gnome-initial-setup = {enable = κ;};};};};
}