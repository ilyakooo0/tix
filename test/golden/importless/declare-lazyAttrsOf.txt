∀ α β γ δ ε ζ η θ.
( (α) // ({emptyValue = {value = String;};}) ~ (β),
  γ.lib = δ,
  δ.lazyAttrsOf = β -> ε,
  δ.mkOption = {default = {}; type = ε;} -> ζ,
  δ.str = α,
  η.types = δ,
  θ.types = δ
) =>
γ -> {options = {value = ζ;};}