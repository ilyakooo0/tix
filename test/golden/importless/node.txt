∀ β γ δ ε. { config = β; lib = γ; options = δ; pkgs = ε } -> { extraOpts = { disabledCollectors = { default = ∀ α. α; description = String; example = String; type = ∀ α. α }; enabledCollectors = { default = ∀ α. α; description = String; example = String; type = ∀ α. α } }; port = Integer; serviceOpts = { serviceConfig = { DynamicUser = Bool; ExecStart = String; RuntimeDirectory = String } } }