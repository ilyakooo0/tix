∀ γ δ. { config = γ; lib = δ } -> { config = { services = { openssh = { enable = Bool; extraConfig = String } }; users = { users = { nix-ssh = { description = String; openssh = { authorizedKeys = { keys = ∀ α. α } }; uid = ∀ β. β; useDefaultShell = Bool } } } }; options = { nix = { sshServe = { enable = { default = Bool; description = String; type = ∀ β. β }; keys = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; protocol = { default = String; description = String; type = [String] } } } } }