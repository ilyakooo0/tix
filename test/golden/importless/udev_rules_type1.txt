∀ α β γ δ ε ζ η θ ι κ λ μ.
( α.linux = β,
  α.maintainers = γ,
  α.mkDerivation =
    { dontBuild = Bool;
      dontPatchELF = Bool;
      dontStrip = Bool;
      installPhase = String;
      meta =
        { description = String;
          homepage = String;
          license = δ;
          maintainers = [ε];
          platforms = β;
        };
      name = String;
      patchPhase = String;
      src = ζ;
      unpackPhase = String;
    } ->
    η,
  α.unfree = δ,
  θ.licenses = α,
  ι.lib = θ,
  γ.jraygauthier = ε,
  κ.lib = α,
  λ.platforms = α,
  μ.lib = λ
) =>
{ fetchurl = {sha256 = String; url = String;} -> ζ;
  libsaneUDevRuleNumber = String;
  stdenv = α;
} ->
η