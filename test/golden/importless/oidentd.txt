∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.oidentd = ζ,
  γ.mkIf =
    ε ->
    { systemd =
        { services =
            { oidentd =
                { after = [String];
                  script = String;
                  serviceConfig = {Type = String;};
                  wantedBy = [String];
                };
            };
        };
      users =
        { groups = {oidentd = {gid = ζ;};};
          users = {oidentd = {description = String; group = String; uid = η;};};
        };
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.types = λ,
  μ.oidentd = β,
  ν.services = μ,
  ξ.gids = β,
  ο.ids = ξ,
  π.uids = β,
  ρ.ids = π,
  λ.bool = ι
) =>
α -> {config = θ; options = {services = {oidentd = {enable = κ;};};};}