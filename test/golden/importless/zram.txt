∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { boot = { extraModprobeConfig = String }; services = { udev = { extraRules = String } }; swapDevices = Integer; system = { requiredKernelConfig = [String] }; systemd = { services = [{ after = [String]; description = String; restartIfChanged = Bool; restartTriggers = ∀ α. [α]; serviceConfig = { ExecStart = String; ExecStartPre = String; ExecStop = String; RemainAfterExit = Bool; Type = String }; unitConfig = { DefaultDependencies = Bool }; wants = [String] }] }; warnings = [String] }; options = { zramSwap = { algorithm = { default = String; description = String; example = String; type = ∀ β. β }; enable = { default = Bool; description = String; type = ∀ β. β }; memoryPercent = { default = Integer; description = String; type = ∀ β. β }; numDevices = { default = Integer; description = String; type = ∀ β. β }; priority = { default = Integer; description = String; type = ∀ β. β }; swapDevices = { default = Null; description = String; example = Integer; type = ∀ β. β } } } }