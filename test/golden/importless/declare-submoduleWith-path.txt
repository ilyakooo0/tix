∀ α β γ δ ε.
( α.lib = β,
  β.mkOption = {default = {}; type = γ;} -> δ,
  β.submoduleWith = {modules = [Path];} -> γ,
  ε.types = β
) =>
α -> {config = {submodule = Path;}; options = {submodule = δ;};}