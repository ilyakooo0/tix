∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ γ. [γ] }; services = { udev = { packages = ∀ β. [β] } } }; options = { programs = { light = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }