∀ ε ζ η. { config = ε; lib = ζ; pkgs = η } -> { config = { systemd = { packages = ∀ γ. [γ]; timers = { fstrim = { timerConfig = { OnCalendar = ∀ α. α }; wantedBy = [String] } } } }; meta = { maintainers = ∀ δ. [δ] }; options = { services = { fstrim = { enable = String; interval = { default = String; description = String; type = ∀ β. β } } } } }