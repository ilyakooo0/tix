∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.isContainer = ε,
  γ.mkDefault = Bool -> ζ,
  γ.mkIf =
    ε ->
    { documentation = {nixos = {enable = ζ;};};
      environment = {variables = {NIX_REMOTE = String;};};
      networking = {useHostResolvConf = η;};
      nix = {optimise = {automatic = θ;};};
      powerManagement = {enable = ι;};
      security = {audit = {enable = Bool;};};
      services = {openssh = {startWhenNeeded = κ;}; udisks2 = {enable = λ;};};
      system = {build = {installBootLoader = String;};};
    } ->
    μ,
  ν.boot = β
) =>
α -> {config = μ;}