∀ α β γ. { config = α; lib = β; pkgs = γ } -> { config = { documentation = { nixos = { enable = Bool } }; environment = { variables = { NIX_REMOTE = String } }; networking = { useHostResolvConf = Bool }; nix = { optimise = { automatic = Bool } }; powerManagement = { enable = Bool }; security = { audit = { enable = Bool } }; services = { openssh = { startWhenNeeded = Bool }; udisks2 = { enable = Bool } }; system = { build = { installBootLoader = String } } } }