∀ β γ δ.
( β.mkDerivation =
    { allowedReferences = [String];
      buildCommand = String;
      meta = ∀ α. (γ.meta = α) => α;
      name = ∀ α. (γ.name = α) => α;
    } ->
    δ
) =>
{klibc = γ; stdenv = β;} -> δ