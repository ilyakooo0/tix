∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ.
( α.darwin = [β],
  α.lgpl3 = γ,
  α.linux = [β],
  α.mkDerivation =
    { buildInputs = [δ];
      configurePhase = String;
      installPhase = String;
      meta =
        {description = String; homepage = ε; license = γ; platforms = [β];};
      name = String;
      patches = [Path];
      src = ζ;
    } ->
    η,
  ζ.homepage = ε,
  θ.licenses = α,
  ι.lib = θ,
  κ.platforms = α,
  λ.lib = κ,
  μ.platforms = α,
  ν.lib = μ,
  ξ.meta = ζ
) =>
{ fetchFromGitHub =
    {owner = String; repo = String; rev = String; sha256 = String;} -> ζ;
  pixie = δ;
  stdenv = α;
} ->
η