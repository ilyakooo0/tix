∀ γ δ ε ζ.
{ buildDunePackage =
    { buildInputs = [γ];
      checkInputs = [δ];
      doCheck = Bool;
      meta = ∀ α β. ((α) // ({description = String;}) ~ (β), ε.meta = α) => β;
      minimumOCamlVersion = ∀ α. (ε.minimumOCamlVersion = α) => α;
      pname = String;
      propagatedBuildInputs = [ε];
      src = ∀ α. (ε.src = α) => α;
      useDune2 = ∀ α. (ε.useDune2 = α) => α;
      version = ∀ α. (ε.version = α) => α;
    } ->
    ζ;
  cstruct = ε;
  eqaf = ε;
  gmp = γ;
  mirage-crypto = ε;
  mirage-crypto-rng = ε;
  ounit = δ;
  ppx_sexp_conv = ε;
  randomconv = δ;
  rresult = ε;
  sexplib = ε;
  zarith = ε;
} ->
ζ