∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  β.rpc = ζ,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [η];};
      systemd =
        {packages = [θ]; services = {rpcbind = {wantedBy = [String];};};};
      users = {users = {rpc = {group = String; uid = ζ;};};};
    } ->
    ι,
  γ.mkOption = {default = Bool; description = String; type = κ;} -> λ,
  γ.types = μ,
  δ.rpcbind = η,
  ν.rpcbind = β,
  ξ.services = ν,
  ο.uids = β,
  π.ids = ο,
  μ.bool = κ
) =>
α -> {config = ι; options = {services = {rpcbind = {enable = λ;};};};}