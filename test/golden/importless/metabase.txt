∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { networking = { firewall = { allowedTCPPorts = ∀ γ. [γ] } }; systemd = { services = { metabase = { after = [String]; description = String; environment = { MB_DB_FILE = String; MB_JETTY_HOST = ∀ α. α; MB_JETTY_PORT = ∀ α. α; MB_JETTY_SSL = Bool; MB_JETTY_SSL_KEYSTORE = ∀ α. α; MB_JETTY_SSL_PORT = ∀ α. α; MB_PLUGINS_DIR = String }; serviceConfig = { DynamicUser = Bool; ExecStart = String; StateDirectory = String }; wantedBy = [String] } } } }; options = { services = { metabase = { enable = String; listen = { ip = { default = String; description = String; type = ∀ α. α }; port = { default = Integer; description = String; type = ∀ α. α } }; openFirewall = { default = Bool; description = String; type = ∀ β. β }; ssl = { enable = { default = Bool; description = String; type = ∀ α. α }; keystore = { default = String; description = String; example = String; type = ∀ α. α }; port = { default = Integer; description = String; type = ∀ α. α } } } } } }