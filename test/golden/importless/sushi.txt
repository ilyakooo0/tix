∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  β.enable = ε,
  γ.mkIf =
    ε ->
    { environment = {systemPackages = [ζ];};
      services = {dbus = {packages = [η];};};
    } ->
    θ,
  γ.mkOption = {default = Bool; description = String; type = ι;} -> κ,
  γ.teams = λ,
  γ.types = μ,
  δ.sushi = ζ,
  ν.sushi = β,
  ξ.gnome3 = ν,
  ο.services = ξ,
  π.gnome3 = δ,
  ρ.gnome3 = δ,
  λ.members = σ,
  τ.gnome = λ,
  μ.bool = ι
) =>
α ->
{ config = θ;
  meta = {maintainers = σ;};
  options = {services = {gnome3 = {sushi = {enable = κ;};};};};
}