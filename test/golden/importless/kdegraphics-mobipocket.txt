∀ γ δ ε ζ.
{ extra-cmake-modules = γ;
  kio = δ;
  lib = ε;
  mkDerivation =
    { buildInputs = [δ];
      meta =
        { license = ∀ α β. (ε.licenses = α, α.gpl2Plus = β) => [β];
          maintainers = ∀ α β. (ε.maintainers = α, α.ttuegel = β) => [β];
        };
      name = String;
      nativeBuildInputs = [γ];
      outputs = [String];
    } ->
    ζ;
} ->
ζ