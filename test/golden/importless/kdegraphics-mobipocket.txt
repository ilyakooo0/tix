∀ α β γ δ ε ζ η θ.
( α.gpl2Plus = β,
  α.ttuegel = γ,
  δ.licenses = α,
  ε.maintainers = α
) =>
{ extra-cmake-modules = ζ;
  kio = η;
  lib = α;
  mkDerivation =
    { buildInputs = [η];
      meta = {license = [β]; maintainers = [γ];};
      name = String;
      nativeBuildInputs = [ζ];
      outputs = [String];
    } ->
    θ;
} ->
θ