∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο.
( α.config = β,
  α.lib = γ,
  α.pkgs = δ,
  ε.magic-wormhole-mailbox-server = ζ,
  ε.twisted = η,
  β.magic-wormhole-mailbox-server = θ,
  γ.baseNameOf = String -> ι,
  γ.mkEnableOption = String -> κ,
  γ.mkIf =
    λ ->
    { systemd =
        { services =
            { magic-wormhole-mailbox-server =
                { after = [String];
                  serviceConfig =
                    { DynamicUser = Bool;
                      ExecStart = String;
                      StateDirectory = ι;
                      WorkingDirectory = String;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    μ,
  δ.withPackages = (ε -> [ζ]) -> ν,
  ξ.python3 = δ,
  θ.enable = λ,
  ο.services = β
) =>
α ->
{ config = μ;
  options = {services = {magic-wormhole-mailbox-server = {enable = κ;};};};
}