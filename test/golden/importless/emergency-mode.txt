∀ α β γ δ ε ζ η θ ι.
( α.config = β,
  α.lib = γ,
  β.enableEmergencyMode = δ,
  γ.mkOption = {default = Bool; description = String; type = ε;} -> ζ,
  γ.optionals = δ -> [String] -> η,
  γ.types = θ,
  ι.systemd = β,
  θ.bool = ε
) =>
α ->
{ config = {systemd = {additionalUpstreamSystemUnits = η;};};
  options = {systemd = {enableEmergencyMode = ζ;};};
}