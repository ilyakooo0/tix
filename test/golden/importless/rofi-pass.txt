∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π.
( α.gpl3 = β,
  α.lib = γ,
  α.mkDerivation =
    { buildInputs = [δ];
      dontBuild = Bool;
      fixupPhase = String;
      installPhase = String;
      meta =
        {description = String; homepage = String; license = β; platforms = ε;};
      pname = String;
      src = ζ;
      version = String;
      wrapperPath = η;
    } ->
    θ,
  α.platforms = ι,
  κ.withExtensions = (λ -> [μ]) -> ν,
  λ.pass-otp = μ,
  γ.makeBinPath = [ν] -> η,
  ξ.licenses = α,
  ο.lib = ξ,
  ι.linux = ε,
  π.lib = α
) =>
{ coreutils = ν;
  fetchFromGitHub =
    {owner = String; repo = String; rev = String; sha256 = String;} -> ζ;
  findutils = ν;
  gawk = ν;
  gnugrep = ν;
  gnused = ν;
  libnotify = ν;
  makeWrapper = δ;
  pass = κ;
  pwgen = ν;
  rofi = ν;
  stdenv = α;
  utillinux = ν;
  xclip = ν;
  xdotool = ν;
} ->
θ