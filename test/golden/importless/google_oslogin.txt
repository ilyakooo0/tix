∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { environment = { etc = { ssh/authorized_keys_command_google_oslogin = { mode = String; text = String } } }; security = { pam = { services = { sshd = { googleOsLoginAccountVerification = Bool; makeHomeDir = Bool } } }; sudo = { extraConfig = String } }; services = { openssh = { authorizedKeysCommand = String; authorizedKeysCommandUser = String } }; system = { nssDatabases = { group = [String]; passwd = [String] }; nssModules = [∀ β. β] }; systemd = { tmpfiles = { rules = [String] } } }; options = { security = { googleOsLogin = { enable = { default = Bool; description = String; type = ∀ α. α } } } } }