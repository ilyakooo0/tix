∀ ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ φ χ ψ ω α1 β2 γ3 δ4 ε5 ζ6 η7 θ8 ι9 κ10 λ11 μ12.
( ζ.services = η,
  ζ.services = θ,
  ζ.services = ι,
  ζ.services = κ,
  ζ.services = λ,
  ζ.services = μ,
  ζ.services = ν,
  ζ.services = ξ,
  ζ.services = ο,
  ζ.services = π,
  ρ.literalExample = σ,
  ρ.mkEnableOption = τ,
  ρ.mkIf = υ,
  ρ.mkOption = φ,
  ρ.mkOption = χ,
  ρ.mkOption = ψ,
  ρ.mkOption = ω,
  ρ.mkOption = α1,
  ρ.mkOption = β2,
  ρ.types = γ3,
  ρ.types = δ4,
  ρ.types = ε5,
  ρ.types = ζ6,
  ρ.types = η7,
  ρ.types = θ8,
  ρ.types = ι9,
  ρ.types = κ10,
  λ11.runCommand = String -> {} -> String -> μ12
) =>
{config = ζ; lib = ρ; pkgs = λ11;} ->
{ config = ∀ β γ δ ε. (β.loki = γ, γ.enable = δ) => ε;
  options =
    { services =
        { loki =
            { configFile = ∀ α. α;
              configuration = ∀ α. α;
              dataDir = ∀ α. α;
              enable = ∀ α. α;
              extraFlags = ∀ α. α;
              group = ∀ α. α;
              user = ∀ α. α;
            };
        };
    };
}