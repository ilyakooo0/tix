∀ γ δ ε. { config = γ; lib = δ; pkgs = ε } -> { config = { assertions = [{ assertion = Bool; message = String }]; systemd = { services = { loki = { description = String; serviceConfig = { DevicePolicy = String; ExecStart = String; NoNewPrivileges = Bool; PrivateTmp = Bool; ProtectHome = Bool; ProtectSystem = String; Restart = String; User = ∀ α. α; WorkingDirectory = ∀ α. α }; wantedBy = [String] } } } }; options = { services = { loki = { configFile = { default = Null; description = String; type = ∀ β. β }; configuration = { default = {  }; description = String; type = ∀ β. β }; dataDir = { default = String; description = String; type = ∀ β. β }; enable = String; extraFlags = { default = ∀ β. β; description = String; example = [String]; type = ∀ β. β }; group = { default = String; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }