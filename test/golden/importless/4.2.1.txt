∀ δ ε ζ η θ.
( δ.mkDerivation =
    { buildInputs = [ε];
      configureScript = String;
      meta =
        { description = String;
          homepage = String;
          license = ∀ α β γ. (δ.lib = α, α.licenses = β, β.gpl2 = γ) => γ;
          maintainers =
            ∀ α β γ. (δ.lib = α, α.maintainers = β, β.wmertens = γ) => [γ];
          platforms = ∀ α β γ. (δ.lib = α, α.platforms = β, β.linux = γ) => γ;
        };
      name = String;
      nativeBuildInputs = [ζ];
      passthru = {libdvdread = ε;};
      preConfigure = String;
      src = η;
    } ->
    θ
) =>
{ fetchurl = {sha256 = String; url = String;} -> η;
  libdvdread = ε;
  pkgconfig = ζ;
  stdenv = δ;
} ->
θ