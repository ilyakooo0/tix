∀ α β γ δ ε ζ η θ ι κ λ μ ν.
( α.ocserv = β,
  γ.ocserv = δ,
  ε.mkEnableOption = String -> ζ,
  ε.mkIf =
    η ->
    { environment =
        {etc = {ocserv/ocserv.conf = {text = θ;};}; systemPackages = [δ];};
      security = {pam = {services = {ocserv = {};};};};
      systemd =
        { services =
            { ocserv =
                { after = [String];
                  description = String;
                  documentation = [String];
                  serviceConfig =
                    { ExecReload = String;
                      ExecStart = String;
                      PIDFile = String;
                      PrivateTmp = Bool;
                    };
                  wantedBy = [String];
                };
            };
        };
    } ->
    ι,
  ε.mkOption = {description = String; example = String; type = κ;} -> λ,
  ε.types = μ,
  β.config = θ,
  β.enable = η,
  ν.services = α,
  μ.lines = κ
) =>
{config = α; lib = ε; pkgs = γ;} ->
{config = ι; options = {services = {ocserv = {config = λ; enable = ζ;};};};}