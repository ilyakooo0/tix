∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { systemd = { services = { nexus = { description = String; environment = { NEXUS_HOME = ∀ α. α; NEXUS_USER = ∀ α. α; VM_OPTS_FILE = ∀ α. α }; path = ∀ β. [β]; preStart = String; script = String; serviceConfig = { Group = ∀ α. α; LimitNOFILE = Integer; PrivateTmp = Bool; User = ∀ α. α }; wantedBy = [String] } } } }; meta = { maintainers = ∀ γ. [γ] }; options = { services = { nexus = { enable = String; group = { default = String; description = String; type = ∀ β. β }; home = { default = String; description = String; type = ∀ β. β }; jvmOpts = { default = String; description = String; type = ∀ β. β }; listenAddress = { default = String; description = String; type = ∀ β. β }; listenPort = { default = Integer; description = String; type = ∀ β. β }; package = { default = ∀ β. β; description = String; type = ∀ β. β }; user = { default = String; description = String; type = ∀ β. β } } } } }