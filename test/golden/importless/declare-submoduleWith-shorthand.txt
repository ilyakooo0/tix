∀ η.
{lib = η;} ->
{ options =
    { submodule =
        ∀ ζ.
        ( η.mkOption =
            { default = {};
              type =
                ∀ δ ε.
                ( η.types = δ,
                  δ.submoduleWith =
                    { modules =
                        ∀ α β γ.
                        [ { options =
                              { config =
                                  ( η.mkOption =
                                      { default = Bool;
                                        type = (η.types = α, α.bool = β) => β;
                                      } ->
                                      γ
                                  ) =>
                                  γ;
                              };
                          }
                        ];
                      shorthandOnlyDefinesConfig = Bool;
                    } ->
                    ε
                ) =>
                ε;
            } ->
            ζ
        ) =>
        ζ;
    };
}