∀ γ δ ε ζ. { config = γ; lib = δ; pkgs = ε; utils = ζ } -> { config = [{ boot = { initrd = { extraUtilsCommands = String; extraUtilsCommandsTest = String; kernelModules = [String]; postDeviceCommands = String } }; system = { fsPackages = ∀ β. [β] } }]; options = { services = { btrfs = { autoScrub = { enable = String; fileSystems = { description = String; example = [String]; type = ∀ α. α }; interval = { default = String; description = String; example = String; type = ∀ α. α } } } } } }