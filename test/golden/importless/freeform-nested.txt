∀ δ.
{lib = δ;} ->
{ config = {nest = {bar = String;};};
  options =
    { nest =
        { foo =
            ∀ γ.
            ( δ.mkOption =
                { default = Bool;
                  type = ∀ α β. (δ.types = α, α.bool = β) => β;
                } ->
                γ
            ) =>
            γ;
        };
    };
}