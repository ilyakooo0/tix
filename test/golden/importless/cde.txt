∀ δ ε ζ. { config = δ; lib = ε; pkgs = ζ } -> { config = { environment = { systemPackages = ∀ β. β }; security = { wrappers = { dtmail = { group = String; setgid = Bool; source = String } } }; services = { rpcbind = { enable = Bool }; xinetd = { enable = Bool; services = [{ extraConfig = String; name = String; protocol = String; server = String; user = String }] }; xserver = { desktopManager = { session = [{ name = String; start = String }] } } }; system = { activationScripts = { setup-cde = String } }; users = { groups = { mail = {  } } } }; meta = { maintainers = ∀ γ. [γ] }; options = { services = { xserver = { desktopManager = { cde = { enable = String; extraPackages = { default = ∀ α. [α]; description = String; example = String; type = ∀ α. α } } } } } } }