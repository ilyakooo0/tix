∀ β γ δ ε ζ η θ ι κ.
( β.mapAttrsToList = (γ -> δ -> String) -> ε -> ζ,
  β.concatStrings = ζ -> String
) =>
{ fetchurl =
    { meta = {homepage = String;};
      sha256 = ∀ α. (δ.sha256 = α) => α;
      url = String;
    } ->
    η;
  lib = β;
  registryDat = θ;
  stdenv = ι;
} ->
κ -> ε -> String