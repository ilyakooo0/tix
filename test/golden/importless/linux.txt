∀ α β γ δ ε.
( α.buildPythonPackage =
    { patches = [Path];
      pname = String;
      postFixup = String;
      postInstall = String;
      setSourceRoot = String;
      src = β;
      version = String;
    } ->
    γ,
  α.isPy27 = Bool,
  δ.pkgs = α,
  ε.pkgs = α
) =>
{fetchurl = {sha256 = String; url = String;} -> β; python = α;} -> γ