∀ α β γ δ ε ζ η θ ι κ λ μ ν ξ ο π ρ σ τ υ.
( α.enable = β,
  γ.mkIf =
    β ->
    { environment = {systemPackages = [δ];};
      services = {dbus = {packages = [ε];};};
      systemd = {packages = [ζ];};
    } ->
    η,
  γ.mkOption = {default = Bool; description = String; type = θ;} -> ι,
  γ.teams = κ,
  γ.types = λ,
  μ.evolution-data-server = δ,
  ν.evolution-data-server = α,
  ξ.gnome3 = ν,
  ο.services = ξ,
  κ.members = π,
  ρ.gnome = κ,
  λ.bool = θ,
  σ.gnome3 = μ,
  τ.gnome3 = μ,
  υ.gnome3 = μ
) =>
{config = α; lib = γ; pkgs = μ;} ->
{ config = η;
  meta = {maintainers = π;};
  options = {services = {gnome3 = {evolution-data-server = {enable = ι;};};};};
}