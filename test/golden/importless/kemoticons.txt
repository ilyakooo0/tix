∀ α β γ δ ε ζ η.
(α.ttuegel = β, γ.maintainers = α) => { extra-cmake-modules = δ;
  karchive = ε;
  kcoreaddons = ε;
  kservice = ζ;
  lib = α;
  mkDerivation =
    { buildInputs = [ε];
      meta = {maintainers = [β];};
      name = String;
      nativeBuildInputs = [δ];
      propagatedBuildInputs = [ζ];
    } ->
    η;
  qtbase = ζ;
} ->
η