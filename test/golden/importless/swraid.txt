∀ α β γ δ.
(α.mdadm = β) => {pkgs = α;} ->
{ boot =
    { initrd =
        {availableKernelModules = [String]; extraUdevRulesCommands = String;};
    };
  environment = {systemPackages = [β];};
  services = {udev = {packages = [γ];};};
  systemd = {packages = [δ];};
}