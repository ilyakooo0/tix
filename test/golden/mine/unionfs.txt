∀ λ μ ν.
{config = λ; lib = μ; pkgs = ν;} ->
{ config =
    ∀ β γ δ ε ζ η θ ι κ.
    ( μ.mkMerge = [β] -> γ,
      μ.mkIf =
        δ ->
        { boot =
            { initrd =
                { extraUtilsCommands = String;
                  kernelModules = [String];
                  postDeviceCommands = String;
                };
            };
        } ->
        β,
      μ.any = (String -> Bool) -> ε -> δ,
      λ.boot = ζ,
      ζ.initrd = η,
      η.supportedFilesystems = ε,
      μ.mkIf =
        θ -> {system = {fsPackages = ∀ α. (ν.unionfs-fuse = α) => [α];};} -> β,
      μ.any = (String -> Bool) -> ι -> θ,
      λ.boot = κ,
      κ.supportedFilesystems = ι
    ) =>
    γ;
}